# Complete UI Reskin — Execution Ledger

Status: active implementation program  
Authority: `handoff/COMPLETE_UI_RESKIN_WORK_ORDER.md` plus its approved reference images  
Updated handoff: includes `night-market-approved.png`

## Locked product decisions

- One adaptive HUD: peaceful = Build, Hero, Journey, Manage; combat = Attack, Block, Skill I, Skill II, Skill III, Item.
- No minimap, HUD skill-points value, Realm Map, Season Pass, or dead `Map soon` entry.
- Item opens a compact paused consumable picker; it replaces dedicated potion buttons.
- Equipment and Store show gold only. Night Market shows live SKR wallet/network state and preserves its required purchase disclosures.
- Queue capacity is five, but `0/5 queued` is not repeated on Manage category cards.
- Runtime values remain authoritative; reference-image values are never copied into gameplay state.

## Migration inventory

| Surface | Reference / component family | State | Required evidence |
|---|---|---:|---|
| Pause | `pause-approved.png` | Implemented, capture review passed | 1920x1080, 2340x1080, 2670x1200 |
| Settings | full medieval shell + scroll controls | Implemented; three-ratio visual pass | default + changed controls |
| Peaceful HUD | `hud-peaceful-approved.png` | Implemented; three-ratio visual and regression pass | all supported ratios; stable four-slot dock |
| Combat HUD | `hud-combat-approved.png` | Implemented; three-ratio visual and regression pass | six actions; cooldown; target; transition |
| Item picker | compact modal shell | Implemented; three-ratio capture passed and visually reviewed | open freezes; use/cancel atomic; zero/cooldown |
| Manage hub | approved Manage reference/art kit | Implemented; three-ratio visual pass | four destinations; locked Troops |
| Manage — Defense | shared Manage destination | Complete; live queue/timer and three-ratio visual evidence passed | full queue; timer; unaffordable |
| Manage — Buildings | shared Manage destination | Complete; live queue/timer and three-ratio visual evidence passed | full queue; timer; unaffordable |
| Manage — Troops | `manage-troops-approved.png` | Complete split selector/detail workspace with authoritative troop data, explicit locks, paging, Train/Upgrade modes, production lines, queue access, and three-ratio evidence | training stack; active timer |
| Manage — Research | shared Manage destination | Complete; live priced/timed rows, locks, paging, queue access, and three-ratio evidence passed | locked; active timer; paging |
| Hero shell | shared player-deck + three-column detail shell | Medieval four-card launcher implemented and captured at three ratios; detail surfaces tracked separately | live portrait/resources |
| Equipment | two approved equipment references | Complete; truthful compare/EQUIP and equipped/REMOVE states captured with authoritative inventory and regression green | compare, Equip, Remove, empty slot |
| Talents | shared Hero shell | Full-body medieval talent graph implemented with readable two-line node names, explicit locks, framed Back/Equipment, compact titled spend popup, and three-ratio popup evidence | owned/available spend-popup evidence captured |
| Skills | shared Hero shell | Four bounded medieval hot-swap action cards implemented; Wisdom and skill points remain exclusive to this destination; assigned-active state captured | assigned-active slot captured |
| Build / Build New | shared cards/modal shell | Complete; category launcher, category return, defensive-upgrade door, placement chips, confirmation/orientation, and timer routes are captured/regression-covered | category return; placement; timer |
| Bag / Item | shared item cards/detail | Medieval shell plus populated selection/use are implemented and production-path asserted at three ratios | Complete: selected x3, HP 45→75, inventory x3→x2, touch/geometry clean |
| Journey — Quest | shared destination shell | Three-poster medieval board implemented; latest screenshot review corrected header density, footer escape, and dense reward overflow | empty/active/reward |
| Journey — Raid | shared destination shell | Medieval camp selector implemented; word-carried difficulty, reclaimed first fold, and ornate Close are in final capture iteration | locked/ready/deploy |
| Realm hub | shared destination shell | Medieval four-card launcher implemented and captured at three ratios; Map and Season Pass absent | destination-state evidence |
| Realm Store | `realm-store-approved.png` | Gold storefront and affordable multi-quantity execution are complete; canonical consumable IDs/names/prices, gold-only wallet, quantity/total, exact debit/credit, success feedback, and three-ratio touch/geometry proof are green | Complete: quantity 3; gold 100→76; inventory 0→3 |
| Night Market | `night-market-approved.png` | Approved merchant subtheme, header art, two-card pack grid, spotlight, framed Actions/catch-up offers, bounded unavailable prices, disclosures, and Close now pass all four authoritative target sizes | live purchase execution remains a packaging/commerce evidence gate |
| Echoes | shared destination shell | Medieval roster and per-Echo assignment card implemented with three-ratio owned/locked/detail evidence | roster/detail/locked |
| Confirmation / warning | shared modal shell | Complete for mapped public routes; build orientation, jewel re-polish, ad consent, offline opt-in, and harvest-overflow decisions have deterministic ratio evidence and rapid-tap regression coverage | confirm/cancel; rapid-tap guard |
| Reward / error / information | shared modal/toast | Complete for mapped public routes; shared toasts, Echo unlock, Welcome Back, Daily Chest, maintenance, wave-clear, and harvest overflow carry deterministic worst-case evidence | representative mapped call sites |
| Title / loading / profile | shared public chrome | Complete UI presentation: Title, normal Login, simulated `GOOGLE_PLAY` production-builder Login, Hero Select, loading, and first-run recovery captured at three ratios; physical AAB scan remains a packaging gate | Google Play-safe production state |

## Shared system status

- Medieval art imported under `Assets/Resources/UI/ElarionMedieval/`.
- `MedievalUiSkin` supplies scalable shell, primary/standard button, and ornate Close treatments.
- Importer supplies nine-slice borders for scalable frames, tabs, progress, and dividers.
- Shared title bars, medallions, status/resource groups, selectors, cards, list rows, equipment slots, progress/timers, lock treatments, toasts, and compact picker controls now cover the mapped public routes. Legacy asset retirement remains reference-driven and must not precede a zero-consumer proof.

## Gate history

- Public-inventory truth and maintenance-strip continuation (2026-09-01): contact-sheet review of the fresh broad run found two obsolete surfaces being presented as current public evidence and one genuine live outlier. Realm Map is a locked retirement with no public navigation, and the standalone CoC queue rail is retired from the HUD in favor of Manage's live queue destinations; both dedicated forensic helpers remain available, but neither is emitted or counted by the public reskin harness. The live operator maintenance notice was a raw black rectangle, floating parchment copy, and detached gold line. It now uses the authored black-iron/gold action plate, reserves full width when no action exists, measures wrapped height against the real label lane, and seats the complete outage reason within the decorative frame. Screenshot review rejected four machine-green iterations before accepting the bounded fifth composition at 1920x1080 and 2670x1200. Focused evidence: `MAINTENANCE_BANNER_CAPTURE_OK 3/3` in `Builds/ui-reskin-maintenance-banner-v5.log`. Exact-state broad evidence: `UI_CAPTURE_OK 91`, `UI_GEOMETRY_OK 91`, and `UI_TOUCH_OK 91/91`, with no RealmMap or QueueCardRail emission, in `Builds/ui-reskin-broad-capture-v7-2026-09-01.log`.

- HUD/dialogue coherence continuation (2026-09-01): completed the remaining HUD-lane visual rejection loop without touching the parallel town/building redesign. Standard and compact dialogue now retain a continuous black-iron backing through the ornate frame; dialogue option and Echo-engage interaction captures remain green. The adaptive peaceful/combat dock now uses authored housing, canonical circular bezels with captions seated below the icon face, a top-right Item stack badge, and a compact two-column gear drawer that does not cover the Heart objective or joystick. Settings selected choices retain readable ivory labels, carry an explicit persistent gold selection marker, and use aspect-preserved circular slider handles. The Daily Quest empty state no longer expands a beige parchment slab across the modal; it uses the shared near-black surface with gold/ivory hierarchy. The compact combat Item picker hides the procedural header rule after replacing that header, eliminating the orphaned line above the modal. Focused evidence: `DIALOGUE_OPTIONS_CAPTURE_OK 6/6` in `Builds/ui-reskin-dialogue-options-v5.log`; `COMPACT_DIALOGUE_CAPTURE_OK 3/3 + INTERACTION_OK` in `Builds/ui-reskin-compact-dialogue-v4.log`; `ECHO_ENGAGE_DIALOGUE_OK` in `Builds/ui-reskin-dialogue-regression-v2.log`; `ADAPTIVE_HUD_CAPTURE_OK` in `Builds/ui-reskin-hud-drawer-medallion-v4.log`; `SETTINGS_CAPTURE_OK` in `Builds/ui-reskin-settings-v3.log`; and `COMBAT_ITEM_PICKER_CAPTURE_OK` in `Builds/ui-reskin-combat-item-v2.log`. Fresh broad evidence: `UI_CAPTURE_OK 97`, `UI_GEOMETRY_OK 97`, and `UI_TOUCH_OK 97/97` with no blank-render marker in `Builds/ui-reskin-broad-capture-v5-2026-09-01.log`. Direct 1920x1080 screenshot review accepted Settings, Daily Quest empty, and Combat Item picker after their final iterations. Cross-lane artifact/build hold remains: the concurrent town/building tree is reported at 30/33, and the current `D:\eoa` data run is 338/339 with only the out-of-lane `SyntyStructureRetheme` art-ledger finding red.

- Live tutorial/build screenshot correction (2026-09-01): Seeker/production screenshots invalidated the prior ship gate. Build Collections no longer instantiates semantic categories through the generic button factory; its two pages now contain bounded artwork/title/purpose cards, while Back/Next/Close remain controls. Echo awakening restores one localizable Close on empty medieval art (no blank action and no baked/runtime duplicate). The persistent Echo count retires its final silver plate for black-iron/gold chrome. The one tutorial Skip now has a deterministic framed face and a dedicated three-ratio evidence entry point. World interaction identities retire always-visible yellow square plates: they use the circular four-point medallion and reveal only within a 10m proximity band with 12m hysteresis. Focused evidence: `BUILD_COLLECTIONS_CAPTURE_OK 6/6` in `Builds/ui-reskin-build-collections-card-v2.log`, `ECHO_UNLOCK_CAPTURE_OK 3/3` in `Builds/ui-reskin-echo-unlock-close-v3.log`, `ECHO_CHIP_CAPTURE_OK 3/3` in `Builds/ui-reskin-echo-chip-v1.log`, and `TUTORIAL_SKIP_CAPTURE_OK 3/3` in `Builds/ui-reskin-tutorial-skip-v7.log`. Broad evidence: `REGRESSION_OK 339/339 suites -- 339 green, 0 red, 0 skipped` in `Builds/ui-reskin-screenshot-fixes-regression-v1.log`; `UI_CAPTURE_OK 94`, `UI_GEOMETRY_OK 100`, `UI_TOUCH_OK 100/100`, and `UI_ENDSTATE_FIT_OK 6` in `Builds/ui-reskin-screenshot-fixes-full-audit-v1.log`; `REGISTERED_SECONDARY_CAPTURE_OK 33/33`, `UI_GEOMETRY_OK 33`, and `UI_TOUCH_OK 33/33` in `Builds/ui-reskin-screenshot-fixes-secondary-v1.log`.

- Party Shop populated Forge v5 (2026-09-01): replaced the misleading sparse generic-route frame with a deterministic production-context proof containing a real Knight, 500 live gold, Forge catalog stock, selected weapon, preview identity, filters, and authoritative purchase/equip states. RCA found that the complete landscape workspace had been seated inside the merchant frame's shallow legacy body drop-zone, collapsing party and filter controls into unlabeled 15–40px rails. The workspace now owns explicit safe inner-panel bands; dynamic list/party/type/spec rebuilds detach stale children before deferred destruction; tabs, member selector, merchandise rows, preview glyph, primary action and Close consume the medieval component family. The selected preview no longer exposes catalog sprites with baked white card backgrounds. Evidence: `PARTY_SHOP_POPULATED_CAPTURE_OK 3/3 frames; real Forge stock selected; touch=clean`, `UI_GEOMETRY_OK 3`, and `UI_TOUCH_OK 3/3` in `Builds/ui-reskin-party-shop-populated-v5.log`.

- Realm Store production purchase v8 (2026-09-01): the stateful fixture opens the real Market `ShopPanel`, selects the canonical Minor Healing Draught, raises quantity to three, and invokes `ShopVM.Buy`. The complete production transaction is asserted at every target ratio: gold 100→76, persisted inventory 0→3, quantity reset to one, and readable purchase feedback retained after list rebuilding. This pass also corrected a stale noncanonical mana ID (`minor-mana-potion`) to `HudCommands.ManaPotionId` (`cons_mana_draught`), reads both names and prices from the consumable catalog, prevents same-frame deferred-destroy row duplicates, and authors every quantity/purchase action above the 112px touch floor. Human review rejected an earlier machine-green frame until the total price was visibly restored. Evidence: `REALM_STORE_PURCHASE_CAPTURE_OK 6/6 frames; quantity=3; gold+inventory asserted; touch=clean`, `UI_GEOMETRY_OK 6`, and `UI_TOUCH_OK 6/6` in `Builds/ui-reskin-realm-store-purchase-v8.log`.

- Bag production-use v7 (2026-09-01): the stateful fixture now enters Potions through the real rail command, selects an owned `minor-heal-potion`, invokes `InventoryVM.Use`, and asserts the complete production path (`BagConsumableUseEffect` → `ConsumableUseService` → `HeroHealth.Heal`) raised HP 45→75 while persisted inventory fell exactly x3→x2. Screenshot review rejected early machine-green frames that showed Gear with potion detail, then exposed an actual stocked-grid exception: `MakeScrollZone` already supplied a `VerticalLayoutGroup`, while landscape Bag illegally added `HorizontalLayoutGroup` to the same object. The landscape strip now uses deterministic explicit card placement. Same-frame VM repaints detach old children before deferred destruction, preventing duplicate raycast targets. The final card and pane visibly carry Minor Healing Draught identity, x3 stack, 30%-maximum heal, 8s reuse time, battle eligibility, and a touch-safe Use action. Evidence: `BAG_USE_CAPTURE_OK 6/6 frames; production effect; hp+inventory asserted; touch=clean` in `Builds/ui-reskin-bag-use-v7.log`.

- Registered secondary-route audit v9 (2026-09-01): added deterministic capture coverage for Benefactors, Cosmetic Shop, Building Upgrade, Workshop, Party Shop, Alchemy, Jeweler, Hero Loadout, Defense Report, Game Guide, and Monthly Ledger at all three supported landscape ratios. The loop exposed and corrected sparse Hero Loadout overlap, short Crafting/Party Shop action bands, Jeweler detail overflow, Game Guide prefab hit rectangles, and Monthly Ledger close/header/touch collisions. The first machine-green Monthly Ledger screenshot was rejected by human review for its brown legacy slab, tiny copy, and truncated tabs; it now uses the full black-iron/gold frame, readable two-line week tabs, mobile-scale reward/state copy, and correctly reserved Close/footer bands. Final route evidence: `REGISTERED_SECONDARY_CAPTURE_OK 33/33 frames; routes=11; touch=clean` in `Builds/ui-reskin-registered-secondary-v9.log`.

- Manage live-state and shared-factory continuation (2026-09-01): production `BuildTimerService.Enqueue` now seeds deterministic active/queued Builder, Training, and Research evidence; the Queue drawer owns an opaque medieval workspace and no longer exposes the browse list beneath it. Evidence: `MANAGE_LIVE_QUEUE_CAPTURE_OK 9/9` in `Builds/ui-reskin-manage-live-queue-v6.log` and `MANAGE_OPERATIONAL_CAPTURE_OK 12/12` in `Builds/ui-reskin-manage-live-timers-v1.log`. Shared framed panels and Obsidian buttons now inherit `MedievalUiSkin`, closing styling drift on registered secondary routes. Post-factory gates remained `UI_CAPTURE_OK 94`, `UI_GEOMETRY_OK 100`, `UI_TOUCH_OK 100/100`, `UI_ENDSTATE_FIT_OK 6` in `Builds/ui-reskin-factory-default-full-audit-v1.log`, with `REGRESSION_OK 339/339` in `Builds/ui-reskin-factory-default-regression-v3.log`.

- Complete secondary-surface audit v9 (2026-09-01): reran the broad current-tree harness instead of relying on old isolated screenshots. V1 exposed 64 geometry findings, 73 touch findings, and six compressed wave-clear cases. The correction loop separated Pause action bands, gave Build Collections a dedicated touch-safe paging row and filled medieval category cards, removed Night Market's Close/commerce overlap, constrained Talent Tree header actions/title to disjoint lanes, and rebuilt compact end-state sizing around its final settled posture. Direct screenshot review rejected machine-green Build cards until their data-bound names and filled black-iron surfaces were visible, and rejected the legacy silver wave-clear shell until the shared medieval skin and complete Repair All cost were present. Final broad evidence: `UI_CAPTURE_OK 94`, `UI_GEOMETRY_OK 100`, `UI_TOUCH_OK 100/100`, and `UI_ENDSTATE_FIT_OK 6` in `Builds/ui-reskin-complete-surface-audit-v7.log`; final category evidence `BUILD_COLLECTIONS_CAPTURE_OK 6/6` in `Builds/ui-reskin-build-collections-final-v11.log`; full suite `REGRESSION_OK 339/339` in `Builds/ui-reskin-complete-surface-regression-v9.log`.

- Coherence continuation v6 (2026-09-01): replaced the compact-button nine-slice collapse with deliberate simple scaling for the shared action art, restoring visible frames on short modal and merchant controls. Added deterministic 12-frame evidence for Ad Consent, Offline Opt-In, Daily Chest, and Harvest Overflow across 1920x1080, 2340x1080, and 2670x1200. Direct review rejected three overflow iterations until the complete collected/uncollected consequence and a visible Close action fit at both normal and ultrawide ratios. Rumor Board now visibly carries `+3 MORE` rather than truncating overflow rewards. Night Market retains its purple merchant subtheme while its two-column offers, bounded unavailable states, action rail, disclosures, and Close remain coherent at 800x360 through 2670x1200. Evidence: `SYSTEM_MODAL_CAPTURE_OK 12/12` in `Builds/ui-reskin-system-modal-capture-v5.log`, `RUMOR_BOARD_CAPTURE_OK 6/6` in `Builds/ui-reskin-rumor-final.log`, `NIGHT_MARKET_CAPTURE_OK 4/4` in `Builds/ui-reskin-night-market-final.log`, and final `REGRESSION_OK 339/339` in `Builds/ui-reskin-coherence-regression-v6.log`.

- Coherence continuation v4 (2026-09-01): extended the shared medieval skin across consent, offline opt-in, harvest overflow, Jeweler discovery, sealed-dungeon information, Daily Chest including its dynamically rebuilt ad action, SKR preview/stake surfaces, Arena, gear choice, NPC upgrade, outpost recruitment, gate intel, camp claim/build prompts, and the Echo combat tutorial. Direct screenshot review rejected a machine-green Night Market pass for a clipped third card, vertically fragmented unavailable prices, truncated spotlight copy, and an off-edge Close; the revised landscape composition now respects its two-card minimum-width contract, uses bounded catch-up pricing, expands spotlight copy, and keeps Close within the bottom band. Rumor overflow now shows two exact grants plus one readable summary instead of four ellipsized slivers. Focused evidence: `RAID_SELECTION_CAPTURE_OK 3/3`, `RUMOR_BOARD_CAPTURE_OK 6/6`, `NIGHT_MARKET_CAPTURE_OK 4/4`. Full gate: `REGRESSION_OK 339/339 suites -- 339 green, 0 red, 0 skipped` in `Builds/ui-reskin-coherence-regression-v4.log`.

- Welcome Back v3 (2026-09-01): replaced the hand-styled UI Toolkit outlier and its borrowed-`PanelSettings` failure mode with the real shared medieval modal. Screenshot iteration rejected blank resource rows and then rejected a Collect/data collision before accepting the final fixed composition. The worst-case fixture proves four gains, Echo wall-health benefit, actual Wood/Iron spend, stalled-material copy, storage-cap warning, and one Collect action without overlap. Evidence: `WELCOME_BACK_CAPTURE_OK 3/3` in `Builds/ui-reskin-welcome-back-v3.log`; full suite `REGRESSION_OK 339/339 suites -- 339 green, 0 red, 0 skipped` in `Builds/ui-reskin-welcome-back-v3-regression.log`.

- Journey/Echo continuation (2026-09-01): direct screenshot review rejected earlier machine-green Quest and Raid evidence. Quest now constrains its title/button hierarchy, keeps status inside the frame, and summarizes five-reward outliers rather than ellipsizing every edge chip. Raid now uses framed word-carried difficulty states, a reclaimed first fold, and the visible factory Close. Echo awakening removes the obsolete lore-toggle/backstory state in favor of one concise acknowledgement and one Close. Current focused markers: `ECHO_UNLOCK_CAPTURE_OK 3/3`, `RUMOR_BOARD_CAPTURE_OK 6/6`, and `RAID_SELECTION_CAPTURE_OK 3/3`; the final Raid first-fold and Quest chip-floor adjustments still require their last recapture before this item closes.

- Front door/profile v3/v2: fresh evidence rejected the prior Title and Login despite existing kit calls: Title still exposed stretched grey controls over baked legacy buttons, while Login was a flat brown slab with grey controls and a stray header mark. Title now uses a compact opaque medieval action tray with black-iron/gold framed actions; Login uses the real FrameCore skin, hides the unused medallion, bounds its explanatory copy, and separates the primary identity action from the always-live Guest escape. Hero Select was then audited as the profile surface; its silver shell, grey slabs, oversized CTA, crest ornament, and `powered by SKR` subtitle were removed in favor of the full medieval frame, `CHOOSE YOUR DEFENDER`, framed carousel cards/controls, and a framed confirmation action. A second screenshot pass moved PREV/NEXT and class labels fully inside their cards. Evidence: `FRONT_DOOR_CAPTURE_OK 6/6` in `Builds/ui-reskin-front-door-v3-capture.log`; `HERO_SELECT_CAPTURE_OK 3/3` in `Builds/ui-reskin-hero-select-v2-capture.log`; `REGRESSION_OK 339/339 suites -- 339 green, 0 red, 0 skipped` in `Builds/ui-reskin-front-door-profile-v2-regression.log`. Desktop evidence correctly shows the wallet branch; the Android `GOOGLE_PLAY` capture remains required to prove `WELCOME / Continue with Google` and absence of crypto copy in the shipping artifact.

- Shared overlays v4: the legally sensitive Jewel re-polish warning was captured before acceptance; v1 exposed title/body collision, a legacy silver shell, horizontal warning overflow, and a hidden Close action colliding with the decisions. It now uses the medieval shell's real header/body/footer zones, compact bounded odds typography, explicit irreversible-loss copy, and distinct KEEP IT / POLISH AGAIN actions without changing the authoritative odds source. Loading and first-run connection recovery now use one branded black-iron/gold focus card, bounded explanatory copy, visible progress, and a framed Retry action; the evidence fixture invokes the production builder without edit-mode-illegal `DontDestroyOnLoad`. Welcome Back's remaining UI Toolkit surface was retokened from a rounded generic card/green button to squared black-iron, aged-gold borders, ivory copy, and a 56px framed Collect action. Evidence: `JEWEL_POLISH_CONFIRM_CAPTURE_OK 3/3` in `Builds/ui-reskin-jewel-confirm-v2-capture.log`; `LOADING_OVERLAY_CAPTURE_OK 6/6` in `Builds/ui-reskin-loading-overlay-v4-capture.log`; `REGRESSION_OK 339/339 suites -- 339 green, 0 red, 0 skipped` in `Builds/ui-reskin-overlays-v4-regression.log`.

- Build confirmation/orientation v4: replaced the fixed 360x440 legacy panel, legacy `Text`, and flat rectangles with the responsive medieval shell, authoritative live 3D preview, explicit yaw state, three framed rotation controls, and framed Confirm/Cancel actions in the shell footer. Screenshot comparison rejected the first two layouts (footer actions outside the frame and an under-framed action family), then corrected both and excluded the neutral floor from camera-fit bounds so the structure is readable. Evidence: `BUILD_PREVIEW_CAPTURE_OK 3/3` in `Builds/ui-reskin-build-preview-v4-capture.log`; `REGRESSION_OK 339/339 suites -- 339 green, 0 red, 0 skipped` in `Builds/ui-reskin-build-preview-v4-regression.log`.

- Hero Skills/Talents v9 states: deterministic popup and assigned-active evidence now covers all three supported ratios. Repeated VM repaint no longer leaves deferred-destroy hotbar duplicates visible in-frame; exactly four cards remain. The state fixture is reversible, selects a class-valid authoritative ability, restores its PlayerPrefs key, dismisses the popup before assigned-state proof, and shows Fireball in Slot 1 without claiming a locked talent is purchasable. The compact prompt now retains an explicit gold talent identity at short landscape heights. Evidence: `HERO_SKILL_TREE_CAPTURE_OK 9/9` in `Builds/ui-reskin-hero-skill-tree-v9-states-capture.log`; `REGRESSION_OK 339/339 suites -- 339 green, 0 red, 0 skipped` in `Builds/ui-reskin-hero-skill-tree-v9-regression.log`.

- Hero Skills/Talents v3: expanded the talent workspace out of the obsolete shallow frame drop-zone, converted Back/Equipment and the four hot-swap slots to bounded medieval controls, replaced truncated one-line node captions with readable backed two-line names, and unified Wisdom/skill-point header cards. Evidence: `HERO_SKILL_TREE_CAPTURE_OK 3/3` in `Builds/ui-reskin-hero-skill-tree-v3-capture.log`; structural change passed `REGRESSION_OK 339/339 suites` in `Builds/ui-reskin-hero-skill-tree-v2-regression.log` before the final header-card-only polish.

- Realm gold Store v7: replaced the legacy portrait slab with the approved landscape categories / merchandise / detail hierarchy; merchandise is now a two-column touch-card grid with icons, names and prices; the selected item opens by default; the right pane owns authoritative details, unit/total gold price, bounded quantity controls and BUY; only gold is shown. Zero-gold captures disable unaffordable execution. Screenshot iteration corrected clipped legacy rows, unrelated resource wallets, invisible selected text, missing card labels, detail overlap, and a non-shipping minus glyph. Evidence: `REALM_GOLD_STORE_CAPTURE_OK 3/3` in `Builds/ui-reskin-realm-gold-store-v7-capture.log`; full gate `REGRESSION_OK 339/339 suites` in `Builds/ui-reskin-realm-gold-store-v7-regression.log` (the final disabled-Buy presentation change subsequently recompiled/captured green).

- Bag/Inventory v4: re-seated the player strip below the title, converted Talents/categories/worn slots to the medieval control and card families, removed the legacy stretched selection wedge and grey slot artifacts, and stopped guidance copy from colliding with the wallet/Close bands. The first full gate correctly rejected undersized tabs/header action; v4 restores the 112 px mobile touch floor. Evidence: `BAG_CAPTURE_OK 3/3` in `Builds/ui-reskin-bag-v4-capture.log`; `REGRESSION_OK 339/339 suites` in `Builds/ui-reskin-bag-v4-regression.log`.

- Pause capture: `PAUSE_CAPTURE_OK 3/3`.
- Full regression after Pause: `REGRESSION_OK 337/337 suites — 337 green, 0 red, 0 skipped`.
- Adaptive HUD contract compile: green on 2026-08-31.
- Combat Item flow compile: marker-asserted `COMPILE_GATE_OK`.
- Full regression after Item modal-arbiter correction: `REGRESSION_OK 338/338 suites`.
- Item picker capture: `COMBAT_ITEM_PICKER_CAPTURE_OK 3/3`; final ultrawide review has no clipping, overlap, truncation, or stray legacy header ornament.
- Settings compile: marker-asserted `COMPILE_GATE_OK`.
- Settings capture: `SETTINGS_CAPTURE_OK 3/3`; visual comparison removed the legacy silver shell, floating title plate, stretched Close, and accidental scrim artwork.
- Full regression after Settings: `REGRESSION_OK 339/339 suites`.
- Manage hub capture: `MANAGE_HUB_CAPTURE_OK 3/3`; four-card hierarchy, shared shell, lock treatment, geometry, and touch gates passed.
- Manage Defense capture: `MANAGE_DEFENSE_CAPTURE_OK 3/3`; approved mobile CTA, shared operational button family, collapsed queue drawer, geometry, and touch gates passed.
- Full regression after Manage compatibility reconciliation: marker-asserted `REGRESSION_OK 339/339 suites` in `Builds/ui-reskin-manage-regression-final`. The obsolete build-collection oracle now verifies the live Defense route instead of demanding legacy long-form button copy.
- Manage operational capture v4: `MANAGE_OPERATIONAL_CAPTURE_OK 12/12`; Defense, Buildings, Troops, and Research each rebuilt at 1920x1080, 2340x1080, and 2670x1200 with fidelity, geometry, and touch gates green. A stale-row defect in the synchronous capture was corrected so selected-tab evidence is now truthful.
- Manage visual review remains open: v4 adds visible production-line status cards, Queue access, category-specific headings, Train-first Troops ordering, live Research cost/duration, and real Defense/Town build routes. Approved Troops medallion selection and Train/Upgrade detail composition are not yet at parity.
- Full regression after Manage operational v4: marker-asserted `REGRESSION_OK 339/339 suites` in `Builds/ui-reskin-manage-operational-regression-rerun`.
- Manage Troops v7: `MANAGE_OPERATIONAL_CAPTURE_OK 12/12`; selector choices derive from `TroopCatalog`, locked choices remain visible with Barracks-tier requirements, five touch-safe medallions precede explicit paging, and Train/Upgrade actions retain authoritative VM commands. The first pager attempt failed 24 geometry/touch findings and the second failed six sub-floor mode tabs; v7 resolves all measured findings.
- Troops v7 visual comparison remains open: its horizontal selector is materially closer, but it occupies the first fold and pushes selected-unit detail below. The next iteration must combine the selector rail and selected detail into one split workspace like `manage-troops-approved.png`.
- Full regression after Troops v7: marker-asserted `REGRESSION_OK 339/339 suites` in `Builds/ui-reskin-manage-troops-v7-regression`.
- Manage Troops v10: `MANAGE_OPERATIONAL_CAPTURE_OK 12/12`; the approved split-workspace hierarchy now keeps the troop selector rail, selected identity, requirement state, Train/Upgrade modes, and authoritative action in one composition. The v8 pass exposed 21 authored overlap/touch failures and v9 reduced these to three; v10 resolves every measured geometry and touch finding.
- Troops v10 visual comparison remains open: the composition is structurally correct, but the action details need more first-fold room and the selector rail/header need a final density pass against `manage-troops-approved.png`. Repeated `0/5 queued` copy was removed from the shared status strip; queue depth remains in the deliberate Queue drawer.
- Full regression after Troops v10: marker-asserted `REGRESSION_OK 339/339 suites` in `Builds/ui-reskin-manage-troops-v10-regression`.
- Manage v14: `MANAGE_OPERATIONAL_CAPTURE_OK 12/12`; Queue moved into the title row opposite Back, the redundant destination band was removed, the selected action entered the first fold, repeated `0/5 queued` text was removed, and the troop rail became portrait-first. Full regression: marker-asserted `REGRESSION_OK 339/339 suites` in `Builds/ui-reskin-manage-v14-regression-rerun`.
- Adaptive peaceful dock foundation: one housed four-medallion `peacefulDock` now owns Build, Hero, Journey, and Manage through their existing authoritative command routes. Both canonical `hud-areas.json` copies select the same component in calm town/explore; legacy dynamic faces remain unoccupied. Compile marker `COMPILE_GATE_OK`; full regression marker `REGRESSION_OK 339/339 suites` in `Builds/ui-reskin-peaceful-dock-regression-rerun`.
- Matched adaptive HUD capture harness added: `ADAPTIVE_HUD_CAPTURE_OK 6/6` captures peaceful and active-combat postures at 1920x1080, 2340x1080, and 2670x1200. V1 visual evidence proves the peaceful dock structure and also records the remaining failures: legacy top plaques and a split/empty combat action composition. HUD parity remains open.
- Adaptive combat dock v2: one housed six-medallion dock now owns Attack, held Block, Skill I, Skill II, Skill III, and Item. Block is wired to authoritative `HeroHealth.SetBlocking`, releases on pointer-up/exit/cancel/disable, reduces post-mitigation damage while held, and prevents simultaneous attack. Item retains the atomic paused picker. `ADAPTIVE_HUD_CAPTURE_OK 6/6` proved the unified dock at all three ratios.
- Adaptive HUD shared chrome v5: Hero/Heart nameplates, compass, and target frame now use the common medieval black-iron/gold artwork. Screenshot iteration caught a real compact-nine-slice collapse (the source has 96 px borders); compact HUD bands now deliberately use simple scaling while full panels retain nine-slice behavior. Fresh evidence: `COMPILE_GATE_OK`, `ADAPTIVE_HUD_CAPTURE_OK 6/6`, and `REGRESSION_OK 339/339 suites` in `Builds/ui-reskin-adaptive-hud-v5-regression-rerun`. HUD parity remains open for resource/event presentation, rail controls, raw bar fills, and removal of the stray Realm shortcut.
- Adaptive HUD event/rail v10: the central event module now retains one anchor across peaceful and active combat, renders authoritative countdown or enemies-remaining content, exposes Start Now only between waves, and uses the medieval frame/button family. The matched capture now registers a real `HudModel` fixture and seeds representative states, preventing empty-model false positives. Screenshot iteration corrected oversized first-pass type, an out-of-frame progress track, and compact rail truncation; Realm and Collectors now use readable gold controls. Fresh `COMPILE_GATE_OK` and `ADAPTIVE_HUD_CAPTURE_OK 6/6` evidence is in the v10 logs. Remaining HUD parity work: resource plaque composition, Heart objective strip, raw vitality/progress fills, menu/joystick treatment, and final three-ratio SME comparison.
- Adaptive HUD v16: the final shared pass replaces raw vitality/progress fills with semantic red/blue/gold rows inside medieval tracks, converts Heart into a concise objective strip, gives the joystick and Settings handle authored ornamental frames, consolidates the peaceful economy/action rail, and removes the retired HUD skill-points chip from the authoritative geometry table. Peaceful and combat states retain stable anchors and their four/six action contracts at 1920x1080, 2340x1080, and 2670x1200. Fresh evidence: `ADAPTIVE_HUD_CAPTURE_OK 6/6` in `Builds/ui-reskin-adaptive-hud-capture-v16.log` and `REGRESSION_OK 339/339 suites` in `Builds/ui-reskin-adaptive-hud-v16-regression.log`.
- Hero family v8: Equipment no longer uses the legacy scrollable paper-doll/3D-preview composition. It now binds one VM-owned selected item into the approved player-strip + Equipped + Inventory + Item Details hierarchy, exposes Back/Talents header actions, uses a single contextual `EQUIP`/`REMOVE` command, and projects candidate comparison values/deltas from authoritative gear definitions. Talent Tree outer/popup/quick-slot controls now consume the medieval shell/button family. Fresh compile: `COMPILE_GATE_OK` in `Builds/ui-reskin-hero-family-v8-compile.log`; full suite after the Equipment architecture change: `REGRESSION_OK 339/339 suites` in `Builds/ui-reskin-hero-equipment-v7-regression.log`. `HERO_FAMILY_CAPTURE_OK 6/6` proves all target ratios, but only the equipped Equipment state is presently truthful; the alternate-item fixture failed to enter the owned store and therefore candidate comparison screenshot evidence remains open.
- Hero Equipment v11: the capture fixture now initializes the production `VillageInventory` singleton and proves two actually-owned ranger weapons. The approved workspace uses the real hero portrait pipeline, explicit health/focus values, gold-only economy state, and a readable comparison/action stack. The harness captures candidate/EQUIP and equipped/REMOVE independently at 1920x1080, 2340x1080, and 2670x1200: `HERO_FAMILY_CAPTURE_OK 9/9` in `Builds/ui-reskin-hero-family-v11.log`. Fresh gates: `COMPILE_GATE_OK` in `Builds/ui-reskin-hero-equipment-v11-compile.log`; `REGRESSION_OK 339/339 suites -- 339 green, 0 red, 0 skipped` in `Builds/ui-reskin-hero-equipment-v11-regression.log`. Candidate-state evidence is no longer open; remaining Hero-family work is the Talent/Skills internal reskin and final narrow-card typography polish.
- Hero Skills v14: Talent nodes now carry readable authoritative nameplates, locked art remains recognizable, Back/Equipment navigation uses the shared medieval controls, and the screen explicitly presents both Wisdom and Skill Points without leaking skill points back onto the HUD. The screenshot loop caught and fixed a missing-frame white fallback. Fresh evidence: `COMPILE_GATE_OK` in `Builds/ui-reskin-hero-skills-v14-compile.log`; `HERO_FAMILY_CAPTURE_OK 9/9` in `Builds/ui-reskin-hero-family-v14.log`.
- Build Collections v5: the earlier blank capture was a false proof caused by the edit-mode harness skipping the runtime catalog bootstrap. The focused harness now initializes the same 28-row production registry and captures both category pages. The live UI no longer pushes seven choices through a four-row overflow: it shows four large cards per page, explicit medieval Back/Next controls, and a clean second page containing Storage, Protection, and Trade. Shared navigation refresh now destroys synchronously only outside play mode, preventing stale-page screenshot overlays while retaining normal runtime destruction. Fresh evidence: `COMPILE_GATE_OK` in `Builds/ui-reskin-build-collections-v5-compile.log`; `BUILD_COLLECTIONS_CAPTURE_OK 6/6` in `Builds/ui-reskin-build-collections-v5.log`.
- Night Market v7: the focused capture proved the earlier 1920 image was stale (the store's authoritative target set is 800x360, 915x412, 1280x720, and 2670x1200). The complete Night Market PNG handoff now has a permanent Sprite import contract; art placement uses a positioned host plus fitted visual child so covenant, illustrated wordmark, wallet, and Mainnet plaques retain their authored slots instead of collapsing at center. Season Track is absent from the live Actions rail, while Redeem a Code and Monthly Ledger remain. Utility and Close controls consume the shared medieval button family. Current evidence: `NIGHT_MARKET_CAPTURE_OK 4/4` in `Builds/ui-reskin-night-market-v6.log`; `COMPILE_GATE_OK` in `Builds/ui-reskin-night-market-v7-compile.log`; `REGRESSION_OK 339/339 suites -- 339 green, 0 red, 0 skipped` in `Builds/ui-reskin-night-market-v7-regression.log`. Open parity work: stronger right-rail frames, complete five-offer-area/price-state proof, and final comparison to `night-market-approved.png`.

- Shared player decks v2: Realm, Hero, and Journey launchers now use the same black-iron/gold card frame, circular four-point identity medallion, gold heading, ivory purpose line, and shared Close control. The first screenshot pass caught purpose copy escaping the short mobile card frame; v2 constrains it to a fitted single line with ellipsis fallback. Evidence: `COMPILE_GATE_OK` in `Builds/ui-reskin-player-decks-v1-compile.log`; `PLAYER_DECK_CAPTURE_OK 9/9` in `Builds/ui-reskin-player-decks-v2-capture.log`. Realm exposes only Store, Defense Report, Monthly Ledger, and Game Guide; retired Map and Season Pass entries are absent. Destination interiors remain separately open.
- Raid selection v3: the reachable camp selector now consumes the same medieval shell and textured black-iron/gold card family as Journey instead of the legacy silver/flat-grey treatment. Screenshot iteration reduced oversized labels, moved reward copy back inside the card, retained non-colour difficulty words, and restored an explicit touch-safe Close action that the old shell failed to render. Three-ratio capture marker: `RAID_SELECTION_CAPTURE_OK 3/3` in `Builds/ui-reskin-raid-selection-v3-capture.log`; full suite: `REGRESSION_OK 339/339 suites -- 339 green, 0 red, 0 skipped` in `Builds/ui-reskin-raid-selection-v3-regression.log`.
- Quest/Rumor Board v3: the three-poster Journey surface now uses the medieval full-screen shell, textured content cards, ornate gold Close, and active gold-framed Previous/Next/Read/Accept controls. The screenshot loop caught compact nine-slice collapse, a missing Close, title/card-border collision, and a four-pixel poster-stack regression; all were corrected without changing authoritative quest/paging/accept behavior. Evidence: `RUMOR_BOARD_CAPTURE_OK 6/6` in `Builds/ui-reskin-rumor-board-v3-capture.log` (two pages at all three target ratios).
- Echo family v3: the roster and assignment card now share the medieval shell, ornate Close, black-iron/gold content cards, circular portrait medallions, explicit readable owned/locked states, and selected/unavailable resource controls. A new deterministic three-owned/three-locked roster fixture closes the former evidence gap. Screenshot iteration corrected missing Close controls, disconnected status labels, insufficient card height, and square portraits. Evidence: `ECHO_FAMILY_CAPTURE_OK 6/6` in `Builds/ui-reskin-echo-family-v3-capture.log`.

- Hero Skills canonical-medallion pass (2026-09-01): replaced the remaining square `slot_talent_*` presentation with the existing canonical `circular-bezel-four-point` asset, circularly masked all source art, and retained lock/selection meaning through explicit glyphs, words, dimming, and a quiet non-ornamental state glow. The first render was rejected because old state tinting made the authored gold bezel nearly disappear; the second render preserves the source black-iron/gold pixels. Evidence: marker-asserted `HERO_SKILL_TREE_CAPTURE_OK 9/9` in `Builds/ui-reskin-skills-medallion-v2.log`; all three base, popup, and assigned states were regenerated at 1920x1080, 2340x1080, and 2670x1200.
- Current public-surface correction wave (2026-09-01, open): screenshot review rejected Equipment's generic button rows and debug-bright bars, Manage Defense's blank no-service status chips, Realm Game Guide's exposed checkerboard packaging margin, and Bag's frame medallion expanding across the complete workspace. Equipment now uses native black-iron rows/cards and canonical medallions (`HERO_FAMILY_CAPTURE_OK 9/9`, `Builds/ui-reskin-hero-family-cards-v3.log`); Manage has explicit no-service status labels (`MANAGE_DEFENSE_CAPTURE_OK 3/3`, `Builds/ui-reskin-manage-defense-labels-v3.log`); Realm uses all four supplied illustrated destination cards with their packaging margins masked (`PLAYER_DECK_CAPTURE_OK 9/9`, `Builds/ui-reskin-player-deck-crop-v3.log`); Bag re-seats the hero identity as a compact header medallion and replaces saturated legacy status gradients with muted native fills (`BAG_CAPTURE_OK 3/3`, `Builds/ui-reskin-bag-polish-v7.log`). The broad rerun is complete with no blank renders: `UI_CAPTURE_OK 97`, `UI_GEOMETRY_OK 97`, and `UI_TOUCH_OK 97/97` in `Builds/ui-reskin-broad-capture-v3-2026-09-01.log`. Its first run exposed six falsely blank dialogue-option captures caused by a stale prior modal registration; the harness now closes the modal arbiter before each dialogue probe and all six frames render. The associated dialogue fit pin is green (`ECHO_ENGAGE_DIALOGUE_OK`, `Builds/ui-reskin-dialogue-regression-2026-09-01.log`). The authoritative full regression remains RED at 337/339 only because the concurrent terrain-quality wave reports Goldfields chroma and quality-level anisotropy failures; the UI/dialogue failure from that run has been corrected and focused-proven. Final broad regression and SME screenshot closure are therefore still required before approval.

- HUD/dialogue lane closure (2026-09-01): regular decision dialogue now uses a full mobile reading canvas, reserves a measured 96px minimum prose well, and presents every response as a touch-safe black-iron/gold action instead of an unframed black row. Direct 1920x1080 review rejected the former clipped opening prose and partial four-choice stack; the corrected frame shows the complete passage and all four choices. Compact townsfolk/helper dialogue retains its HUD-safe strip, keeps the 36px speaker name inside the upper iron rail, and shows the one-line response without clipping. The whole visible non-choice modal remains the advance target, while choice buttons retain independent actions and combat interruption continues through the panel arbiter. Focused evidence: `DIALOGUE_OPTIONS_CAPTURE_OK 6/6` in `Builds/ui-reskin-dialogue-options-v5.log`; `COMPACT_DIALOGUE_CAPTURE_OK 3/3 + INTERACTION_OK` in `Builds/ui-reskin-compact-dialogue-v4.log`; `ECHO_ENGAGE_DIALOGUE_OK` in `Builds/ui-reskin-dialogue-regression-v2.log`. Final UI-only broad evidence after these changes is `UI_CAPTURE_OK 97`, `UI_GEOMETRY_OK 97`, and `UI_TOUCH_OK 97/97`, with no blank render, in `Builds/ui-reskin-broad-capture-v4-2026-09-01.log`. Per owner scope, terrain/town/building visuals are excluded from this lane and remain isolated to the separate redesign worktree. Artifact hold remains active pending cross-lane integration and final release gates.

- Public-inventory continuation (2026-09-01, closed for the current reachable set): Realm Map and the standalone QueueCardRail remain excluded because both are retired product surfaces; their dedicated helpers remain available for forensic checks. Monthly Ledger now has focused proof at all three target states with `MONTHLY_LEDGER_CAPTURE_OK 3/3`, `UI_GEOMETRY_OK 3`, and `UI_TOUCH_OK 3/3` in `Builds/ui-reskin-monthly-ledger-v13.log`. Night Market now uses a neutral black-iron substrate, bounded merchant-purple accents, shared framed controls, explicit Close text, real resource/action icons, a non-duplicated disclosure band, two readable core cards on compact landscape, three on the 2670-wide surface, and all three catch-up offers in the first rail view. Its authoritative focused evidence is `UI_CAPTURE_FIDELITY_OK 4`, `UI_GEOMETRY_OK 4`, `UI_TOUCH_OK 4/4`, and `NIGHT_MARKET_CAPTURE_OK 4/4; fidelity=clean; geometry=clean; touch=clean` in `Builds/ui-reskin-night-market-black-iron-v15.log`. The fresh current-tree broad pass is `UI_CAPTURE_OK 91`, `UI_GEOMETRY_OK 91`, and `UI_TOUCH_OK 91/91` in `Builds/ui-reskin-public-broad-current.log`. Adding the Monthly Ledger CTA exposed a one-line canonical-string mirror drift; the StreamingAssets mirror is now byte-identical to the Resources authority and the combined suite is `REGRESSION_OK 339/339 suites -- 339 green, 0 red, 0 skipped` in `Builds/ui-reskin-integrated-regression-byte-sync.log`.

## Artifact hold

- Current integrated closure (2026-09-01): current-tree compile is `COMPILE_GATE_OK` in `Builds/ui-reskin-integrated-compile-current.log`; data/gameplay regression is `REGRESSION_OK 339/339 suites -- 339 green, 0 red, 0 skipped` in `Builds/ui-reskin-integrated-regression-byte-sync.log`; mapped-capture coverage is green in that suite (`rows covered: 30/32`, with two explicitly documented exemptions); the 30-state 2670-wide human-review contact sheet is `Builds/ui-reskin-public-2670-contact-sheet.png`. Google Play source isolation is green (`PLAY_PACKAGING_REGRESSION_OK`, `Builds/ui-reskin-play-source-packaging-current-v2.log`). The remaining platform proof is deliberately artifact-bound: build the held `GOOGLE_PLAY` player, capture its Login/front door showing `Continue with Google` and no wallet/crypto copy, then scan the physical AAB. This is not claimed before the cross-lane build hold is released.
- Google Play Login visual closure (2026-09-01): added an editor-only presentation override to the production `LoginPanelController`; the player branch remains compile-selected exclusively by `GOOGLE_PLAY`, while the harness can now render the exact production layout before the held build. V1 exposed a real sub-floor `Play as Guest` band at 2340 and 2670; its authored band was enlarged rather than relying on runtime clamping. Direct review of all three final frames shows only `WELCOME`, `Continue with Google`, and `Play as Guest`, with no wallet, Solana, SKR, or crypto copy. Evidence: `GOOGLE_PLAY_LOGIN_CAPTURE_OK 3/3; fidelity=clean; geometry=clean; touch=clean` in `Builds/ui-reskin-google-play-login-v2.log`; unchanged normal branch `FRONT_DOOR_CAPTURE_OK 6/6` in `Builds/ui-reskin-front-door-post-play-seam.log`; combined tree `REGRESSION_OK 339/339` in `Builds/ui-reskin-regression-after-play-login-v2.log`. This proves UI composition and copy, not the physical AAB contents; artifact scanning remains mandatory after the hold releases.

Windows, APK, AAB, WebUI, Firebase, Vercel, and store artifacts are intentionally held until every ready implementation is complete and the compile, regression, geometry, touch, screenshot-parity, packaging, and platform-policy gates are green.
