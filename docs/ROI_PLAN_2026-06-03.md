# Backlog ROI Plan — 2026-06-03 (post heroes/tutorial/HUD build)

_Supersedes the morning `WO_ROI_TRIAGE.md` (several of its top items shipped today). Lens: **the build is what the grant judges** — fix what a player hits in the first 2 minutes before any polish or economy. Owner doctrine: "stop fixing internal plumbing while the front door is falling off its hinges."_

ROI = (grant-demo impact × first-2-min visibility × reach) ÷ (effort × risk).

---

## What THIS session already cleared (reconcile board → Done/verify)
- **DEF-211 / DEF-204** front-door rebuild — shipped (build live), pending owner playtest-confirm.
- **DEF-221** People heroes + Orc family (Phase 1) — in this build.
- **DEF-222** Tutorial + companion onboarding — in this build.
- **DEF-133** wave-XP halved — shipped + baked (`91c4bac`); **close it**.
- **DEF-114** gate-to-ground — committed + baked this session; **verify + close**.
- **DEF-195 / DEF-214** moat-water + dark-village — improved in this build (moat material + lighting lift); re-judge after playtest, may downgrade.
- **DEF-219** wrong hero model (Elara→mage) — **likely fixed** by the Cleric body + abilitySlug decouple; **verify in this build**.
- **DEF-91** replace KayKit NPCs w/ People pack — partially done (heroes/orcs imported); NPC bodies remain.

## Board NOISE — close/cancel (don't spend effort)
- **DEF-137 / DEF-138** ClaimableNode — not in this branch (review-clone). Cancel as obsolete.
- **DEF-154** split VillageSceneBuilder — owner+CLI already agreed it's cosmetic (0 perf, asmdefs enforce boundaries, reflection-string risk). Deprioritize/cancel; don't re-propose.
- **DEF-179** tutorial redesign — superseded by DEF-222 (shipped). Merge/close.
- **DEF-1..4** Linear template — ignore.

---

## TIER 0 — P0: the in-game "front door" (do NEXT, after this build validates)
These are the *exact* analog of the broken title screen, one screen deeper. A reviewer hits all of these in the first 2 minutes. Highest ROI on the board.

| DEF | Title | Lane | Why P0 | Effort |
|---|---|---|---|---|
| **DEF-224** | Enemies ignore hero, walk through walls, no aggro/collision | Combat/AI | **The core loop doesn't work** — combat is the game. Nothing else matters if enemies don't fight. | Code, med |
| **DEF-212** | In-game UI panels broken — stacking, empty, catalog unavailable | UI/HUD | The whole in-game UI is broken on mobile = unplayable demo. | Code, med |
| **DEF-217** | Triple interaction prompt on buildings (3 UI elements) | UI/HUD | Confusing first-touch; ties to the MobileInteractButton work (DEF-203). | Code, low |
| **DEF-218** | Workshop can't close on mobile + "No recipes loaded" | UI/HUD | Dead-end trap + empty content = broken-feeling. | Code, low-med |
| **DEF-219** | Wrong hero model (Elara→mage) | Combat/AI | First-30s hero-select payoff. **Probably fixed today — verify first.** | Verify |

**These run as 3 parallel lanes** (Combat/AI: 224 → UI/HUD: 212+217+218 → verify 219), no scene-file collision.

## TIER 1 — Core-loop completeness + cheap juice (high ROI, mostly code)
| DEF | Title | Lane | Note |
|---|---|---|---|
| DEF-206 | Enemy HP bar + target-lock UX (hide until engaged) | Combat/AI | Pairs with the FloatingHealthBar fix shipped today. |
| DEF-225 | Floating "+X XP" text on kill | UI/HUD | Cheap dopamine; pairs with the new XP bar. |
| DEF-223 | Daily quest system (Day-1: build 4 towers) | UI/HUD | The tutorial already sets this objective — close the loop. |
| DEF-132 | Hero faces wrong direction (body-yaw) | Combat/AI | Re-check with the new People bodies first (may have changed). |

## TIER 2 — World/visual polish (bake-gated, owner-in-loop)
DEF-215 gate↔wall gap · DEF-216 rampart stairs (StairNavLink relates) · DEF-208 oversized tower model · DEF-220 forge model · DEF-191 castle upper level · DEF-109 residual (tower/veins). Batch into one bake pass.

## TIER 3 — Narrative / progression (feature work, medium ROI)
DEF-180 Sylas join · DEF-209 defensive storyflow · DEF-213 building-specific interactions (Arcane Tower→talents) · DEF-119 cutscene · DEF-207 pet acquisition · DEF-181 hero cards.

## TIER 4 — Economy / monetization design (DEFER — "not before the grant")
DEF-12 Wisdom earn · DEF-29 Glimmer earn · DEF-121 resource economy · DEF-189 node discovery. Per scope-discipline: simple flat-% upgrades, not deep shops. Hold.

---

## Recommended execution (when this build lands + owner confirms)
1. **Verify-pass** on this build: DEF-219 (hero model), DEF-211/204 (front door), DEF-133/114 (close), HUD bars, XP bar, moat/lighting.
2. **Fan out the TIER-0 cluster** as 3 parallel worktree agents (Combat 224, UI 212/217/218, verify 219) — same model that worked today.
3. **TIER-1 juice** next (206/225/223) — fast wins that make the loop feel alive.
4. **One TIER-2 bake pass** batched (215/216/208/220/191).
5. Hold TIER-3/4 until the loop is demo-solid.

Doctrine: never ship polish over a P0 the player hits first. Parallel lanes by §9 (World/Env, UI/HUD, Combat/AI, VFX/Audio, Monetization) so agents never collide.
