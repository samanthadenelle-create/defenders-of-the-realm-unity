# GEAR GENERATOR — COVERAGE MANIFEST

> AUTO-GENERATED by `DeNelle.Editor.Catalog.GearCatalogGenerator.Generate` (WO-Item-2).
> Do NOT hand-edit — re-run *Defenders > Catalog > Generate Gear Catalog*.

**Generated:** 2026-08-14 14:05

## Catalogued this run (committed + Resources-loadable)

- Models scanned: **441** (skipped 1 backups/uncategorized)
- weapons.json rows now: **96**
- armor.json rows now: **24**

Source folders scanned:
- `Blink (Addressables) — Weapons + Armor sets` (Resources-loadable)
- `Resources/Heroes/Props/Weapons` (Resources-loadable)

Only gear that is **committed AND Resources-loadable** is catalogued. Backup
files (prefixed `_`) and models whose name carries no known category keyword
are skipped (no phantom entries).

## NOT catalogued — gitignored bundle (BLOCKED on the Addressables enabler)

The bulk gear bundle is **gitignored AND outside `Assets/Resources/`**, so it is
**NOT runtime-loadable by `Resources.Load`** → the generator deliberately emits
**no entries** for it (a row whose `prefabPath` can't resolve would blank the
store/equip view — the BUG#22 class). Approximate inventory on disk:

| Pack | Location | Approx. meshes | Status |
|---|---|---|---|
| Blink — Weapons (MegaWeaponPack) | `Assets/Blink/Art/Weapons` | ~805 | gitignored, outside Resources |
| Blink — Characters/outfits | `Assets/Blink/Art/Characters` | ~551 | gitignored, outside Resources |
| KayKit (adventurers + dungeon) | `Assets/Models/KayKit*` | ~9000 fbx | gitignored, outside Resources |

### Recommendation — Addressables, NOT a Resources mirror

To catalogue the bundle, add an **Addressables-backed gear source** to this
generator (`IGearSource` over the Addressables catalog — the code is already
structured for it; `Sources[]` takes another entry with **no rewrite**). Do NOT
mirror the packs into `Assets/Resources/` — Resources is force-included in every
build and would bloat the WebGL download with thousands of unused meshes. Mark
the gear group Addressable, then the generator emits `prefabPath` as the
Addressables address and the runtime loads on demand. **This is the enabler that
unblocks the ~800/~9000-asset catalog.**
