# PM AUDIT — 2026-07-12

> Senior-PM audit of "Echoes of Elarion / Defenders of the Realm" against the NORTH STAR:
> a playable demo in **Pi Browser (mobile web)** for the **Pi Hackathon, July 31, 2026**, where
> **build mode IS the demo** (player-defined city), one Knight, isolated real-time BattleArena,
> zero crypto in V1, judged by the ten-year-old feel bar.
>
> Sourced from working-tree evidence read this session (no guessing): `KEY_FACTS.md`,
> `CANON_GROUND_TRUTH_2026-07-12.md`, `WorkOrders/WORK_ORDER_684_outstanding_items_board.md`,
> `docs/SECURITY_AUDIT_2026-07-12.md`, `Logs/debug/FLEET_TRIAGE_2026-07-12.md`,
> `Builds/data-regression.log`, `docs/HANDOVER.md`, `git log`.
> Severity is stated as TEXT (owner is red/green colorblind) — never color alone.

---

## 1. VERDICT

**Yes — a credible July-31 Pi demo is reachable, but ONLY if the next ~2 weeks spend on flow
and one real device run, not polish.** The engine is healthier than its backlog looks: the
always-run core lane passes end-to-end every fleet run (boot -> hero -> build-move -> save
round-trip -> vendor -> HUD -> combat invariants; `FLEET_TRIAGE_2026-07-12.md` line 8), the
instrumentation/regression/web-observability machine is genuinely strong, and there are zero
CRITICAL security findings. **The single biggest threat is that the demo has never been run
end-to-end in the actual Pi Browser on a phone** (`WO-684` §B.9: "No evidence this has ever been
done"). Every quality claim above the core loop is Windows-headless-proven only, and the north
star explicitly says the owner's hands on a phone are the only verdict. Underneath that, one
demo-lethal flow bug is proven by data: **a judge who leaves the castle cannot get back**
(`HOME_RETURN_FAIL :: gate=<none>`, x4 — WO-602). The runway is enough; the discipline must flip
from feature-adding to flow-closing plus a single traced device session that generates the real
punch list.

---

## 2. STATE OF THE DEMO — the critical path

Critical path: boot -> hero-select -> build city -> battle -> save/return.

**WORKS end-to-end (fleet PASS, reproduced every run):**
- Boot -> gameplay, save round-trip, vendor, build-move, HUD panels, and combat invariants all
  PASS in the always-run core lane, 4/4 seeds (`FLEET_TRIAGE_2026-07-12.md` line 8).
- Build-mode touch placement is healthy in multiple device sessions per screenshot + db:
  `Armed -> tap -> PlaceConfirm -> Place() -> under-construction` (`CANON_GROUND_TRUTH` line 50).
  The uGUI verb bar + PLACE button + kit d-pad landed tonight (commits `c963a553`, `f123859d`).
- ATB combat engine terminates deterministically (Victory, 15/15 enemy-id map valid) and the
  Dialogue path passes full parse/reachability/runner checks (`data-regression.log`: `[atb-engine]`,
  `[dialogue]`).

**BREAKS a judge's session (proven by captured data):**
- **WO-602 home-return unwired [SEVERITY: CRITICAL for demo]** — `HOME_RETURN_FAIL :: gate=<none>`,
  no SceneTransitionTrigger targets the hub, x4. Leave the castle and there is no way back. A
  session-ender for a judge who explores.
- **Encounter-return strand [SEVERITY: HIGH]** — `AssertEncounterRealPath: hero NOT returned
  (dist 7088.1m)`, x4 (WO-453). Battle resolves but the return-teleport lands the hero ~7km off.
- **CavePortal egress unreachable [SEVERITY: HIGH]** — `hero could not path to CavePortal_Trigger,
  closest 403m of radius 16m`, x3 (WO-608 port-around class).
- Two NEW flaky-but-real (1/4) fails on the same egress/encounter system: MaintainScatter cull
  miss on warp (#5) and an encounter resolve-loop stall (#9) — route to the PlayMode/Combat
  coverage lane, not one-off fixes.

**UNVERIFIED on real Pi Browser [SEVERITY: HIGH — the headline gap]:**
- No traced end-to-end run has ever been done in the actual Pi Browser (`WO-684` §B.9).
- Combat touch completeness on a phone thumb (move + aim + Q/W/E/R + interact) is unverified;
  the input audit once found `TryCast` with NO input surface (`WO-684` §B.11).
- No clip-decode proof on WebGL — only a real browser proves audio actually decodes; a
  `Loading FSB failed for audio clip "SwordSwing"` was db-captured twice, paired with 167ms/4000ms
  stalls (`CANON_GROUND_TRUTH` line 37). WO-682 prewarm mitigates but is device-unverified.

---

## 3. RISK REGISTER

Severity as text. Owner column names who acts.

| # | Risk | Severity | Evidence | Owner | Mitigation |
|---|------|----------|----------|-------|-----------|
| R1 | Demo never run on real Pi Browser; whole quality claim is headless-only | CRITICAL | WO-684 §B.9 | PO+CLI | One `?trace=1` traced device session -> real punch list, BEFORE more polish |
| R2 | WO-602 home-return unwired — judge cannot return to hub | CRITICAL (demo) | `HOME_RETURN_FAIL gate=<none>` x4, Fleet #1 | CLI | Wire a hub-return SceneTransitionTrigger / return portal |
| R3 | Encounter-return strand lands hero ~7km off | HIGH | `dist 7088.1m` x4, Fleet #2 (WO-453) | CLI | Fix return-teleport anchor; add PlayMode oracle |
| R4 | HelpMenu 5-tap self-grants 25k crystals in RELEASE builds | HIGH | SECURITY H4, `HelpMenu.cs:70-75,155-175,234-276` | CLI | `#if DEVELOPMENT_BUILD||UNITY_EDITOR` gate before any public build |
| R5 | Open unauth POSTs, no rate limit; no web_trace TTL cron -> unbounded DB growth + flood | HIGH | SECURITY H1/H2; no `crons` in vercel.json | CLI+PO | Add `api/admin/cleanup.js` cron + WAF rate-limit on trace/track/bug-report |
| R6 | Reward endpoints (promo/redeem, referral/claim+generate) unauth -> self-farm | HIGH (pre-value) | SECURITY M2 | CLI | Apply `verifyAndConsume` nonce gate (install-brag.js precedent) |
| R7 | OuterWorld ~1fps open blocker, root-unproven | HIGH | Catalog P1 #2, WO-684 §B.10 | CLI | Instrument-first per §12; F8-37 audit walks 51 renderers/frame |
| R8 | 129MB data ship, no mobile perf budget, LOW-fps + 229ms long-tasks | HIGH | WO-684 §B.10, `CANON_GROUND_TRUTH` line 54 | PO+CLI | Define "loads in X s / holds Z fps"; un-park WO-545 Addressables if it fails |
| R9 | Apex dragon model licensed CC BY-NC | HIGH (legal, pre-commercial) | memory `black-dragon-license`, WO-684 §D.15 | PO | License or replace before any commercial ship |
| R10 | Desktop release still ships BuildOptions.Development (DevTools leak) | MED | `DesktopBuild.cs:178`, WO-684 §C.13 | CLI | Flip to `BuildOptions.None` for release |
| R11 | B2 dual-wallet divergence — Grant income invisible to upgrade ledger | MED (economy) | `data-regression.log` VILLAGE ECONOMY FAIL | CLI | Route income via GrantSpendable / unify pools |
| R12 | Pet active-slot resets on reload (flag_17) | MED (economy) | `data-regression.log` GLIMMER ECONOMY FAIL | CLI | Persist slot field + SaveSchema round-trip |
| R13 | Tribes/Wards/Arena-WL not persisted (3 fail-by-design) | MED | `core-save-suite` reds | CLI | Thread through SaveSchema + version bump |
| R14 | fountain_healing L2/L3 upgrade models load NULL from Resources | MED | `data-regression.log` BUILD ECONOMY | CLI | Provide/repath `Structures/Fountain_L2/L3` |
| R15 | Loader-stage web errors (pre-Unity-boot) reach no telemetry | MED (blind spot) | WO-684 §C.12 | CLI | ~10-line template JS beacon to /api/trace |
| R16 | Privacy note absent; analytics + traces collect session data | MED (compliance) | WO-684 §D.16 | PO | Publish privacy statement for Pi listing |
| R17 | Loader-error / preview SSO friction slows device testing | LOW | WO-684 §C.14 | PO | Standing share-bypass or disable preview protection |
| R18 | Neon credential once pasted in chat (DEPLOY.md:11) | LOW-MED | SECURITY S5 | PO | Confirm rotated |

No CRITICAL security findings; SQL uniformly parameterized, no committed secrets, local PlayerPrefs
edit is accepted-by-design (soft-currency-client-owned stance; nonce-auth scaffolding exists).

---

## 4. WHAT'S DONE vs IN-FLIGHT vs NOT-STARTED

| DONE (proven) | IN-FLIGHT (in tree / lanes open) | NOT STARTED |
|---|---|---|
| Core loop boot->build->battle->save (fleet PASS) | WO-683 d-pad DPAD-specific fleet probe verdict + owner felt-pass | Real Pi-Browser traced run |
| Build-mode touch: verb bar + PLACE + kit d-pad (`f123859d`,`c963a553`) | 4 SME regression suites wired; Combat+Dialogue added tonight (`98131cb0`) | Mobile perf budget definition |
| WO-682 quiet web errors: FSB root fix + combat prewarm + oracle (`965309a6`) | VFX Caster tag-to-catalog | PlayMode test asmdef suite (WO candidate) |
| Save schema v30, signed HMAC local save | Gear curation overlay (446-pool -> curated shipped set) | CI / automated pre-commit REGRESSION_OK gate |
| Tofu sweep 47->5 (`98131cb0`); ASCII sweep near-complete | db-viewer activation (owner sets ADMIN_DASH_KEY + redeploy) | HelpMenu release-grant gating (R4) |
| WebTrace live end-to-end (`?trace=1` -> Neon, runtime-log echo) | Security top-3 fixes (specced, not applied) | Home-return wiring (WO-602) |
| Deterministic ATB engine + Dialogue path pass | Data dual-copy sync per gear ruling (flagged expected) | Encounter-strand fix (WO-453) |
| Security audit complete (0 CRITICAL) | 5 SME architect regression paths (Combat/Dialogue now covered) | Privacy statement; dragon license/replace |

---

## 5. WHAT'S MISSING — gaps a shipping PM flags

- **No CI.** Every gate is manual discipline; nothing auto-runs `REGRESSION_OK` on commit/PR.
  Named the single biggest coverage-leverage item (`WO-684` §R.6). Highest-leverage missing safety net.
- **No real-Pi-Browser run.** The entire feel/perf verdict is headless-Windows-only (`WO-684` §B.9).
- **No mobile perf budget.** 129MB ship, LOW-fps, 229ms long-tasks, OuterWorld ~1fps — all
  measured against no target (`WO-684` §B.10).
- **Combat touch completeness unverified** on a device thumb (`WO-684` §B.11).
- **No PlayMode test suite.** Backend delta-sync, PersistenceBridge triggers, OnApplicationQuit
  ordering, BaseLayoutLoader.Spawn, Obsidian pixel invariants — none headless-reachable (`WO-684` §R.1).
- **Loader-error blind spot.** Errors before Unity boots reach no telemetry (`WO-684` §C.12).
- **Closed tonight (credit):** Combat/ATB and Dialogue were the last two uncovered architect paths;
  both got SME suites in `98131cb0`. Combat now shows one honest fail-by-design (orc-raider stat
  divergence: one id, two stat blocks Hp 95 vs 170) — a real defect the fleet couldn't see before.

---

## 6. RECOMMENDED SEQUENCING TO JULY 31

Ruthless order. Demo-lethal flow bugs and ONE real Pi-Browser traced run come BEFORE any polish.

**TOP 5 for the remaining runway:**

1. **Wire WO-602 home-return** (and re-anchor the WO-453 encounter return-teleport). Fleet proves
   both break every run; a judge who explores is dead-ended today. Highest demo-lethality.
2. **Run ONE real Pi-Browser traced felt-session** (`?trace=1`) end-to-end on a phone and generate
   the actual punch list. Everything above the core loop is unverified until this exists.
3. **Ship the security top-3 before any public/shareable build:** gate the HelpMenu 25k-crystal
   self-grant (R4), add the web_trace TTL cron + WAF rate-limits (R5), nonce-gate the reward
   endpoints (R6). Small edits, real exposure.
4. **Set a mobile perf budget and measure the 129MB ship + OuterWorld ~1fps against it;** instrument
   OuterWorld first (§12), un-park WO-545 Addressables only if it misses. Perf is a device verdict,
   not a desktop one.
5. **Verify combat touch completeness on a device** (move + aim + Q/W/E/R + interact all
   thumb-reachable) and confirm the WO-683 d-pad DPAD-specific probe + owner felt-pass close.

After the top 5: economy-integrity schema lane (R11-R14, one-at-a-time), loader-error beacon,
compliance gates (dragon license + privacy note) before any commercial listing, then polish.

---

## 7. WHAT'S GOING WELL — credit the machine

- **Instrument-first culture is real and paying off.** Bugs arrive as captured lines with RCA
  (`HOME_RETURN_FAIL gate=<none>`, `dist 7088.1m`), not theories. The §12 hard gate is being kept.
- **Ticket pipeline QA->CLI->PO** is running with role separation; 10 fleet tickets triaged
  8-known/2-new with clean routing.
- **Fleet (4-bot seeded chaos)** verifies the core loop every run and isolates fails to the two
  known open strands — a trustworthy regression signal.
- **6-path SME regression coverage** now catches real defects the fleet couldn't (dual-wallet,
  pet-slot, dual-copy drift, tofu, orc stat divergence) with ZERO false positives — 8 truthful reds.
  Combat + Dialogue paths closed tonight.
- **Web observability proven end-to-end:** `?trace=1` -> `/api/trace` -> Neon, read via the `[sig]`
  runtime-log echo. Real captures already flowing (SwordSwing FSB + stalls).
- **Canon system + doc diagnostics:** `KEY_FACTS.md` living card, dated ground-truth anchor,
  `doc_diagnostics.py` freshness checker (caught the v30 schema drift, `c339bcfb`). Docs stay green.
- **Owner tooling shipped:** db-viewer, Gear/VFX/Motion Casters — self-service authoring and a
  read path into live data.

---

### Appendix — the 8 truthful regression baseline failures (`Builds/data-regression.log`)

1. **Arena prefab** — `ForestClearingArena` Ground + DistantGround `ArenaGround` mat binds NO base
   texture (flat/untextured); ties to F8-37 arena z-fight cluster.
2. **Village economy (B2)** — dual-wallet divergence: `Grant(wood:25)` moves shop pool +25 but
   upgrade ledger +0 (income invisible to upgrade flow).
3. **Glimmer economy (flag_17)** — no persisted pet active-slot field; multi-slot pet roster
   resets on reload.
4. **Core-save (x3 fail-by-design)** — Tribes, Wards, Arena W-L have no PersistedState field; each
   resets on reload.
5. **Build economy** — `fountain_healing` upgradeVisualPath L2 + L3 (`Structures/Fountain_L2/L3`)
   load NULL from Resources.
6. **Data-web** — dual-copy drift across armor/daily-quests/skin/stake-rewards/tower-perks/weapons
   (weapons: 266,592 B StreamingAssets vs 19,093 B Resources; Resources wins). Gear ruling = curated
   small set is truth (446-item pool stays as Gear-Caster curation source); stays flagged-expected.
7. **HUDUI (5 tofu)** — U+2248, U+2692, U+1F48E, U+25C8, U+2726 render as tofu on device (down from
   47; ASCII sweep near-complete).
8. **Combat/ATB (1 fail-by-design)** — orc-raider stat divergence: spawners build Hp 95,
   GarrisonController builds Hp 170 (x1.2 = 204). One id, two stat blocks — unify the roster source.
