# HANDOVER — 2026-06-10 (CLI session)

## ⚠️ READ FIRST — trust situation + working agreement
This session broke owner trust. Causes: repeated false **"it's fixed / should work"** claims before
the owner verified, and a **stale build** (I ran a *normal* `build-windows.ps1` instead of a clean one,
so IL2CPP reused a stale compile and the freeze fixes were **not in the exe** — the owner tested code
that didn't contain the fixes, and lost the cycle).

**Agreement going forward (honor this):**
- **No "fixed / should work / this is the one" claims.** State only verifiable facts: "committed as `<hash>`",
  "gate compiled (`COMPILE_GATE_OK`)", "the code now does X instead of Y." Predictions are off the table.
- **Owner's playtest is the ONLY verdict** — not a green gate, not my diagnosis.
- **Root-cause before patching.** No more symptom-patches.
- Don't hype builds / don't push "test this."

---

## ⏰ MORNING FIRST (night of 2026-06-10 → 11)
1. **TEST `Builds\Windows\DefendersOfTheRealm.exe` @ 22:02** — known-good HUD + the **candidate perf fix**: the per-frame `FindObjectsByType` whole-scene **bridge scans were retired** (`TownHudBridge`/`PartyHudBridge`/`WaveHudBridge` → O(1) `Tower.ActiveCount`/`StoryCompanion.Active` registries; commit **`463a5e8`**). THE open question: does the open world still go frame-by-frame? PerfDiagnostic is still in the build — owner plays → says "done" → CLI reads `[PERFDIAG]` heap/CPU. Clean ⇒ bridge scans were the leak; still slow ⇒ deeper, PerfDiagnostic narrows it. *(This was never tested last night — owner went to sleep mid-architecture-pass.)*
2. **New unified HUD is STASHED** (`git stash list` → "WO-403 new unified HUD rewrite") — to be **redone modular** (<800 lines, bounded-context) per `docs/WO-403_405_RECONCILIATION_AND_AM_PLAN.md`. 3 owner decisions wait at the top of that doc (OK the light-kit refactor, build combat group to `hud_mobile_combat.png`, number the buildings-collection WO).
3. **Architecture canon committed** (`095ac06`): `docs/ARCHITECTURE_PRINCIPLES.md` is binding law (HP-B2B bounded-context; presentation never touches objects; pooling; **tests = permission gate**). Plus `WORLD_COLLECTION_MODEL_DIRECTIVE.md`. CLAUDE.md points at it.
4. **Monolith split (owner's overnight directive)**: 20 files >800 lines. CLI wrote the **split PLAN → `docs/MONOLITH_SPLIT_PLAN.md`** (`095ac06`-area; bounded-concern decomposition + test gate per file + Wave A/B/C/D order) and executed **only the one provably-safe win overnight**: deleted dead **`BattleHud` + `BattleVfx`** (~1,675 lines, commit **`01fd781`**, gate-verified, zero code/scene refs — it was the old UIDocument HUD; live one is `BattleHudUgui`). CLI **held the rest** (Wave B/C/D = live-gameplay refactors: BuildModeController 1682, HeroInventoryController 1559, WaveManager 1556…) — per the affirmed discipline (no bulldozing unverified gameplay while she sleeps; law = tests gate refactors). Execute those in the morning per the plan, each with a characterization test + owner build-verify. Plan flags: `BattleHud` done; `SmartMobileCamera`/`GameState.cs` = DO-NOT-SPLIT (cohesive); `TitleController`+`HeroSelectController` share ~600 lines (shared `HeroRosterView` WO); `HeroInventoryController`/`ShopPanel` re-implement `ElarionUiKit` helpers (dedupe).

---

## 🔴 CURRENT BLOCKER — OuterWorld "frame by frame" (~1 fps)
- **Symptom:** after the city (OuterWorld) streams in additively, the game runs ~1 fps ("frame by frame"),
  **even with 0 enemies spawned.** Hero + village load fine first; it degrades once OuterWorld is visible.
  It is a sustained per-frame cost, **not** a hang/crash (earlier reports of "hang/crash" were this + a
  separate now-fixed hard-hang).
- **Status: 5-agent read-only RCA done — static analysis EXHAUSTED, moved to MEASUREMENT.**
  Each agent ruled out its own category with evidence:
  1. per-frame scene-scans → the one real cost was the tower scan (fixed, see below); rest throttled/gated.
  2. rendering/scene-complexity → OuterWorld.unity is NEARLY EMPTY (10 renderers, 1 terrain, 1 light, mobile-URP,
     sub-1ms GPU frame). "Additive doubling = heavy render" theory is WRONG — OuterWorld is light.
  3. physics/navmesh → all throttled/masked/one-shot; portal retry-cap verified; `TorchFireController` has an
     unmasked per-frame OverlapSphere but is NOT in any committed scene (check gitignored torch props live).
  4. update-storm/GC/telemetry → no storm at 0 enemies; EventTracker is 30-60s loop not per-frame.
  5. VFX (owner's clue "started with the VFX") → ruled out as an IDLE cost: `ProjectileVFXCatalog` spawns
     on-shot only, VfxPool/VFXManager instances are SetActive(false), the VFX commit `49955aa` touches ZERO
     tower files. BUT static analysis CANNOT see runtime GPU/render or shader-compile cost — the VFX clue can
     only be confirmed/refuted by a profile.
- **The one PROVABLE per-frame cost:** `DefenseTower`/`ArcaneTower` `Rescan()` ran `FindObjectsByType<MonoBehaviour>`
  every 0.4s/tower over the whole loaded world (introduced `9c5706c`, LONG before VFX). **Fixed in `4b5208c`,
  verified live in HEAD** (`git merge-base --is-ancestor 4b5208c HEAD` = yes).
- **Two things static analysis CANNOT answer** → why we measure: (a) did the build the owner tested actually
  contain `4b5208c` (the stale-build trap)? (b) if yes-and-still-slow, is it a runtime render cost (the VFX clue)?
- **MEASUREMENT STEP (current):** `PerfDiagnostic.cs` (commit `c63dc03`) — a ProfilerRecorder instrument that
  logs `[PERFDIAG] ... fps=.. CPU-frame=..ms GPU-frame=..ms draws=.. gc=.. => CPU-BOUND / GPU-BOUND` every 2s.
  Clean build with it cooking. **Owner runs it once → into OuterWorld → ~5s → the Player.log verdict tells us
  CPU(script) vs GPU(render), which resolves BOTH open questions.** THEN the targeted fix. (Diagnostic is
  removable; delete `Assets/_Modules/Core/Debug/PerfDiagnostic.cs` after.)
- NOTE: gate had to park an unrelated untracked WIP file `Assets/Editor/UiKitReviewSceneBuilder.cs` (other UI
  session; references an undefined `Stretch()`); parked in `_wip_parked/`, restore after the build.

---

## ✅ Committed this session — UNPUSHED (~80 commits ahead of `origin/feat/tower-core-loop`, held for owner sign-off)
Freeze fix (OuterWorld-load: portal-retry cap + 3-min outpost delay + staggered realize) · the perf-fix
*attempt* (tower scan) · **real rigged+textured Knight** (hero-tank AccuRIG; replaced the broken Tripo FBX) ·
**RPG UI pack** integrated (Tech hud elements → HUD/inventory/battle, sprite-first w/ fallbacks) ·
**garrison recipe engine** (4 scenes from `garrison-recipes.json`, JSON-only new locations) · real particle
**VFX** (replaced billboard projectiles) · full **light UI sweep** (HUD/inventory/shop/battle/structure-preview) ·
weapon grip (hand-relative) · hero idle pose (WO-376) · dialogue readability (options dark) · camera facing-trail ·
ATB cleanup · char-select framing · marketplace stock fix · runtime hardening · WO-408 texture toolkit (NOT run) ·
dev grant bump (50k) · **owner's hand-dialed MainCastle_Hall locked in** · test scenes removed (CastleTest/2,
Village2Test, EnemyOutpostTest, Village3 + Village3 dropped from build settings).
Notion board reconciled to git (one dupe WO-374 page fixed; WO-403/404 flagged as maybe-prematurely-Done).

---

## 📋 Open / pending
- **The RCA → the real perf fix** (in flight; do not patch before the root cause).
- **WO-409 bug 1 — magenta towers** (task #45): the scan proved it's NOT null-material (4142 scan hits are all
  unused VFX-pack particle null-materials); it's a **built-in Standard/Legacy shader rendering pink under URP**
  on the tower/structure prefabs. Targeted fix: find those materials → swap Standard→URP/Lit (PolyperfectUrpFix
  or a targeted swap). **Do NOT run the broad `MagentaMaterialFixer`** — its null-fill step would break the
  `Resources/VFX` projectiles. Scripts committed as tooling: `MagentaMaterialScanner/Fixer`, `UiSpriteRefValidator`.
- **WO-409 bug 2 — HUD `*`/`#` glyphs**: addressed by the RPG-UI pack integration (run `RpgUiImporter.Run`; done
  this session — 25 sprites in `Resources/RpgUi`). Owner verifies they render.
- **WO-408 texture-opt — committed but NOT RUN.** Run `TextureAuditTool.RunBefore → TextureBatchOptimizer.Run →
  TextureDuplicateFinder.Run → WebGLPlayerSettingsConfigurator.Run → TextureAuditTool.RunAfter` to cut WebGL
  textures 203 MB → ~40-60 MB (WebGL platform overrides only; QA thumbnails in `Builds/TextureAudit/`).
- **WebGL/itch size:** itch rejected the build ("Zip contains file that is too large" — `WebGL.data` 223 MB
  uncompressed). Fix = Gzip compression (`webGLCompressionFormat=Gzip`; decompressionFallback already ON, so it
  loads on itch via the JS fallback) OR the WO-408 texture-opt. Itch channel: `denellestudios/...:html5`.
- **Arena**: fully solutioned in `docs/ARENA_SOLUTION.md` — ~75% built; **owner decisions captured**: the
  defender base is the player's **own self-designed flat-grid base** (NOT the multi-level castle, which AI can't
  path — see memory `arena-defender-is-self-designed-base`), **gated behind base-building** (build a base → arena
  unlocks). PvE-first; async-PvP later via the same `BaseLayout` snapshot (no live netcode). Phase 1 buildable on
  owner go.
- **CastleHubBuilder offset-capture** (deferred): owner hand-dialed `MainCastle_Hall`; the scene is committed, but
  the builder should be updated to reproduce the offsets so a regen can't revert the owner's work.

---

## 🔧 Operational facts (IMPORTANT — these caused real misses this session)
- **CLEAN BUILDS ARE MANDATORY for verifying code changes:** `Remove-Item Library\Bee -Recurse -Force` before
  `build-windows.ps1`. A normal/incremental build can reuse a **stale IL2CPP compile** → the C# changes DON'T
  reach the native exe. A green gate compiling does NOT mean the build contains the code. (This wasted an owner
  test cycle — the 18:04 build was stale.)
- **Editor coordination:** any batchmode (gate/build/run-unity-method) **aborts** if the Unity editor is open
  ("Unity OPEN - abort" — correct behavior, avoids project lock). Owner opens the editor to hand-dial scenes /
  play-test. Confirm `Get-Process Unity` is empty (only "Unity Hub" OK) before batchmode.
- **Dev tool:** **F1** (or the on-screen **DEV** corner chip) → **"Load up (full base)"** = +50k Wood/Stone/Iron
  (`DevPanelController`, bumped this session for base-build testing).
- **3-min outpost delay:** `RaidOutpostSystem` now delays the EnemyOutpost spawn 180 s (owner's idea) so the heavy
  fort+garrison realize never lands on the city-emerge frame. The frame-by-frame is NOT the outpost (0 enemies +
  it's delayed/staggered).
- Player.log: `C:\Users\Kayden-Laptop\AppData\LocalLow\DeNelle\Defenders of the Realm\Player.log`.

## RCA targets / key files
OuterWorld load = `WorldSceneLoader` + `Assets/Scenes/OuterWorld.unity`. Per-frame candidates to scrutinize:
`StructureAttackAlert`, `TowerCombat`, `WaveManager`/`AlertIntelSystem`, `NodeDiscoverySystem`, `WardTetherService`,
`Settlement`, `HarvestSite`, `ClaimableCamp`, `WorkerManager`, `SmartMobileCamera`, `DungeonWorldPortalSpawner`,
the HUD bridges. The perf-fix-attempt: `DefenseTower.cs`/`ArcaneTower.cs` (`4b5208c`).
