# READY-state RCA - the full story, 2026-09-06 (evening)

Every ticket that was READY TO IMPLEMENT or REOPENED at HEAD `c9efbe0a5`, root-caused from what was read this
session: captured logs, PNGs, and `file:line` at HEAD. Where a cause is not proven it says so and names what
would prove it. Nine tickets that had already landed in `32659c0f6` / `5bc5025f5` were flipped earlier tonight
and are not repeated here (1420, 1432, 1434, 1435, 1436, 1437, 1439, 1441, 1442, 1443, 1356).

## Executive summary - ordered by severity

| # | WO | Root cause in ten words | Fix size | Blocker |
|---|---|---|---|---|
| 1 | NEW (api) | Renewal cap INSERTs `signed_at`; column missing on live Neon | 1 migration file + 1 owner run | owner runs `run-schema-repair.mjs` |
| 2 | NEW (persistence) | Cloud LOAD applies seven currency fields, drops the whole town | medium (call `ApplyPersisted`/`MigrateForImport`) | none |
| 3 | 1327 | Clamp fixed BOUNCE class; "orb stayed at me" is a persisting-effect class | unknown until captured | device capture inside walls |
| 4 | 1215 | 18 of 19 shields have no offset row, attach at identity | medium (derive from bounds per canon) | none; Fail note empty |
| 5 | 1244 | Writes answer `OPS_WRITE_NOT_CONFIGURED`; `ADMIN_OPS_KEY` unset on deployment | 1 env var | owner sets the key |
| 6 | 1438 | Selector preferred nearest STRUCTURE over reachable unit; navmesh under felled wall unmeasured | in lane (uncommitted) | raid capture for `holeNavmesh=` |
| 7 | 1411 | Collections/ghost/confirm never carried affordability, verbs, cost/time | in lane (uncommitted) | headless capture |
| 8 | 1408 | COLLECT calls `Dismiss`; no door to what finished while away | in lane | none |
| 9 | 1405 | Defense rows emit `"grid " + cellX` from the VM | in lane | none |
| 10 | 1409 | Badge/LEDGER overlap unproven at HEAD; copy half landed | in lane | none |
| 11 | 1445 | `Grant` discards the pre-clamp remainder, the one burning path | small | none |
| 12 | 1444 | `FaceCountText` composed, never painted; two authorities | small | owner ruling a/b |
| 13 | 1430 B | Five authored fields still read by nobody; exempted by dated pointer | five small | none |
| 14 | 1348 | Held behind 1343-1347; all five are now FIXED/CLOSED, hold is stale | medium | release the hold |
| 15 | 1292 | Blocked on 1291 (30/33 addresses swapped, 3 unmapped) | bake | 1291 + Unity bake |
| 16 | 1373 | Blocked on the exclusivity reversal ruling | large | owner ruling §4 |
| 17 | 1377 | Blocked on the save-serialisation rename risk ruling | medium | owner ruling §4 |
| 18 | 1314 | Root retracted; parked with Pi | large | owner's word |
| 19 | 1431 | Cosmetic staff grip; tracked, not queued by owner's word | small | none |

Recommended order: 1 and 5 tonight (one owner command each), 2 next (data-loss class), then gate the four lanes
in flight (6-10), then 4 and 3 with captures, then 11-13, then release 14.

---

## The two P0s with no ticket yet

### NEW-A. The api renewal cap will 500 every wallet mint the moment it deploys
- **Symptom:** none yet; it is uncommitted. The moment any seat runs `vercel --prod` it goes live.
- **Root cause (proven at source, and on the live DB this session):** `api/_lib/wallet-auth.js:315-320`
  `INSERT INTO auth_sessions (token, wallet, identity_kind, expires_at, signed_at)` runs on the NORMAL mint path
  (`api/auth/session.js:175`). Live Neon: `tools/wo1440-alter-column-sweep.mjs` -> `MISSING ON LIVE DB:
  auth_sessions.signed_at`. The file's comment at `session.js:137-146` claims a schema-missing renewal "falls
  through to full verification, so a lagging DB cannot cause an outage" - the fall-through lands in `issueSession`,
  which is the INSERT that fails. Same shape as `identity_kind` in WO-1440 §7c.
- **Tried:** nothing deployed. The unit test (`test/auth.session.renewal-cap.test.js`, 9/9 green) asserts the
  comment text, not the property.
- **Fix:** (a) a numbered migration `api/migrations/20260906_0020_auth_sessions_signed_at.sql` (the ALTER exists only
  in `api/schema.sql:244`); (b) owner runs `node tools/run-schema-repair.mjs` with `DATABASE_URL` set, judged by
  `SCHEMA_PARITY_OK`; (c) only then commit + deploy. Also close the P1 found alongside: the cap gate
  `session.js:91 (sessionHeader && !nonceHeader)` is skipped by any junk `X-Nonce`, and `verifyWallet` returns ok
  on the session rail before touching the nonce (`wallet-auth.js:569-572`), which resets the chain origin.
- **Blocker:** owner-only DB credential. **Severity P0** (outage class on deploy).

### NEW-B. Cloud LOAD restores currencies only - a reinstall loses the town
- **Symptom:** not yet felt, because no cloud save has landed all day (WO-1441). It will be felt on the first
  reinstall or second device.
- **Root cause (proven at source):** `GameStateService.cs:2099-2140` copies `BestWave`, `Resources`, `Voidshards`,
  `AetherCrystals`, `Stone`, `Iron`, `Wood` from a full `PersistedState` (`:2894`) and then `Save()` at `:2145`.
  `ApplyPersisted` / `MigrateForImport` are called only on the local path (`:387`, `:404`). The server row holds the
  whole state (`api/game/load.js:100-104`); the client discards layout, army, queue, hero level, echoes.
  Second consequence: `PersistenceBridge.cs:117-138` fires this on every scene enter and the resource copy is
  unconditional, so a stale row overwrites locally earned resources and persists the rollback.
- **Tried:** nothing; no ticket existed.
- **Fix:** route the cloud row through the same `MigrateForImport` + `ApplyPersisted` the local path uses, and
  compare `serverLastSeenMs` (already parsed at `:2079`) to the local save before overwriting. Silo: Core/State.
- **Blocker:** none. **Severity P0** (data loss class). Also mint P2: `api/game/save.js:339` lets
  `schema_version` go backwards (defaults to 10 when absent, `:332`).

## Reopened by owner felt-test

### WO-1327 - the fireball returns to the caster
- **Owner's words:** seq 4152 (08-31) *"fireball when i cast spins at me after casting"*; seq 4644 (09-02)
  *"casts at me and stays at me"*; bounce note 09-04 14:36 on build `2026.09.04.354315`: *"red glowing orb stayed at me"*.
- **Root cause:** NOT proven. The RESULT (`f27e95724`, 09-02) read the prefab: `collidesWith` = all 32 layers,
  `m_Bounce` 1.0, `m_EnergyLossOnCollision` scalar 1 over a zero curve, and clamped at `VFXManager.cs` (bounce 0 /
  dampen 1 / lifetimeLoss 1 at the one spawn owner). The WO itself says at `:33` *"a mechanical reading of captured
  settings, not a proven root"*. The 09-04 build carried the clamp and the owner still saw an orb stay on her.
  The WO's own line `:82` names it: the new symptom is the seq-4644 class (an effect PERSISTING on the caster),
  not the bounce class the clamp addressed. No F8 capture carries those words; seq 4680 at 14:35:49Z is an
  `[Flow:EndState]` line, not this.
- **Why the fix did not hold:** it fixed a different mechanism than the one she sees. Candidates that were never
  examined: a second emitter in the sequenced marquee prefab parented to the caster, a light/aura child with its
  own lifetime, or the spawn owner parenting the whole effect to the hero transform.
- **Fix:** none until captured. Tonight's lane adds permanent `[Flow:VFX]` lifecycle tracing (spawn parent, travel,
  hit, despawn, position relative to caster). Then one capture: cast the fireball inside the walls with F8.
- **Blocker:** device capture. **Severity P2** by the WO; felt twice, so treat as P1 for the felt-test loop.

### WO-1215 - the shield sits through the body
- **Owner's words:** 08-26 *"and shield sits through the body not seated correctly"*; 09-03 felt-test **Fail**,
  note empty, build not recorded in `proof/owner-validations.json`.
- **Root cause (proven at source, unchanged since the WO was minted):** `Assets/OffsetForge/offsets.json` has 26
  rows and two mention a shield (`shield_A` hand-dialled; `ShieldWithItemLogic` at rot 0,0,0). `weapons.json` has
  19 shield entries; **18 have no offset row**, so they attach at identity, which is the screenshot.
- **Tried:** `c7268971d` (08-26) added `AttachmentOffsetRegression` (233 lines) and bumped the suite to 294/294.
  It pinned the two rows that exist; it did not derive offsets for the other 18, and the owner's shield is
  evidently one of the 18. The regression cannot see that because it asserts the file, not the seat.
- **Fix:** derive shield seat from mesh bounds + name per `docs/WEAPON_ARMOR_ORIENT_LOGIC.md` (canon says identity
  is a principle violation), so every shield id gets a pose without a hand row; add a case that fails when any
  shield id in `weapons.json` resolves to identity. Silo: gear seating.
- **Blocker:** none; a device PNG after the fix. **Severity P1** (visible on every shield).

### WO-1244 - the command center console
- **Owner's words:** none recorded (Fail, empty note, 09-03).
- **Root cause (proven at source):** the console exists (`api/admin/console.js`, `ops.js`, `stats.js`). Writes are
  gated on a second secret: `ops.js:20` `X-Admin-Ops-Key must match ADMIN_OPS_KEY`; `:33` *if unset the endpoint
  answers `OPS_WRITE_NOT_CONFIGURED`*. The WO's own banner says the key is not set on the deployment, so
  acceptance 5 (flip a toggle from the phone) cannot pass, and acceptance 2 (a true 390 px screenshot) was never
  produced because headless Edge enforces ~500 px.
- **Why the fix did not hold:** the surface was built; the deployment configuration was not.
- **Fix:** owner sets `ADMIN_OPS_KEY` in the Vercel project env (production) and redeploys; then a phone-width
  screenshot. If she still marks Fail, the next fail note must say what she saw - the record holds nothing.
- **Blocker:** owner env var + the deploy landmine in NEW-A (a redeploy sweeps the api work in). **Severity P1** (ops).

## In a lane tonight (uncommitted in the working tree)

### WO-1438 - raid AI does not push the breach
- **Root cause (proven from `logs/debug/troop-ai-blind-2026-09-06.log`, 14:36):** `RETARGET#1 reason=foe-null ->
  won='Wall_Outer_SS_11(WallSegment)' ... accepted[unit=1,struct=17]` - a live defender was inside the sweep and a wall
  4.2 m away won; that archer then walked seven wall segments. Also `routeStatus tally: 135 CalculatePath-FAILED /
  7 PathComplete`: the probe measured the target's kind, not the breach (a structure's `WorldPosition` is off-navmesh).
- **Tried:** `5bc5025f5` shipped `[Flow:TroopAI]` instrumentation only; the behaviour fix was deliberately left.
- **Fix (in tree):** units and structures in separate buckets; a reachable unit beats a nearer structure for
  non-siege roles; `holeNavmesh=`/`selfNavmesh=` probe fields; `CombatFactionRules` reaches `IDamageable`;
  `AwarenessSensor` no longer commits to own-faction structures. **Unpinned:** whether the navmesh under a felled
  wall is walkable (`0 carving obstacle(s) dropped` on every collapse) - if `holeNavmesh=NOT-WALKABLE` on the next
  raid capture, a carving `NavMeshObstacle` is still required and the selector fix is half.
- **Blocker:** gate + raid capture. **Severity P1** (raid loop).

### WO-1411 - Build never says what you can afford
- **Root cause (proven at source, HEAD):** `BuildCollectionBrowser.cs:214` painted a category title with no
  affordability; the ghost rail at `BuildHudController.cs:686-696` painted three glyphs; `BuildPreviewModal` had no
  cost/time line; `BuildFirstUseGuide` only advanced from the browser, so arming a ghost from the carousel left
  *"First build: select a category."* over the ghost.
- **Fix (in tree):** VM fold `StructureCardVM.AffordableCount`; PLACE/ROTATE/CANCEL words; cost . time . crew line
  from the commit path's own three steps; `GhostArmed()`; renames Towers / Walls & Gates in both JSON twins
  (99 LF each, byte-verified); `BuildAffordabilityWordsRegression` new; three oracles re-pointed.
- **Blocker:** gate + headless capture (label fit inside the ornate caps is the open risk). **Severity P1** (retention).

### WO-1408 / WO-1405 / WO-1409 - the "reason to tap the next screen" trio
- **1408 root cause (source):** `WelcomeBackPopup.cs:107-108` COLLECT -> `Dismiss`; the away-time facts (queue
  completions, defense report, army fill) exist and are not surfaced. Fix: rows become doors through `PanelRouter`
  and the existing return arbiter. In lane.
- **1405 root cause (source):** `ManageScreenVM.cs:1063` emits `"grid " + cellX`; Buildings/Troops half landed in
  `3c677027e` (`ManageScreenPanel.cs:3367,4293`). Fix: VM composes display name + compass side; suite
  `ManageRowBenefitRegression`. In lane.
- **1409 root cause (PNG `NightMarket_2670x1200.png`, 09-05):** nine "unavailable"s with no sentence; copy half
  landed in `3c677027e` (`PackStore.cs:3118`); badge/LEDGER overlap seen on the device frame with a wallet too, so
  layout not fixture. Fix: the geometry half + `NightMarketNoWalletRegression`. In lane.
- **Blocker:** gate + captures. **Severity P1** each (the sprint's own name).

## Minted tonight

### WO-1445 - `Grant` discards the clamped remainder
- **Root cause (source):** `OfflineHarvestService.cs:1043-1050` banks `ClampGrant` results and computes the
  remainder nowhere, against WO-1434's own law (`[silo-never-burns]`). Dead on the owner's save (`total=0`).
- **Fix:** retain on the existing pending store; `FlowTrace.Warn` at the clamp; one RED case. **Severity P2.**

### WO-1444 - the QUEUE face count is composed and never painted
- **Root cause (source):** `ManageScreenVM.cs:3481` composes `FaceCountText`; no reader; pill hardcoded at
  `ManageScreenPanel.cs:2091`; badge recomputed by the View at `:2368-2372`. The word FULL never reaches a player.
- **Fix:** (a) paint it and retire the badge, or (b) delete it and toast FULL on refusal. **Blocker:** owner ruling.
  **Severity P2.**

## Parked, blocked, or held

- **WO-1430 group B:** five authored fields still read by no production code, each exempted in
  `AuthoredFieldReaderRegression` with a dated pointer (`:88`); group A closed in `bb51b8b9c`. Fix: wire or delete
  each field; five small edits. No blocker. P2.
- **WO-1348:** held until 1343-1347 landed. All five now read FIXED or CLOSED, so the hold is stale; release it.
  Cause is not a defect: the VFX pick is build-time and the ask moves it to a runtime tunable. P2.
- **WO-1292:** blocked on 1291 (IN PROGRESS, 30/33 addresses, 3 unmapped) and needs a scene bake under the Unity
  lock. Editor script landed `33ba9c966`. P2.
- **WO-1373:** blocked on the exclusivity reversal ruling (§4). `ing_rough_stone` exists only in
  `DungeonExclusiveItems.cs:50`. Owner. P2.
- **WO-1377:** blocked on the save-serialisation rename ruling (§4): moving `DeNelle.Core.Web3` can be data loss
  if a serialized type name moves. Owner. P2.
- **WO-1314:** the "webview heap" root was retracted 09-03 (same failure in a desktop browser); parked with Pi
  by the 09-02 ruling. Payload reduction is still worth doing when Pi resumes. P3.
- **WO-1431:** cosmetic staff grip, tracked not queued by the owner's word. P3.

## What would close the most, fastest
1. Owner: `run-schema-repair.mjs` (unblocks NEW-A) and `ADMIN_OPS_KEY` (unblocks 1244). Two commands.
2. CLI: gate + commit the four lanes; one headless capture pass (Build, WelcomeBack, NightMarket, Manage, Raid).
3. Owner: one F8 capture casting the fireball inside the walls on build 358574+ with the new VFX trace.
4. CLI: NEW-B cloud load through `ApplyPersisted`, before any real player has a second device.
