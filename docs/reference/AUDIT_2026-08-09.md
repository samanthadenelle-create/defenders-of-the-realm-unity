# FULL CROSS-SILO AUDIT — 2026-08-09

**Status: KNOWN DICTIONARY (durable registry, not a one-off report).** Per memory
`audit-outputs-as-known-dictionaries`, audit answers are stored as canonical source-cited registries.
Every row carries a `file:line` and a coverage verdict. **No row was accepted without evidence.**

**Method:** SUNDAY_HOUSEKEEPING steps 3 + 4 (full silo audit + regression-coverage proof) — the two
steps the 2026-08-02 sweep skipped. Twelve read-only auditors, one per silo, run in parallel, gate-free.
Anchor at time of audit: `CANON_GROUND_TRUTH_2026-08-09.md`, HEAD `c8320434`, pushed, 0/0.

**Gates at time of audit** (read off the markers, never off this doc):
`COMPILE_GATE_OK` (`Builds/gate-ship3.log`, 08-08 19:36) · `REGRESSION_OK 130/130 suites`
(`Builds/regression-ship3.log`, 19:38) · `UI_CAPTURE_OK 44` (`Builds/ui-capture-ship.log`, 14:30).
⚠ **Read §0 before trusting any of those.**

---

## 0. ⛔ HOW MUCH A GREEN GATE IS WORTH — read this first

The gate that certifies everything else was itself audited. Its verdict governs how you read the rest
of this document.

| # | Finding | Evidence |
|---|---|---|
| **G1** | **The regression denominator is self-reported and can SILENTLY SHRINK.** 78 of 130 suites are registered inside `Guard.Try(...)` with the return value discarded (`Guard.cs:31-41` swallows and returns false). A suite that THROWS appends no `[tag]` line and adds no failure — it simply leaves the total. The marker would read `REGRESSION_OK 125/125 suites` and still be green. **There is no pinned expected count anywhere.** | `DataRegression.cs:385-608`, `:613-615`, `:637` |
| **G2** | **`checkin_gate.ps1` has no log-freshness check and ignores the wrapper's exit code** on its two heaviest stages. Stage 3 judges purely by `Select-String` on `Builds\data-regression.log`; stage 4 identical. Only stage 2 reads `$rc`. | `checkin_gate.ps1:162`, `:189-194`, `:219-222` |
| **G3** | **A missing `python` on PATH marks stage 1 FAIL and silently SKIPS the entire regression suite** (stages 3-6 gate on `$compileOk`). | `checkin_gate.ps1:139-142`, `:150`, `:154`, `:166` |
| **G4** | **PlayMode tests have NEVER produced an artifact.** No `tests-PlayMode.xml` exists anywhere, ever. The 6 `[UnityTest]` PlayMode tests are entirely unevidenced. Newest EditMode artifact (`Builds/test-results-EditMode.xml`, 930/930) is **four days older** than the ship3 gate run. | `Assets/Tests/PlayMode/*` |
| **G5** | **The hollow-pass ratchet cannot see the shape actually in use.** `FindHollowPassLines` fires only on `== null` / `IsNullOrEmpty` / `!File.Exists` / `!Directory.Exists` in a 4-line window; the live pattern is a **bool helper** (`if (!InstallState(...)) { reason = "needs fleet"; return true; }`). Four suites hollow-pass unbaselined and undetected while the suite reports "6 baselined / 0 new" — a false all-clear. | `RegressionMarkerRegression.cs:480-486` vs `PackGrantRegression.cs:80-81`, `FoundingReachabilityRegression.cs:68-69`, `BuildingUpgradeAuthorityRegression.cs:56-57`, `PackCosmeticIntegrityRegression.cs:90-91` |
| **G6** | `HudUiRegression` **catches its own exception and returns `true`** — a broken oracle reports green by design. | `HudUiRegression.cs:228-234` |
| **G7** | RULE 3 (gate-grep ownership) scans only `tools/` and `.claude/skills`. Four root-level scripts grep `_OK` markers and are outside the scan: `run-tests.ps1`, `overnight-apk-build.ps1`, `overnight-webgl-deploy.ps1`, `webgl-vercel-overnight.ps1`. | `RegressionMarkerRegression.cs:89` |
| **G8** | `SESSION_GUARDS_OK 6/6 checks` is a **hardcoded literal**, not derived from what ran — the exact "count is a label" failure the shaped `REGRESSION_OK` marker exists to kill. | `SessionRegression.cs:71` vs `DataRegression.cs:637` |
| **G9** | Three suites are **near-tautologies**: they assert a substring exists in a `.cs` file **without stripping comments**, so a commented-out or dead-branch call satisfies them. | `DungeonExitReachableRegression.cs:26-48`; contrast `DungeonReturnSceneRegression.cs:31-38`, which *does* strip |
| **G10** | Stale ratchet id: the Obsidian baseline pins **`PauseHudBootstrap.cs`, annotated as a dead never-instantiated class**. It can never resolve, so it permanently inflates the "15 tracked" figure. | `UiObsidianConformanceRegression.cs:178-181` |

**VERDICT.** `REGRESSION_OK 130/130` on 2026-08-08 is **honest for the 130 suites that reported** — no
suite is unconditionally tautological, all 11 conditional hollow-passers took their real branch in that
run, marker discipline holds (three distinct emitters verified at source; only `ROOMFORGE_REGRESSION_OK`
collides as a substring), and `checkin_gate.ps1` now parses clean (0 AST errors).

**Trust it for:** catalog/data integrity, source-shape laws, pure-function math.
**Do NOT read it as** "130 suites ran" (G1), as covering PlayMode (G4), or as correctness rather than
liveness for `UI_CAPTURE_OK` / `COMPILE_GATE_OK`.

### ★★ THE PATTERN — these are not N separate defects. They are ONE defect class.

**Every gate audited asserts that a thing EXISTS. Almost none assert it is CONSUMED at the site that
needs it.** Once you see it, most of this document collapses into one shape:

| Gate | What it proves | What it does NOT |
|---|---|---|
| `HudUiRegression` CHECK 5 (safe area) | `PiSignInController` obeys the inset | that the **HUD** does — 9 zones, **0 calls** (F74) |
| CHECK 2 (UIDocument fence) | no new **`.cs`** constructs a `UIDocument` | that **no UXML ships** — 6 scenes do (§3a) |
| CHECK 3 (`RarityGlyph`) | the glyphs **render** in the shipped font | that anything **uses** them — `EquipmentPanel` doesn't (F33) |
| `UiMvvmConformance` | a View **references** a VM | that it stopped **reading state** — file-level only (`:26-29`) |
| `HudActionBarRegression` | **set membership** is right | any **geometry** — 78.66 px never measured (F15) |
| `UI_CAPTURE_OK 44` | 15 **panels** rendered | the **HUD kit** — 0 captures, ~100% of screen time (F76) |
| `DynamicDifficultyRegression` | the **math** is correct | that anything **reads** it — 3 of 5 levers dead (F17) |
| `VfxResourceSelfContainment` | **prefabs** are self-contained | the **catalogs** — 100/110 GUIDs leak (F1/F2) |
| `DataWebRegression` version check | a `version` field **exists** and matches | that a **change bumps it** — 24 didn't (F22) |

Canon named this exact shape three days ago about a different system (the difficulty multipliers):
**"the gate proves the part that was never broken."** It is the §12 shape, and it is also the 08-06
"PATTERN OF THE NIGHT" — *a value asserted rather than derived from the artefact that owns it.*

**Consequence for remediation: the highest-leverage change is not any individual fix in this document —
it is converting these gates from EXISTENCE assertions to CONSUMPTION assertions.** `RarityGlyph` is the
cleanest proof: a font-verified, regression-pinned, ASCII, colourblind-safe rarity channel existed the
whole time and the one panel that needed it never called it. **No fix was ever required — only a gate that
asks "who consumes this?"** Fix the symptoms first and you leave the mechanism that produced them intact.

### What a headless gate STRUCTURALLY cannot see

1. **Orientation / world transform** — nothing in the 130 reads an up-axis. This is the `70a86c17` class:
   a change that laid the entire town on its side passed every headless gate.
2. **Screen-space geometry for direct `Screen.*` readers** — `Screen.*` never moves in batchmode;
   confessed at `UICaptureLaunch.cs:543-547`.
3. **Skinned-mesh pose / animation** — the run itself logs `pose-verify skipped (headless/culled …
   smrVisible=False)` for five enemy models (`regression-ship3.log:14362-14577`). Those rigs are green
   because they were **never evaluated**.
4. **Coroutine / play-mode presentation** — `[structure-targetable]` states the collapse presentation
   "is a coroutine and is skipped outside play mode by design".
5. **Runtime-state-dependent economy paths** — four suites reflect over `GameStateService`'s state seam;
   a rename turns them green-and-empty.
6. **Materials/textures resolvable only on this machine** — the entire reason `[vfx-self-contained]`
   exists. See F1/F2 below for where that gate is itself blind.
7. **Colour, contrast, legibility** beyond glyph metrics.

---

## 1. RANKED LEDGER — P0

| # | Silo | Defect | Evidence | Coverage |
|---|---|---|---|---|
| **F1** | VFX | ★ **MEASURED 2026-08-09 — the figure below was an ESTIMATE and it UNDER-STATED the exposure.** The extended oracle ran and reported the real numbers: **`HovlVfxCatalog.asset` reaches 645 assets in gitignored roots, `VFXCatalog.asset` reaches 34 — 679 references over 675 distinct assets.** The 54 prefabs are genuinely clean (the 08-06 mirror held). Now pinned as a dated ratchet, `KnownCatalogExposure`. ⚠ **The obvious remedy is WRONG:** these are largely whole pack PREFABS, not loose materials, so a blanket `VfxResourceArtMirror` run would import a deliberately-gitignored pack into git against the 07-15 big-art policy. Likely-correct fix = trim the catalogs to gameplay-reachable rows (26 of 79 enum values have zero callers) and mirror only that subset — an owner-scoped content decision. Original estimate follows: **`HovlVfxCatalog.asset`: 100 of 110 distinct GUIDs resolve into GITIGNORED art roots** (`Hovl Studio/`, `UnityTechnologies/`, `Spells Pack/`). On a fresh clone / the second machine, **every `PlayKey(...)` effect resolves null** — tower projectiles, casts, impacts, `Heal_Aura`, upgrade fireworks. | `Assets/Resources/VFX/HovlVfxCatalog.asset` (GUID join vs gitignore roots) | **UNCOVERED — see F2** |
| **F2** | VFX/gates | **`VFX_ART_MIRROR_OK` / `VFX_SELF_CONTAINED_OK` is scoped to PREFABS ONLY** — `VfxPrefabPaths()` calls `FindAssets("t:Prefab", …)`, so the two ScriptableObject catalogs are never measured. **The gate built to prevent exactly this class is green while 103 references leak.** | `VfxResourceSelfContainmentRegression.cs:154-166` | the suite IS the defect |
| **F3** | UI | **Two ENABLED `UIDocument`s ship in `Dungeon_HealersCottage.unity`** (build index 4) — `DungeonHud.uxml` + `CraftingPanel.uxml`, both `m_Enabled: 1`, both binding by UXML element name. UXML does not work in builds: blank dungeon HUD, dead crafting panel, in the richest dungeon in the game. | `Dungeon_HealersCottage.unity:185`, `:200-201`, `:50053`, `:50070-50071`; `DungeonHudController.cs:109-113`; `CraftingPanelController.cs:124-132` | **UNCOVERED** — `HudUiRegression.cs:335` CHECK 2 is a **source fence over `.cs`; it never opens a `.unity`** |
| | | ⚠ **FIX GUIDANCE — "disable the UIDocument" is NOT automatically the right fix, and this is NOT a text edit.** Hand-editing serialized `.unity` YAML to flip `m_Enabled` is the resave-corruption path (CLAUDE.md §3) — this is Editor work or a code-side disable. For `ATBBattle` the fix is probably **delete** the dead document and confirm `BattleHudUgui` is wired, not disable-and-ship-a-dead-object. **For the two dungeon documents no uGUI replacement was found, so disabling them removes the crafting panel and dungeon HUD entirely** — someone must open those scenes and look. The correct in-repo pattern for a code-side disable is `TitleController.cs:145-162` (`Guard.Try` + a FlowTrace line). | | |
| **F4** | UI | `BattleHUD.uxml` is the live source of an enabled `UIDocument` in shipped `ATBBattle.unity`, though `BattleHudUgui.cs` is its uGUI replacement and nothing disables it. | `ATBBattle.unity:430`, `:437`; contrast `TitleController.cs:145-162` | UNCOVERED |
| **F5** | Web | **`api/promo/redeem.js` grants rewards on a CLIENT-SUPPLIED `playerId`** — no auth, no CORS, no rate limit. Enumerate codes, farm crystals/coins against any id incl. wallet ids. | `api/promo/redeem.js:32-54`, `:49` | UNCOVERED |
| **F6** | Web | **`api/referral/claim.js`** — unauth POST, client-chosen `claimerId`, grants 25 crystals **and burns the victim's one-claim-ever slot** (permanent griefing of a real player's bonus). | `api/referral/claim.js:55`, `:86-91`, `:103-113` | UNCOVERED |
| **F7** | Web | **Four wallet-gated endpoints re-introduce the `module.exports.config` ordering bug** that `_lib/http.js:145-155` documents as fatal: `config` discarded → bodyParser stays ON → `readBody()`'s `'end'` never fires on a spent stream ⇒ **hang to function timeout on every call** (DoS + billing burn), auth gate unreachable. | `leaderboard/submit.js:40`; `profile/username.js:35`; `profile/social.js:34`; `referral/install-brag.js:38`. Correct order only at `game/save.js:427-432` | UNCOVERED |
| **F8** | Economy | **WO-931 CONFIRMED at source.** `StubWalletProvider` has no `#if` guard, compiles into every player; `WalletService` auto-selects it on release desktop/WebGL and Android without SDK. Buy → fake Connect → mock 2000 SKR → fabricated 88-char base58 → `ApplyPackContents` grants the pack for ZERO payment and fires `purchase_completed` with the fake txSig. | `StubWalletProvider.cs:48`, `:68-73`, `:146-172`, `:210`; `WalletService.cs:360-383`; `PackStore.cs:512-530` | `WalletProviderSelectionRegression.cs:191-211` pins the **default only** — does not close the hole |
| **F9** | Economy | **The flag defense is DEFEASIBLE PER-DEVICE.** `FeatureFlags.Get` reads **PlayerPrefs first**, so any device that ever stored `ff.realmstorepurchase=1` keeps a live Buy rail regardless of the shipped default. The store needs no scene authoring — `PackStoreBootstrap` spawns it via `AddComponent` on demand. | `FeatureFlags.cs:613`; `PackStore.cs:377-379`; `PackStoreBootstrap.cs:79` | UNCOVERED |
| **F10** | Economy | **`WalletService.PayFlat` is gated by NOTHING** — no `FeatureFlags` reference in the method; straight to `_provider.SendPayment` (the stub's fake success); caller grants Glimmer off the fabricated signature. Dead **only** by scene-absence (guid has 0 references, no `RuntimeInitializeOnLoadMethod`) — one `AddComponent` from live. | `WalletService.cs:585-620`; `CryptoPaymentManager.cs:186`, `:209-226` | **UNCOVERED** — `PayFlat` appears in no test or oracle anywhere |
| **F11** | VFX | **WO-929 ROOT CAUSE.** `VFXManager.ReturnToPool` re-parents **unconditionally**; its own twin `VFXManager.Hovl.cs:397-398` **has** the `activeInHierarchy` guard. One guarded, one not — same file family. | `VFXManager.cs:900` vs `VFXManager.Hovl.cs:397-398` | **UNCOVERED** (`VfxLoopFlagRegression` has no `SetParent`/`activeInHierarchy` assertion) |
| **F12** | VFX | Firing site proven **from a BUILDING**, not just a pooled enemy: `HarvestAura.OnDisable` → `StopHeld(immediate:true)` → `VFXHandle.Stop(true)` → `ReturnToPool`, aura parented at `PlayAura(type, transform)`. Second site: `SupportFieldStructure.OnDisable` → `StopField(immediate:true)`. **WO-929 must not be scoped to the pooled-enemy path.** | `HarvestAura.cs:228` → `:393-399` → `VFXHandle.cs:120`, seat `:361`; `SupportFieldStructure.cs:253`, `:461`; capture `logs/f8-inbox/capture-20260808-131709.md:8` | UNCOVERED |

---

## 2. RANKED LEDGER — P1

| # | Silo | Defect | Evidence | Coverage |
|---|---|---|---|---|
| **F13** | Core + Hero | **`equippedRingId`/`equippedAmuletId` are the ONLY 2 of 82 `PersistedState` fields absent from `Snapshot()`.** Declared and migrator-seeded; never snapshotted, never applied. Accessory equip lives in PlayerPrefs only → does not survive export/import, backend sync, or a new device. *(Found independently by two silos. This is canon's CS-1 ticket, now located.)* | `SaveSchema.cs:436,442`; `SaveMigrator.cs:403-404`; absent from `GameStateService.cs:411-504`/`:505+`; real authority `GearLoadout.cs:739,747,957,962` | `CoreSaveRegression.cs:282-283` proves the migrate **seed** only; `SaveLoadRoundTripTest.cs:227-235` documents the exclusion as deliberate |
| **F14** | Core | **`OutpostCaves` XML says the resolver "DOES NOT EXIST YET, so this ships OFF" — actual `defaultOn: true`, with no trailing correction.** A live gate pointed at unbuilt code. Worst of the flag-lie class, because every other lie is recoverable from a `//`. | `FeatureFlags.cs:433-437` vs `:438` | UNCOVERED |
| **F15** | UI | **Action-bar face is 78.66 ref px wide against `MinTouchPx` 112 — 33.3 px UNDER floor.** Measured: `ActionBar` zone `(0.270,0.015)→(0.730,0.150)` on 1080×1920 = 496.8 px; `BarSlotW = (1−0.01×5)/6`. **NOT a `ClampMinTouch` overlap** — `ApplyActionBar` re-zeroes `offsetMin/offsetMax` every repack, wiping the guard. | `HudKitController.cs:121-122`, `:1412-1414`; `HudAreasHost.cs:104`; floor `ElarionUiKit.cs:318` | **UNCOVERED** — `HudActionBarRegression` asserts set membership/count only; `HudUiRegression:585` measures safe-area corners, never bar faces |
| | | ⚠ **NOT A TASK — A DECISION. The 6-face bar CANNOT satisfy the 112 px floor inside its current zone; every lever is blocked.** (a) Gap → 0 gives 496.8/6 = **82.8 px**, still 29 under — *mathematically impossible*. (b) Reaching 112 px/face needs ≥707 px = 0.655 of screen; symmetric expansion lands `0.1725→0.8275`, overrunning `MoveCluster` (`0.010→0.270`, the movement joystick) by 0.0975 and `ActionRail` (`0.780→0.995`) by 0.0475 — *requires relocating two other zones*. (c) 5 faces = 95.4 px, still under; you must drop to **4** (120.5 px) to clear — *contradicts the 6-face ruling*. **The bottom band is over-subscribed.** ⚠ **And WO-911 IMPROVED this**: at the old 7 faces they were 66.7 px, so 7→6 raised them 12 px. **Pre-existing debt partially mitigated on the way past — EXISTING, not recently broken; urgency lowers accordingly.** Physical scale: 112 ref px ≈ 7.1 mm at 400 dpi (matches Apple 44 pt / Google 48 dp); these faces land at **~5.0 mm**, in a row of six adjacent targets where horizontal precision is exactly what is demanded. **Recommendation: defer the relayout, but pin the MEASUREMENT in an oracle now so the number cannot silently get worse.** | | |
| **F74** | UI | **ALL NINE HUD ZONES IGNORE THE DEVICE SAFE AREA.** `SafeAreaInset` — the shared `Screen.safeArea` helper from WO-868 — has **exactly ONE caller in the entire codebase**: `PiSignInController.cs:335`. Neither `HudAreasHost` nor `HudKitController` references it at all. On any phone with a cutout or rounded corners, `Vitals`/`System` (top edge 0.985) and `ActionBar` (bottom edge 0.015) sit **outside** the safe area. Ships to Seeker/Android — this affects every screen on every notched device. **Compounds F15**: the action bar is both under the touch floor AND unclipped at the bottom edge. | `Assets/_Modules/Core/UI/SafeAreaInset.cs:48`; sole caller `PiSignInController.cs:335`; zones `HudAreasHost.cs:99-105` | **UNCOVERED** — `HudUiRegression.cs:585` asserts safe-area corner *buttons* meet the touch floor but **never checks that the HUD zones respect the inset** |
| | | ⚠ **F74 IS TWO CLAIMS. Only one is provable, and a RESULT file must not conflate them.** **(a) "The HUD zones make no safe-area call" — PROVEN at source**, and verified further: there is **NO alternate implementation** — no `Screen.safeArea`, no cutout/notch handling anywhere under `Assets/_Modules/HUD/`. This is data/logic-decidable, so it is legitimate headless `DataRegression` territory (`INSTRUMENTATION_STANDARD.md:198-200`) and an oracle for it is **executable today, no device needed**. **(b) "…therefore they render under the gesture bar" — UNPROVEN.** Rendering class; needs eyes (`HANDOVER.md:93-97`). **Write the fix against (a); do not let anything claim (b) without a device shot.** | | |
| | | ⚠ **FIX-PATH CORRECTION — the obvious fix is the wrong one.** `HudAreasHost.Add()` (`:127-133`) writes `anchorMin`/`anchorMax` as **pure fractions** and then zeroes `offsetMin`/`offsetMax`. **Safe-area insets are PIXELS, not fractions**, so this can NOT be done by tweaking the fraction pairs at the nine `Add(...)` call sites (`:95-117`) — that yields nine wrong constants that are also resolution-dependent. The inset must be applied **after layout resolution**, or the zone rects converted to offset-based. Know this before starting. ⚠ Also note `ClampMinTouch` writes through the same `offsetMin`/`offsetMax` channel and `ApplyActionBar` re-zeroes it every repack (see F15) — **whatever applies the inset must not be wiped by that repack.** | | |
| | | ⚠ **AND THERE IS NO APPLIER TO CALL — one must be written.** `SafeAreaInset` has exactly ONE applier, `ApplyTopRight` (`:112`), which hard-sets `anchorMin = anchorMax = pivot = (1,1)` — it **corner-pins a fixed-size box**. Calling it on a stretch-anchored fraction rect would **collapse the zone**. What DOES exist and should be reused is the pure static math — `LeftInset`/`RightInset`/`TopInset`/`BottomInset` (`:63-74`) — which is **headlessly assertable with no live `Screen`** and is already the basis of the CHECK 5 oracle. The work is: add a stretch-mode applier that KEEPS the fraction anchors and writes `offsetMin`/`offsetMax` from those four statics, converted to canvas units via the parent Canvas `scaleFactor` (the conversion `FitTopRightNow` already does at `:132-134`), then call it on the mounts; reuse the existing `SafeAreaCornerWatcher` pattern (`:142-174`) for rotation/resolution refit rather than writing a second watcher. ⚠ **`EdgeMarginPx = 44f` (`:58`) is authored as a DEVICE-pixel CORNER margin justified from the 2670x1200 Seeker capture — whether the same 44 px is correct as a full-bleed HUD zone inset is an OWNER JUDGEMENT, not a given.** | | |
| | | ★ **F74 AND F15 ARE COUPLED — decide them together.** `EdgeMarginPx` is applied **on top of** `Screen.safeArea` (`:50-57`, `:99-101`), i.e. it is pure extra breathing room *after* the OS has already excluded the notch and gesture bar. The HUD zones also already carry their own fractional insets (ActionBar bottom 0.015, MoveCluster 0.030/0.010, System 0.995), so applying safe-area **plus** 44 px double-insets them. **Concretely: the ActionBar zone is 496.8 ref px; insetting 44 px per side leaves 408.8, which drops the face from 78.66 px to ~64.7 px — 14 px WORSE than the gap recorded in F15.** Recommended read: apply **safe-area insets only, no additional `EdgeMarginPx`,** to the stretch zones, and keep 44 for the corner-pinned chrome where it was actually measured. **If uniform breathing room is preferred instead, F15 must be re-ruled in the same breath — the two decisions cannot be made independently.** | | |
| | | ⚠ **Minor, same class:** the `FlowTrace.Step` at the end of `BuildAreas` reports **"9 area mounts"** while at least **11** `Add(...)` calls are visible in `:95-118` alone. A log string stating a count that no longer matches the code. Verify the real count before quoting it anywhere. | | |
| **F16** | Village | **Selling a singleton never resurfaces its baked twin.** `SellSelected` → `NotifyRemoved` fires while the `PlacedStructure` is still alive (`Destroy` is 21 lines later, deferred anyway), so `HasPlacedInstance` still true → `EnforceInternal` takes stand-down instead of `ResurfaceBakedTwins` → `IsBuilt` stays true → `SingletonReleased` never fires. **WO-843 fixed exactly this for the DEATH path with `NotifyRemovedDeferred`, and that fix's doc-comment claims "parity with the sell path".** | `BuildModeController.cs:2215`, `:2236`, `:3176`; `StructureSingleton.cs:460`, `:267`, `:365`; contrast `:602-620` | **UNCOVERED** (`DestroyedStructureRegression.cs:286` covers the destroy leg only, as pure data) |
| **F17** | Enemies | **Three of five adaptive-difficulty multipliers are computed but have ZERO gameplay readers** — `EnemyCountMultiplier`, `BossHpMultiplier`, `BossDamageMultiplier`. Every boss wave ignores the softer boss curve the math file exists to produce; the count signal is dead. | producers `Core/Difficulty/DynamicDifficulty.cs:119,122,125`; only consumers are the Hp/Damage pair at `WaveManager.cs:1761-1762`, `:1876-1877` | **UNCOVERED** — `DynamicDifficultyRegression` + `DynamicDifficultyTests` call `DifficultyMath.*` (pure math), **not the runtime surface**, so they prove the part that was never broken |
| **F18** | Enemies | **`TribeManager.BuildRaiderDef` hardcodes stats contradicting `enemies.json`**: `orc-raider` hp **60** vs **130**, dmg 11 vs 12, xp 20 vs 24; `necromancer` hp **90** vs **1700**, dmg 13 vs 17, xp 28 vs 220, name "Wound Necromancer" vs "Alduin's Necromancer". | `TribeManager.cs:379`, `:383`, `:393`, `:395` | **UNCOVERED** — the divergence oracle guards `WildlandsRoster` only; TribeManager never calls it |
| **F19** | Enemies | **`WardTetherService.BuildKindleDef` is a SECOND, DIFFERENTLY-WRONG copy** of the same table — `orc-raider` hp 55/dmg 10/xp 18; `necromancer` hp 85/dmg 12/xp 26. Two hardcoded tables disagreeing with the catalog **and with each other**. | `WardTetherService.cs:487`, `:491` | UNCOVERED |
| **F20** | Hero | **The `2f10f6ac` free-paid-weapon fix does NOT cover Ranger/Mage.** `StarterLoadout` registers only `"knight"`, so `StarterOrCatalogFloor` falls through to the unguarded catalog-wide `GearCatalog.BestWeapon` — the exact pre-fix path — for both classes `ff.knightonly` just made selectable. | `GearLoadout.cs:85`, `:609-613` | UNCOVERED |
| **F21** | Hero | Armor auto-equip is still catalog-wide — the identical free-shop-item leak. Self-flagged in-code, not fixed. | `GearLoadout.cs:301`, comment `:293-300` | `AegisSetReachabilityRegression.cs:112` is explicitly "INFORMATIONAL (not a pass/fail gate)" |
| **F22** | Data | **`DataWebRegression`'s version check is presence + cross-copy agreement only — never "a change bumps it".** 24 catalogs had content changed with no version bump on their most recent commit (worst: `enemies.json` +95, `en.json` +265, `themes.json` +369, `waves.json`, `abilities.json`). | `DataWebRegression.cs:352-398` | UNCOVERED |
| **F23** | Data | **Drift + version checks iterate the StreamingAssets root ONLY**, so a **Resources-only file — the copy that WINS at runtime — is never drift- or version-checked.** `widget-params.json` has no `version` field at all and is invisible to the gate. | `DataWebRegression.cs:208`, `:356` | UNCOVERED |
| **F24** | Dungeons | **The RoomForge dual-copy gate is a hardcoded 3-file list** that includes no `dg_*` layout — **including `dg_sunken_vault.json`, the exact file that drifted on 08-08.** The next drift ships the same way. | `RoomForgeRegression.cs:162` | UNCOVERED |
| **F25** | Dungeons | **`StairwellRoom` — the room in EVERY shipping dungeon — is absent from `rooms-catalog.json`**, so the 0.25 m footprint-sampling `[room-shell]` oracle (which iterates `cat.rooms`) never samples it. Its floor/ceiling wholeness is checked only by piece-count + scale-equality: **the union-bounds class the sampler was built to replace.** | catalog has 23 room ids, no Stairwell; oracle loop `RoomForgeRegression.cs:545`, sampler `:692` | UNCOVERED (sampling); shape covered by `[stairwell-no-shaft]` |
| **F26** | Web | **Unauth + unthrottled POSTs**: `trace` (no body cap, no line cap — unbounded jsonb into prod Neon), `events/track` (**one sequential DB write per array element** — 100k events = 100k writes = guaranteed timeout + cost), `bug-report` (~420 KB b64/POST, no rate cap), `referral/generate` (row flooding + code-space exhaustion), `tower-swap/log` (**forges rows into the PAID-swap audit trail with an unverified `txSig`**). | `trace.js:21-30`, `:48-52`; `events/track.js:22-31`, `:55-88`; `bug-report.js:43-54`; `referral/generate.js:39-55`; `tower-swap/log.js:29-32`, `:46-58` | UNCOVERED |
| **F27** | Web | **There is NO oracle whatsoever for `api/**`** — no `scripts` block, no test dep, no endpoint test anywhere. Every web finding is uncovered *by construction*, which is why they accumulated silently. | `package.json`; `Assets/Tests/EditMode` | n/a |
| **F28** | Economy | **The ad covenant holds by luck, not construction.** `Walk()` compares only string **values**; on a `JProperty` it descends to `prop.Value` and never tests `prop.Name`. A reward in the sibling files' own shape — `{"economy":{"crystals":700}}` — carries `crystals` as a **key** and passes `[no-premium-grant]` clean. | `AdPlacementCovenantRegression.cs:302-327`; shape precedent `skr_store.json:86` | self-gap |
| **F29** | Economy | **SKR rail underprices the USD rail 2.876×** — Token Coffer $0.02897/SKR vs `packs.json`'s uniform $0.0833/SKR. A buyer pays $19.99 for the pack advertised at $49.99. | `skr_store.json:134-137`; `packs.json:191-196`, `:26-31`, `:140-145` | **UNCOVERED** — `MonetizationCovenantRegression.cs:93-100` loads both files but never compares price |
| **F30** | NPCs | **`DailyQuestHud` is orphaned** — spawned hidden, and TOWN ACTIONS "Quests" routes to RumorBoard. Nothing calls `Toggle()` outside editor capture. *(Orphaned by the WO-835 §3c Quests→RumorBoard split.)* | route `HudKitController.cs:1752-1755`; contract `DailyQuestHud.cs:130-131`; hidden spawn `DailyQuestHudBootstrap.cs:29` | `DailyQuestEmptyStateRegression` covers content only — UNCOVERED for reachability |
| **F31** | NPCs | **Potion crafting is unreachable** — both `PanelId.ConsumableCrafting` openers need a live `ApothecaryWorkbench`; the injector stands down unconditionally on any marker-set save; **no `apothecary` row exists in `structures-catalog.json`.** Recipes stranded. | `ConsumableUseService.cs:136-145`; `CraftingStationInjector.cs:110-117`; `consumable-recipes.json:2` | UNCOVERED |
| **F32** | NPCs | **Echo lane bonuses Crafting/Defense/Exploration have ZERO production readers** — write-only. Only reads anywhere are editor oracles. *(See §4 for the important nuance: this is dead code, NOT a lie to the player.)* | writer `EchoBonusCalculator.cs:354-357`; holder `EchoLaneBonuses.cs:44,47,50`; sole reads `EchoSpecializationRegression.cs:479` | UNCOVERED for consumer existence |
| **F33** | UI | **Colour-only meaning, both known items CONFIRMED OPEN plus three new.** Build ghost valid/invalid is hue-only at **two** sites, and `TowerPlacementSystem` **computes a `BuildRejectReason` and throws it away**. Hero health bar has **no numeric label at all** — while the XP strip beside it explicitly reasons about colourblind safety. New: item rarity hue-only; crit-vs-normal floaters gold-vs-red only. | `GhostPreview.cs:31-32`, `:206`; `TowerPlacementSystem.cs:88-89`, `:241-243`; `HudKitController.cs:1450-1453`; `ElarionUiKitNameplate.cs:158-165`, `:174-177`; `EquipmentPanel.cs:381`, `:900-909`; `BattleController.cs:909-910` | UNCOVERED |
| | | ★ **ADDITIONAL COLOUR-ONLY SITES — the dungeon `.uss` state classes, self-documented as colour.** `CraftingPanel.uss:12-13` states in its own header: *`crafting-ingredient-cell--have` (ingredient count satisfied — **green**)* / *`--need` (ingredient still short — **amber**)*. At `:91`/`:97` the only distinguishing property is `border-color` — `rgba(74,200,140,.85)` vs `rgba(224,158,74,.7)`. **Green-vs-amber on "do I have this ingredient" is a red/green axis, in a crafting screen, for a red/green colourblind owner.** Same file also carries `crafting-result-label--ready` (green) vs `--done` (gold). `DungeonHud.uss:14-15`: `oil-bar-fill--warning` (amber/red) and `--empty` (**pulses** red — the pulse is a genuine non-colour cue, so that one is partly mitigated; the warning band is not). ⚠ **If these widgets are ever rebuilt, that is the cheapest moment this will ever be to fix** — a text or shape cue costs nothing extra during a rebuild and is a separate ticket afterwards. | | |
| | | ★ **DOWNGRADE — the rarity row is NOT new-feature work. The owner's non-colour rarity channel ALREADY EXISTS, is oracle-pinned, and is ALREADY IN USE ELSEWHERE.** `ElarionUiKit.RarityGlyph(int)` (`:1793`) + `RarityGlyph(string)` (`:1806`) are public statics; `HudUiRegression.cs:427-444` pins them for tiers 0-4 (non-empty AND renderable by the shipped font) under a comment naming them *"the colorblind owner's non-color rarity channel"*; and **`HeroInventoryController.cs:413-415` already wraps and calls them.** `EquipmentPanel.cs:381` simply goes straight to `RarityTint` and never calls the glyph. **So this is an INCONSISTENCY BETWEEN TWO PANELS, not a design decision needing owner taste** — wire the existing API (PREFLIGHT_GATE item 3: reuse, do not reinvent). Read `RarityGlyph`'s actual signature before wiring; a string overload exists, so the `RarityTint` string may pass straight through. | | |
| **F34** | VFX | **`GameAudioMixer.mixer` is a Master-only stub with `m_ExposedParameters: []`** — 65 lines, one group. Every `AudioService` `SetFloat` return is discarded, so Master/SFX/UI/Voice sliders **silently do nothing**. | `GameAudioMixer.mixer:18`, `:28`; unchecked `AudioService.cs:825, 859, 1170, 1174`; checked `AudioMixerBridge.cs:111` | **UNCOVERED** (no suite greps the mixer) |
| **F35** | VFX | Oneshot cap is a **flat serialized 40 with no scene tiering**, while `VfxLoopBudget` tiers loops (24/48/32) and deliberately leaves oneshots alone. **This — not a leak — is the 40/40 cause.** | `VFXManager.cs:169` vs `:145-155`; `VfxLoopBudget.cs:89-95` | UNCOVERED |
| **F36** | VFX | Same illegal-`SetParent` shape at **six more parented-aura owners**: `EnemyAuraVFX.cs:219`, `GearAura.cs:380` & `:784`, `DragonBoss.cs:1391` & `:1688`, `HeartAuraController.cs:282`, `EchoSpiritPresentation.cs:105`. Oneshots too — `PlayOneshot` parents to a caller transform. | `VFXManager.cs:491` | UNCOVERED |
| **F37** | UI | `VillageHud.uxml` enabled in `Dungeon_Demo.unity`, which **is** in build settings. ⚠ **RE-FRAMED: this is not really a UXML defect — `Dungeon_Demo` is a GENERATED artifact that leaked into the build.** `Assets/Editor/DungeonComposer.cs:30` hardcodes `ScenePath = "Assets/Scenes/Dungeon_Demo.unity"` and **saves** there. ⚠ **Execution caveat — CORRECTED 2026-08-09, my earlier note here was WRONG.** I originally wrote that regeneration was "outside build settings, so harmless." **It is not.** `DungeonComposer.cs:121` calls `EnsureInBuildSettings(ScenePath)`, and `:294-299` appends it with `enabled: true`; the file header at `:15-16` states it plainly: *"creates + SAVES a NEW scene … **+ registers it in Build Settings**"*. **So one composer run undoes BOTH halves of the fix — the scene returns AND re-adds itself to the build.** ⚠ And it is not menu-only: there is a `[MenuItem]` at `:58`, but the header at `:18` documents `DeNelle.Editor.DungeonComposer.BuildDemo` as a **`run-unity-method` batchmode entry point**, so a script can invoke it headlessly. Mitigating: nothing in the repo calls it (every other hit is the newer `GraphDungeonComposer`, or `DungeonChainBuilder` which copied the idiom rather than calling it), and it self-describes as superseded v1 demo tooling. **The durable fix is to guard the GENERATION path** — drop the `EnsureInBuildSettings` call or write outside `Assets/Scenes` — which makes the deletion permanent without touching anything live. ⚠ **Do NOT delete `DungeonComposer.cs` as "obviously unused"** — that is exactly the reasoning that would have destroyed the `dg_stair_rig` / `dg_descent_probe` control group, which carries an explicit "⚠ DO NOT DELETE" at `DungeonMultiLevelRegression.cs:41-63`. This file has no such marker, but its removal is a separate deliberate question. The scene and the `EditorBuildSettings.asset:54` row must be removed **together** or Unity keeps a broken entry. | `Dungeon_Demo.unity:1692`, `:1699`; `EditorBuildSettings.asset:54`; `DungeonComposer.cs:30` | UNCOVERED |
| | | ⚠ **SEQUENCING for all scene work (F3/F4/F37): scene surgery needs the Unity Editor OPEN; gates and builds require it CLOSED (`START_HERE.md:32-33`). These cannot run in one breath — Editor session first, close, then gate.** | | |
| **F38** | VFX | `VFXCatalog.asset` still reaches 3 gitignored Spells-Pack prefabs (`Buff_Nature`, `Explosion_Arcane`, `Explosion_Fire`) — same F2 blind spot. | `Assets/Resources/VFX/VFXCatalog.asset` | UNCOVERED |
| **F73** | Economy | **`TowerSwapMenu` is a REAL-MONEY USDC rail with ZERO `FeatureFlags` references** — `Cost: {_cost:F1} USDC` (`:246`), a USDC/SCR currency picker (`:440`), a confirm CTA (`:321`). It is gated by **nothing** — not `RealmStorePurchase`, not anything. Dead **only** by reachability: `TowerSwapService.cs:54` needs a scene-wired `_menu`, and its guid `7307a9b4…` appears in no scene under `Assets/Scenes/`. **Same shape as F10 — a money surface whose only defense is that nobody wired it up.** Gate it while it is cheap. | `TowerSwapMenu.cs:246`, `:321`, `:440`; `TowerSwapService.cs:54` | **UNCOVERED** |
| **F39** | Fleet | **`AutoPilotDriver` has NO raid probe.** ~28 `RunPhase("Assert…")` gameplay phases across 7,313 lines, not one is a raid; the only raid touch is a **screenshot** of the selection screen. The raid loop has zero headless assertion coverage. | `AutoPilotDriver.cs:6255-6265` | UNCOVERED |

---

## 3. RANKED LEDGER — P2 / P3 (condensed)

| # | Silo | Defect | Evidence |
|---|---|---|---|
| F40 | Village | **Free-build → sell arbitrage**: `RefundCostFor` re-derives from `CostFor` and never consults `FreeBuildsUsed`, so a zero-cost founding placement sells for 50% of the catalog basket (~4 baskets from nothing on a v32 save). | `BuildModeController.cs:2703`, `:2726-2732`, `:2209` |
| F41 | Village | **Ambiguous `Resources.Load`: exactly 3 ids** carry both a `.fbx` and a same-stem `.prefab` — `Tower_Wooden_Watchtower`, `_L2`, `_L3` = **the entire `tower_ground_archer` ladder**, the one row needing `preservePrefabRotation`. Winner is resolution-order dependent (undefined by Unity contract). If the `.fbx` wins, the baked −90 and materials are gone — **the on-its-side + oversized + white symptom `bb6dc010`/`70a86c17` chased.** Latent today (prefab wins). **Likely the real root under WO-928 defects B and C.** | dup files under `Assets/Resources/Structures/`; load site `VisualFactory.cs:150`. `DataRegression.cs:1273` asserts **non-null only, never asset identity** |
| F42 | Village | Sell mid-build discards the v37 paid basket (`CancelJob`, then a re-derived 50%) — `BuildJobData.Paid` is dead on the sell path. | `BuildModeController.cs:2223` vs `BuildTimerService.cs:1068-1108` |
| F43 | Village | Singleton truth query is **case-inconsistent** — BaseLayout leg uses ordinal `==`, legs 2-4 use `OrdinalIgnoreCase` → a case-drifted `itemId` reads unbuilt and the twin resurfaces beside the placed structure. | `StructureSingleton.cs:122`, `:458` vs `:132`, `:138`, `:461` |
| F44 | Dungeons | **The bake reachability probe is a single `placedOrder[0] → placedOrder[last]` path and is LOG-ONLY** — `PathPartial` prints a diagnostic and the scene saves anyway. No per-descent probe, no abort. Matters because reachability is gated by the **first** failure on the path, not the average. | `DungeonBaker.cs:432-445`, `:457-479`, `:490-494` |
| F45 | Dungeons | Exit arch is opaque `URP/Unlit` primitives (WO-1008 fixed only `Beacon_Beam`'s opacity); chest + breakables are acknowledged placeholder Unlit cubes. | `DungeonExitInteractable.cs:205-208`, `:306`, `:317`; `DungeonTreasureCache.cs:381-388`; `BreakableContainer.cs:137` |
| F46 | Enemies | Three rosterIds spawned by raid/ward code have **no `enemies.json` entry at all** — `caveman`, `feral-wolf`, `tiefling-cultist`. No catalog row for a designer to tune. | `TribeManager.cs:380-382`; `WardTetherService.cs:488-490` |
| F47 | Enemies | `WildlandsRoster`'s fallback — whose comment claims "IDENTICAL numbers … so a missing catalog can NEVER reintroduce the divergence" — **diverges**: `XpReward = 22` vs catalog `24`. A catalog read failure silently underpays every Wildlands kill. | `WildlandsRoster.cs:151` |
| F48 | Enemies | **Wave can self-complete before its enemies spawn** — clear test is a bare `_liveEnemies.Count == 0` with no spawn-in-flight guard, while `SpawnBatch` awaits `batch.Delay` before adding. Masked only because smart composition is ON and `enemies[]` is empty. | clear `WaveManager.cs:2325-2328`; delay `:1993-1994`; no pending counter exists |
| F49 | Enemies | **Status effects survive pooling** — `EnemyDamageable`'s `CombatStatusTracker` is cleared by no pool path; a long Freeze/Slow/Burn leaks into the body's next life. Acknowledged in-code and deferred. | `EnemyDamageable.cs:71`, `:142`; `Enemy.cs:2554-2559`. `EnemyPoolResetRegression` sweeps `Enemy`/`EnemyBrain` fields only — **`EnemyDamageable` is not in its sweep** |
| F50 | Hero | `HealAmountMultiplier` ignores `effect.ability` — `SumModifyHeal` matches on `stat=="heal"` alone, so `ranger.t3n1` (authored for `ranger.healing-shot`) buffs **every** heal the class casts. | `HeroTalentModifiers.cs:549-560` vs `hero-talents.json:71` |
| F51 | Hero | 6 talent-unlocked abilities have **null `vfxCast` AND `vfxImpact`** — silent, invisible casts at 90-600 damage. Header admits the fields are deliberately held pending an owner VFX tag. | `abilities.json:61,63,68,70,72,73,74` |
| F52 | Hero | Wrong-family VFX: `ranger.r` (AoE ult) plays `RangerTowerUpgraded_Projectile` as its **cast** burst; `ranger.w` + `mage.frost-nova` use `Freezing_Projectile` in the cast slot. Keys resolve, `IsLoop: 0`, so no leak — but the cast beat is a tower-projectile trail. | `abilities.json:32`; `HovlVfxCatalog.asset:870-876` |
| F53 | VFX | **Double-return → double-enqueue of the same instance.** `ReturnToPool` enqueues with no containment check, and the deadline sweep removes the slot *before* calling it, so a late `ReturnAfterSeconds` lands one GameObject in the pool twice and `Acquire` hands it to two live users. | `VFXManager.cs:903-905`, `:1005-1010`, `:873-877` |
| F54 | VFX | Nothing reclaims at `timeScale == 0` — both `Time.time` and `WaitForSeconds` freeze, so oneshot slots are held for the whole pause. | `VFXManager.cs:994-1014` |
| F55 | NPCs | **9 dead `tut_*` dialogue rows** with no entry point in code or `tutorial-steps.json` and no internal goto (stranded by the Tutorial V2 rewrite). | `dialogues.json:926,941,964,983,1056,1075,1090,1105,1124`. `DialogueRegression.cs:249` checks orphan **nodes inside** a dialogue, not orphan **rows** |
| F56 | NPCs | **`TutorialDirector`'s welcome beat is dead** — plays `"WelcomeToElarion"`, an id absent from `dialogues.json`; the `NodeExists` guard silently no-ops it forever. | `TutorialDirector.cs:424-429` |
| F57 | NPCs | **Market has THREE live names in front of the player**: "Store" (nameplate), "Market Stalls" (vendor shopfront, also hardcoded), "Marketplace" (dialogue affiliation). | `canon-strings.json:101`; `vendors.json:18-19`; `dialogues.json:41`; `ConsumableUseService.cs:150` |
| F58 | NPCs | Latent `[[missing:*]]`: `buildingDesc.market` / `buildingDesc.jeweler` absent from `en.json`. Not player-reachable today (`BuildingDescription` has zero callers), but **`DataRegression.cs:1693` only asserts displayName non-empty, never that the key RESOLVES — the exact hole that let `763d1a60` ship.** | `VillageStrings.cs:81-84`, `:132` |
| F59 | Core | Reflection bridge **fails silently** — `FindHeroLocomotion`/`FindHeroLocomotionOn` return null with no trace when the type resolves null. Contrast `PersistenceBridge`, which `Warn`s on missing type and `Fail`s on missing member. | `SceneRouter.cs:384`, `:397` vs `PersistenceBridge.cs:175-181`, `:197-202` |
| F60 | Core | More flag-doc lies (truth recoverable from a trailing `//`, but the summary is wrong): `WebTrace` "Default OFF" vs `defaultOn:true`; `PartyShop` "Default OFF" vs true; `ARENA = ON — full loop verified` vs `defaultOn:false`; `CombatHud611` self-contradicts inside one summary. | `FeatureFlags.cs:125/130`, `:145/148`, `:22-23/33`, `:668/670-673` |
| F61 | Data | **5 dead catalogs — zero readers anywhere.** `ad-creatives.json` has zero mentions in any `.cs`; `towers.json`, `heart.json`, `enemy-roles.json`, `audio-mix.json` appear only inside `DataWebRegression`'s own pin list. **The gate is dual-copying and mirroring four catalogs nothing loads.** | `DataWebRegression.cs:121-129` |
| F62 | Economy | **8 of 8 `skr_store.json` cosmetic SKUs are dangling** — none exists among the 37 ids in `cosmetics.json`. Mitigated only because the file has no runtime reader at all. | `skr_store.json:20,32,44,83-84,103-105` |
| F63 | Economy | **153 convenience tokens sold, 0 delivered** — code says so in as many words; `founders-vow` alone advertises 25+25+5+3. | `PackStoreVM.cs:126-128`; `PackCatalog.cs:63`; `packs.json:211-232` |
| F64 | Web | `profile/get.js` returns **404**, violating the 200\|400\|500 contract every other file enforces. | `api/profile/get.js:56` |
| F65 | Web | Audit IP salt is a **hardcoded constant in a git-tracked file** → `ipHash` is rainbow-tableable back to an IP by anyone with repo read. | `api/_lib/audit.js:33` |
| F66 | Web | Guest rate limiter **fails open** when `guest_rate_limit` is missing — a schema-drifted deploy silently disables the only throttle in the codebase. | `api/_lib/wallet-auth.js:378-406` |
| F67 | Web | Deploy chain writes `CHAIN_DONE` **on failure**: `build-webgl.ps1`'s throw is caught and execution continues; a **stale** `Builds/WebGL/index.html` satisfies the only guard; `vercel deploy`'s exit code is never checked. `ship-webgl.ps1:69-85` does it correctly — **the good pattern exists and was not used.** | `webgl-vercel-overnight.ps1:8-12`, `:14`, `:17-22` |
| F68 | Data | `tower_wall_wizard` (Ballista) declares `maxLevel: 3` but has **no `upgradeVisualPath`/`upgradeTexturePath`** — L2/L3 upgrades are a visual no-op. | `structures-catalog.json` entry; fallback mirrors the same gap at `CatalogBootstrap.cs:244-290` |
| F69 | Village | `s_footprintCache` has no `[RuntimeInitializeOnLoadMethod]` reset (every other static in the silo has one) → stale footprints across enter-play with domain reload disabled. | `StructureFactory.cs:637` vs `StructureSingleton.cs:91` |
| F70 | Dungeons | `SolveMate` hardcodes `yaw = 0f` on vertical sockets while its comment claims it "keeps authored child yaw" — `rot = Quaternion.Euler(0,0,0)` is absolute, so authored yaw is **wiped**. Live for the 3 legacy pair-model graphs. | `GraphDungeonComposer.cs:635`, `:643-647` |
| F71 | Fleet | The three Raid regression suites **ARE wired** into `DataRegression.RunAll` **despite their own file headers claiming "not yet wired / left to the committer."** A session will "fix" a wiring problem that does not exist. | wired `DataRegression.cs:315`, `:429`, `:481`; lying headers `RaidDeployUiRegression.cs:22-26`, `RaidArenaShapeRegression.cs:32-34` |
| F72 | Gates | 2 EditMode tests assert nothing (`Assert.Pass`); 4 more self-`Ignore`. | `EnemyLeashLogicTests.cs:98`; `QuestCompletionTests.cs:284`; `EquipVMTests.cs:154`; `GearIconCatalogTests.cs:43,56`; `MagentaGuardTests.cs:70,90` |

---

## 3a. ★ COMPLETE UXML INVENTORY — corrects F3/F4/F37, which UNDER-COUNTED

**Method (definitive, not a grep guess):** every `*.uxml.meta` GUID joined against every `Assets/Scenes/*.unity`.
**Result: 7 UXML documents across 6 scenes** — my original findings named 4 across 3. **F3/F4/F37 were an
under-count and this table supersedes them.**

| Scene | Document(s) | Status |
|---|---|---|
| `ATBBattle.unity` | `BattleHUD.uxml` | **Safe to delete** — `BattleHudUgui` is code-built at runtime (`BattleController.cs:153-155`, comment: *"Self-contained… No UIDocument / UXML"*) |
| `Dungeon_HealersCottage.unity` | `CraftingPanel.uxml` + `DungeonHud.uxml` | ⚠ **RE-FRAMED — these panels are almost certainly ALREADY DEAD in player builds.** See the box below. |
| `Dungeon_Demo.unity` | `VillageHud.uxml` | Generated artifact leaked into build settings — see F37 (regenerates via `DungeonComposer.cs:30`) |
| `Title.unity` | `TitleScreen.uxml` | Reported **disabled at runtime** by `TitleController.cs:145-162` — the proven disable pattern. **Unverified by me** |
| `HeroSelect.unity` | `HeroSelectScreen.uxml` | Reported **disabled at runtime** by `HeroSelectController.cs:185-189`. **Unverified by me** |
| `PetSelect.unity` | `PetSelectScreen.uxml` | Reported **LIVE** — builds into `rootVisualElement`. **Unverified by me** |

### ★ "KEEP HEALERS COTTAGE" PRESERVES EDITOR-ONLY FUNCTIONALITY — verified in-repo

The instinct to keep those two documents rests on "deleting removes the oil HUD and crafting panel."
**That premise does not survive contact with the repo's own comment.**

`Assets/_Modules/Onboarding/PetSelectController.cs:9-11`, verbatim:

> *"THE SCREEN is built ENTIRELY IN CODE — no UXML, no USS. Project memory: **UXML-sourced UIDocuments
> come up blank in player builds (BattleHUD / BuildMenu learned the hard way)**, so this screen constructs
> its root / header / card row / footer directly on the UIDocument's rootVisualElement with inline styles."*

That is an in-repo, first-hand statement of the failure, naming two prior victims. And
`DungeonHudController.cs:109-113` binds **exactly the way it warns against** — by UXML element name:
`_root.Q<VisualElement>(RootName)`, `hud.Q<VisualElement>(OilBarFillName)`, `hud.Q<Label>(OilTimeLabelName)`.
`CraftingPanelController.cs:124-132` is the same idiom.

`Dungeon_HealersCottage.unity` is **build index 4**, and canon calls it *"the richest dungeon in the game
(lore/mini-boss/chests/crafting)"*. So a player entering Healer's Cottage today most likely gets **no
lantern/oil HUD and a dead crafting panel** — while it works fine in the Editor, which is why nobody saw it.

**This makes "keep" the option that preserves nothing.** The three real choices are: (a) accept, knowing
those panels are dead in the player; (b) delete, losing nothing that works in a build; (c) convert to uGUI —
**the only option that yields a working panel.**

★ **AND (c) IS CHEAP — MEASURED, NOT ESTIMATED.** The conversion is a **skin swap against VMs that already
exist**: `Assets/_Modules/Dungeons/UI/` contains **`DungeonHudVM.cs` (146 lines)** and **`DungeonCraftVM.cs`
(149 lines)** sitting beside the two controllers. Total binding surface is **12 elements** —
`DungeonHudController` (188 lines, **4** `.Q<>` binds) + `CraftingPanelController` (345 lines, **8**). The
uGUI precedent is **two files away in the same folder**: `DungeonToastView.cs` and `LoreReadingModal.cs`
already build through `ElarionUiKit`, and `LoreReadingModal` is already in the capture set. So this is not
"rewrite two panels" — it is *re-skin 12 bound elements against existing VMs, using a local precedent*.
**The first framing of this decision asked "what breaks if we delete it?" when the useful question was
"what does it cost to fix?" — the second question is what surfaced the VMs.** ⚠ **Still UNPROVEN in the strict sense** — this is a strong
documentary + code-shape inference, not a device capture. **Per §12 it earns a build check, not a
conclusion.** One player-build entry into Healer's Cottage settles it.

### ⚠ AND THE FENCE DOES NOT MERELY MISS THEM — IT SANCTIONS THEM

⚠ **CORRECTED 2026-08-09 (regression-plan pass): the range is `:99-123` and it permits 22 surfaces, not 12.**
I read only the first dozen entries and under-counted by ten.

⚠ **AND IT BANS THE WRONG THING — this changes what the scene fence must assert.** The fence matches
UIDocument **CONSTRUCTION** (`UiDocumentSmells`, `HudUiRegression.cs:195-200`), never UXML **SOURCING**.
So it structurally cannot tell the SAFE shape (a controller that builds inline into `rootVisualElement`,
as `PetSelectController.cs:9-11` deliberately does) from the BROKEN one (binding by UXML element name,
as `DungeonHudController.cs:109-113` does). **A scene-level fence must therefore key on a non-zero
`sourceAsset`, not on the presence of a UIDocument** — otherwise it will red-flag the very pattern that
was adopted to work around the bug.

`HudUiRegression.cs:99-123` `UiDocumentBaseline` is a **`.cs`-level allowlist that already explicitly
permits 22 UIDocument-using surfaces**, including **both** HealersCottage controllers
(`CraftingPanelController.cs`, `DungeonHudController.cs`) and `PetSelectController.cs`, plus
`WalletConnectDialog`, `JupiterSwapBootstrap`/`Service`, `SplashLoading`, `DevPanelController`,
`DevBootstrap`, `AdminOverlay`, `MusicToggleBootstrap`, `BuildStructureInfoPanel`.

**So this is not an oversight the gate failed to catch — it is a baseline that SANCTIONS a pattern canon
says does not work in builds.** That is a materially different (and worse) finding than "the fence is
source-only", and it means the scene-scanning fence is a **second, independent axis**, not a fix to the
first.

⚠ **A naive scene scan goes RED on four scenes**, and the cheap reaction — blanket-allowlisting all four —
guts the gate. The gate's whole value is distinguishing **"serialized but killed at runtime"**
(Title, HeroSelect) from **"serialized and LIVE"** (PetSelect, HealersCottage). Recommended shape: fail
only where an enabled UIDocument has a non-zero `sourceAsset` **AND** its owning controller has no
runtime-disable call; allowlist exactly two, each with a falsifiable reason.

---

## 3b. ⚠ EVIDENCE CLASS — what this audit did and did NOT prove

**Self-assessed against CLAUDE.md §12: "Static code-reading LOCATES candidates; it NEVER CONCLUDES the
cause."** This audit is overwhelmingly static reading. Its findings are NOT all of equal standing, and
treating them as one tier would repeat the failure the §12 gate exists to stop.

| Class | Standing | Which findings |
|---|---|---|
| **Serialized fact** — read directly out of a scene/asset/JSON on disk | **Sturdiest.** Not behaviour, not inference. | F3, F4, F37 (UIDocument rows in `.unity`), F41 (duplicate files on disk), the dual-copy sweep, F61 |
| **Structural fact** — a grep/GUID sweep proving presence or absence | **Sturdy.** Absence is provable. | F1, F2, F10, F27, F32, F39, F73, F74 (sole-caller sweeps) |
| **Arithmetic over authored constants** | **Sound as arithmetic**, unproven as rendering. | F15 (78.66 px) |
| **Behavioural inference from source** | **CANDIDATE ONLY — not an RCA.** | F16, F17, F40, F48, F49, F53 and most P2/P3 rows |
| **Pixel-verified** | Proven by looking. | **F75 only** |

⚠ **No finding in this document except F75 carries a captured proving line.** Per
`TICKET_PIPELINE.md:73-78` an RCA is not done until the proving line is quotable. **Treat this document
as a ranked CANDIDATE LIST that tells you where to instrument — not as a set of diagnosed root causes.**
Anything in the rendering/physics/input class (F15, F33, F74) is explicitly out of reach of static
reading and of the headless gate alike, per `INSTRUMENTATION_STANDARD.md:198-202`.

### ⚠ AND THE CAPTURE SET CANNOT SETTLE THEM — a coverage hole

`UI_CAPTURE_OK 44` / 47 PNGs in `Builds/ui-capture/` (08-08 14:30) are **ALL MODAL PANELS**. There is
**no capture of the town HUD, the action bar, or any persistent HUD zone** — i.e. the thing on screen
every second of play is never captured, at any resolution. So the pixels that would confirm or kill
**F15 and F74 do not exist**, and cannot be obtained without a new capture target or a device shot.

| # | Silo | Defect | Evidence | Coverage |
|---|---|---|---|---|
| **F75** | UI | **`DailyQuestHud` renders a MIS-ANCHORED body panel — found by opening the PNG, invisible to the green marker.** The parchment body ends partway down the frame; the band beneath it is **split** — black across the left half, and a tan panel running from the horizontal centre to the right frame edge, clipped by the border. The Close button straddles the seam. Something is anchored to the right half instead of the full content width. **Cause NOT yet diagnosed — this is a symptom, and per §12 it earns instrumentation, not a guess.** | `Builds/ui-capture/DailyQuestHud_2670x1200.png` (all three resolutions show it) | **UNCOVERED** — `DailyQuestEmptyStateRegression` asserts empty-state *content*, never layout; `UI_CAPTURE_OK` proves the panel rendered, never that it looks right |
| **F77** | Process | **THE F8 TRIAGE LOOP HAS BEEN UNATTENDED FOR 3 DAYS AND 128 SEQUENCES.** `ACK.json` `lastAckSeq: 2120`, acked **2026-08-06T15:43:35Z**; `PING.json` is at seq **2248**, fired **2026-08-08T18:17:10Z**. The inbox holds **1,574 capture files**. CLAUDE.md §14 exists so "the owner is NEVER the bug detector" and so every flag surfaces on the CLI **without the owner asking** — that guarantee has not been held since 08-06. **This is the mechanism that is supposed to catch everything else in this document before the owner sees it.** | `logs/f8-inbox/ACK.json`, `PING.json`; 1,574 × `capture-*.md` | **UNCOVERED** — nothing asserts ack-freshness or alerts on a growing backlog |
| **F78** | Core | **`SaveSchema.cs:11` header comment says `CurrentVersion = 36` while the const 25 lines below at `:36` is `37`.** A doc-comment lying about the value it sits above — the exact `CLAUDE.md` "comments lie" class, in the single most load-bearing constant in the save system. Cheap fix; high blast radius if believed. | `SaveSchema.cs:11` vs `:36` | **UNCOVERED** — `CoreSaveContractRegression` pins the version **triple**, never the header prose |
| **F76** | Gates | **The UI capture set covers modal panels only — zero persistent-HUD targets.** The HUD is the highest-exposure surface in the game and has never been screenshot-verified at any resolution. | `Builds/ui-capture/*.png` (47 files, all panels) | **UNCOVERED** |

---

## 4. ★ REFUTED AT SOURCE — stop carrying these

Each of these was a standing belief in canon or a WO. All are **closed**; re-deriving them wastes a session.

| Belief | What killed it |
|---|---|
| **"THE SEAM: nothing can damage a wall, gate or enemy tower"** — canon's "prerequisite under BOTH raid roadmaps" | WO-853 dual-implemented both interfaces AND widened the troop mask on **both** entry points. `WallSegment.cs:53`, `Gate.cs:67`, `DefenseTower.cs:57`, `RaidSpire.cs:61`; `TroopController.cs:189`, `:201-202`, `:394`. Covered by `TowerWallLosRegression`, `StructureTargetableRegression:440`, `DefenseTargetableRegression:136`, `RaidArenaShapeRegression:363`. **This un-parks the WO-774.0 posture ruling.** |
| **"Adaptive difficulty is INERT — WaveManager records none of the six fields"** | All six `EncounterSample` fields ARE measured and recorded — `WaveManager.cs:2471-2484`, armed `:2341`, consumed `:1761-1762`, `:1876-1877`. The real defect is narrower and different — see **F17**. |
| **"Orphan third gear catalog at `Assets/Data/Canonical/` is live and still being fed"** | Directory **does not exist** — deleted in `c55a5561`. No `.cs` references it; all gear tooling writes StreamingAssets. Could not shadow the pair anyway (`LocalJsonCatalogSource.cs:33-52`). |
| **"`CatalogBootstrap.RegisterFallback`'s three rows are drifted / ArcaneSpire renders pure white"** | All three now field-equal, verified field-by-field incl. `visualTexturePath: Structures/ArcaneSpire_Albedo`. Guarded by `BuildEconomyRegression.cs:1191-1290` `[fallback-parity]`. |
| **"Dual-copy is drifting"** | Full sweep, 80 files/side: of 77 paired files **only `weapons.json` and `armor.json` drift, and both are the deliberate owner gear ruling.** The 08-08 `dg_sunken_vault` drift is fixed; all 17 dungeon layouts + graphs byte-identical. **The defect is the missing GATE (F22-F24), not current drift.** |
| **"Wood/Iron dual-wallet asymmetry: `Grant` writes both stores, `TrySpend` debits only the pool"** | `TrySpend` debits `GameState.Wood/Iron`; `GrantInternal` writes those same fields once; `_wood/_iron` is a no-GameState fallback only. `EconomyService.cs:315-347`, `:399-436`. Oracle already exists: `VillageEconomyRegression.cs:118-148`. |
| **"The Echo lane picker is ~75% placebo"** | `PickableLanes = { LaneHarvest }` only — Crafting/Defense/Exploration are **never offered to the player**. `EchoAssignments.cs:71-81`, `EchoCardVM.cs:203`. The stubs (F32) are dead code, **not a lie in the UI**. |
| **"A double-registered dialogue verb could break ALL dialogue"** | **Structurally impossible** — verbs are `case` labels in one `switch` (`DialogueCommandSink.cs:79-205`), so a duplicate is a **compile error**. One production `RegisterSink` caller. |
| **"WO-910: 31 dead talent nodes, both tier-4 capstone rows dead"** | Independent re-count: **29, not 31.** `mage.t4n2` (+0.25) and `mage.t4n3` (+0.40) carry `damageBonus`, which `HeroTalentModifiers.cs:64` sums — their primary value applies today. Live totals: **Ranger 3/20, Mage 8/20.** `ranger.t2n2`'s poison rider **does** fire. **The ratchet baseline is currently protecting two working nodes** (`TalentStrategyRegression.cs:205-207`). |
| **"The oneshot pool 40/40 is a counter leak"** | It **cannot leak monotonically** — every slot releases at `Time.time >= Deadline` regardless of coroutine or host fate (`VFXManager.cs:994-1014`). 40/40 is **genuine throughput saturation**; the fix is the cap/grace (F35), not the reclaim. |
| **"`PlaySfxAtPosition(SfxId)` is a dead no-op"** | Not a no-op — falls back to `ProceduralSfx.For(id)` at `AudioService.cs:668-672`. |
| **"`EliteVFXController` is dead three ways"** | **Partially** refuted: statics `SpawnVfxFor`/`PlayDeathShake` are live callers (`Enemy.cs:720`, `:2701`). Only the **instance** path is dead (guid in 0 prefabs/scenes). |
| **"`HeroLocomotion`'s header lies about being a pure transform"** | Already corrected — the header now states the truth (`HeroLocomotion.cs:4-8`) and matches the agent config at `:526-538`. |
| **"`no TTL cron` for trace rows"** | The 7-day TTL cron **does exist** in this repo (`vercel.json` `crons`, `api/admin/cleanup.js:47-73`, never fails open). Still true for **prod**, which runs old `api/`. |
| **"Weapons/armor attached at identity rotation"** | No identity-rotation attach on any live path; `Quaternion.identity` appears only as a pre-reseat reset or the documented native-prefab grip-at-origin contract. `EquipmentController.cs:1804`, `:2366`, `:2755-2762`. |

---

## 5. ★ THE INVERSION — do not promote `api/` to prod

Canon calls promoting `api/` to prod "the single highest-value action on the board."

**That recommendation is now inverted. Prod running OLD `api/` code is currently what is protecting you.**
F5, F6 and F7 are live in the repo copy. Promoting ships three critical auth holes and a
four-endpoint DoS. **Fix F5/F6/F7 first, then promote.**

---

## 6. COVERAGE PROOF (ritual step 4)

- **Findings: 78.** **UNCOVERED: 72.** Partially/indirectly covered: 6.
- ⚠ **Exactly ONE finding (F75) is pixel-verified. See §3b before acting on any other.**
- **Add a persistent-HUD capture target** (closes F76, and is the precondition for ever proving F15/F74).
- **Zero coverage by construction** for the entire `api/**` surface (F27) and for **PlayMode** (G4).
- **Three gate holes are themselves the highest-leverage fixes**, because each one blinds a whole class:
  **F2** (self-containment scoped to prefabs) · **F23** (drift/version checks skip Resources-only files,
  i.e. the side that WINS) · **G1** (a throwing suite silently leaves the denominator).
- **Proposed oracles, in leverage order:**
  1. Pin an **expected suite count** so G1 cannot hide a vanished suite.
  2. Extend `VfxResourceSelfContainmentRegression` from `t:Prefab` to **`t:ScriptableObject`** (closes F1/F2/F38 at once).
  3. Make `DataWebRegression` iterate the **union** of both roots, and add a **content-hash → version-bump** assertion (closes F22/F23).
  4. A **scene-level UXML fence** that opens `.unity` files in build settings (closes F3/F4/F37 — the existing fence is source-only).
  5. An **asset-identity** assertion at `DataRegression.cs:1273` (non-null is not enough — closes F41).
  6. A **`SetParent`-during-(de)activation** lint over the pooled-VFX surface (closes F11/F12/F36).
  7. A **catalog-key-RESOLVES** assertion, not merely non-empty (closes F58, the hole that let `763d1a60` ship).
  8. A **touch-band** assertion over action-bar faces (closes F15).
  9. An **`api/**`** test harness — anything at all; there is currently nothing (closes F27's whole class).

---

## 7. PROVENANCE

Twelve read-only silo auditors + one canon writer, 2026-08-09, against HEAD `c8320434`.
No agent edited code; no gate, build, or git write was run by any agent.
Where an auditor's claim contradicted the tree, **the tree won and the claim is recorded as refuted in §4.**
One agent drifted off-brief and proposed unrequested work; its recommendation was discarded and only its
evidence-backed findings (F39, F71) were kept.
