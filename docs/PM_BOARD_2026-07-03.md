# PM BOARD — 100% UI Polish Initiative — 2026-07-03

> PM deliverable (read-only on code). Source of truth: `docs/UI_COVERAGE_MATRIX_2026-07-03.md`
> (incl. its work log — conversions #2,#4,#5,#44,#46,#47,#47b,#50 are code-complete IN VERIFICATION,
> riding the next gate). Owner signs off only on bot-verified results. Update this board + the
> matrix in the same breath as each landing (§15 same-breath rule).
>
> **OWNER RULE (image pair, BINDING):** NOTHING reaches the owner without the **IMAGE PAIR** —
> the runtime screenshot (bot capture from the built game) beside its reference image (Blink
> template / design source) — per surface, every time. Every row's DONE definition includes
> "image pair produced". The pair is the sign-off artifact; text claims never reach her.
> A surface with NO capture route cannot produce a pair and cannot close — see §2.1.

Legend: **S1–S6** = in-flight silos · **NW-n** = next-wave assignment defined below ·
**ORCH** = orchestrator job · **PO** = owner decision/review · **DONE-V** = code-complete, awaiting
the combined verification cycle · **TERM** = terminal state already reached.

---

## 1. FULL JOB BOARD — every matrix row accounted for

### A. PanelRouter panels (rows 1–13)

| Row | Surface | Owner of remaining work | Remaining to all-green |
|---|---|---|---|
| 1 | Hero Talents | **S6** (locked-text) + **NW-15** | Locked-node red-text readability (S6); EYES review of existing shot |
| 2 | Workshop/Crafting | **DONE-V** + NW-15 | Verify cycle (gate+oracle+shot) then EYES |
| 3 | Building Upgrade | **NW-10** + NW-15 | WO-A finish (small diff); EYES |
| 4 | Cosmetic Shop | **DONE-V** + NW-15 | Verify cycle; EYES |
| 5 | Pet Skill Tree | **DONE-V** + NW-15 | Verify cycle; EYES |
| 6 | Party Shop | **NW-10** | Empty medallion fix + X audit (NOTE: gold `close_normal.png` X IS Blink canon — only ad-hoc text-X violates; re-verify against 07-03 ruling before "fixing") |
| 7 | Rumor Board | **NW-10** + NW-15 | Styled partial finish; EYES |
| 8 | Hero Skill Tree MVVM | **NW-15** | EYES review only |
| 9 | Hero Loadout | **NW-10** + NW-15 | Styled partial; EYES |
| 10 | Alchemy | **NW-10** + NW-15 | Styled partial; EYES |
| 11 | Jeweler | **NW-10** + NW-15 | Styled partial; EYES |
| 12 | Equipment paper-doll | **NW-10** (WO-582 ref) + **NW-13** | Restyle to Obsidian paper-doll reference; NEEDS OWN CAPTURE ROUTE (NOT_REGISTERED in hub walk — existing shot is -nographics noise) |
| 13 | Game Guide | **NW-10** + NW-15 | Styled partial; EYES |

### B. Front-end screens (rows 14–21)

| Row | Surface | Owner | Remaining |
|---|---|---|---|
| 14 | Studio bumper | **NW-13** + NW-15 | Capture phase + EYES (STYLED n/a — video) |
| 15 | Title screen | **S3** | Conversion + duplicate-UIDocument shrink |
| 16 | Story intro | **S3** | Conversion |
| 17 | Intro sequence player | **NW-12** | Styled partial + TESTED/EYES — NOT in S3's brief (gap, now assigned) |
| 18 | Hero Select | **S4** | Blink creation-carousel (WO-559) |
| 19 | Pet Select ✂ | **S6** — **SEQUENCED** | Delete — but referenced from TitleController/OnboardingPanelGuard/HeroSelectController → must land AFTER S3+S4 (see R1) |
| 20 | Onboarding coach marks | **NW-12** | Restyle + TESTED/EYES — NOT in S3's brief (gap, now assigned) |
| 21 | Village load overlay | **S4** | Legacy Text → kit |

### C. HUD postures (rows 22–28)

| Row | Posture | Owner | Remaining |
|---|---|---|---|
| 22 | calm(town) | **NW-9** | Move-cluster flat arrows → kit skin (EYES already ✅) |
| 23 | build posture | **S2** (styled dep) + **NW-13** | EYES shot after S2 lands (build-mode walk capture phase) |
| 24 | calm(explore) | **NW-13** + NW-15 | Capture + EYES |
| 25 | hostile(prebattle) | **NW-13** + NW-15 | Capture + EYES |
| 26 | hostile(activebattle) | **NW-13** | No fleet phase drives it visually — TESTED+EYES both blocked on a battle-posture drive phase |
| 27 | hostile(postbattle) | **S5** + **NW-13** | EndState owns; force-show capture phase |
| 28 | modal stand-down | **ORCH** (one trace read) | ✅ by design BUT open defect: icon pair floats right-edge during modals — kit dock intents (HudKitController :596) or legacy launcher. Data-first, no code until traced |

### D. Non-router overlays (rows 29–49)

| Row | Surface | Owner | Remaining |
|---|---|---|---|
| 29 | EndStateView | **S5** + NW-13 | Verify styling; force-show capture; EYES |
| 30 | GameOverScreen | **S5** | Route into EndStateView |
| 31 | GameOverUI ✂ | **S6** — **SEQUENCED after S5** | Referenced by `HeroHealth.cs` (death flow S5 also touches) — see R4 |
| 32 | BattleArenaHud results | **S5** | Victory/defeat/flee → EndStateView |
| 33 | Arena Panel | **NW-10** + NW-13 | Styled partial; capture+EYES |
| 34 | Dialogue view | **NW-15** | EYES review only (reference impl) |
| 35 | BuildMenu | **S2** | Conversion |
| 36 | BuildPaletteUI | **S2** | Conversion + shared Close |
| 37 | BuildSelectionUI | **S2** | Conversion + shared Close |
| 38 | BuildStructureInfoPanel ✂ | **S6** — **SEQUENCED after S2** | Same folder as S2's files; check S2 files don't reference it (R2) |
| 39 | BuildPreviewModal ✂ | **⛔ HOLD — PO decision** | "Never instantiated" is FALSE-adjacent: referenced by `PartyShopPanelMvvm.cs`, `NPCUpgradeStation.cs`, `TowerPreviewCamera.cs`, and Offset Forge canon (WO-490) REUSES it. Reclassify KEEP or spec a refactor — do NOT delete on the matrix's word (R3) |
| 40 | TowerManagerPanel | **S2** | Conversion |
| 41 | LevelUpSkillPopup | **NW-10** + NW-13 | Styled partial; capture+EYES |
| 42 | UiSpotlight | **NW-12** | Styled partial; TESTED/EYES |
| 43 | ObjectiveBannerUi | **NW-12** | Styled partial; TESTED/EYES |
| 44 | HelpMenu | **DONE-V** + **NW-13** | Verify cycle; capture route (not router-driven) |
| 45 | BugReportView | **NW-10** + NW-13 | Styled partial; capture+EYES (mind privacy canon: hide identity UI in captures) |
| 46 | Jukebox | **DONE-V** + **NW-13** + NW-17 | Verify cycle; capture route; music medallion icon check |
| 47 | Settings + Pause | **DONE-V** + **NW-13** | Both converted (UXML retired); capture route via pause driver |
| 48 | GearGrantToast | **NW-13** | TESTED oracle (toast-fired assert) + EYES |
| 49 | BuildFeedbackToast | **NW-13** | TESTED oracle + EYES |

### E. Social / store / raid / misc (rows 50–68)

| Row | Surface | Owner | Remaining |
|---|---|---|---|
| 50 | Leaderboard | **DONE-V** + **NW-13** | Verify cycle; capture route |
| 51 | ClanChat | **NW-7** | Close defect FIXED 07-03; FULL RESTYLE uncovered by any silo → NW-7 |
| 52 | *(row number absent from matrix)* | **PO/PM** | Numbering jumps 51→53 — confirm no surface was lost in enumeration; fix matrix numbering |
| 53 | PlayerProgressPanel ✂? | **NW-16** | Verify orphan (read-only ref sweep) then delete or reclassify |
| 54 | DailyQuestHud | **NW-8** | Stand-down shipped 07-03; FULL RESTYLE uncovered → NW-8 |
| 55 | QuestTrackerHud | **NW-9** | Overlap fixed+verified 07-03; remaining = fragment restyle |
| 56 | CompassHud | **NW-9** | Styled partial |
| 57 | SocialAccessCluster | **NW-9** | File deleted in kit demolition — VERIFY replacement exists (chat/leaderboard entry points still reachable?) |
| 58 | EchoWorkforceHud | **NW-13** | TESTED + EYES only |
| 59 | PackStore | **S1** | FrameMerchant conversion; survey done — money contract list in matrix work-log is the QA bar |
| 60 | Pi Sign-In | **S6** | Legacy → kit (small diff); privacy canon applies to captures |
| 61 | RaidSelectionScreen | **NW-13** | TESTED + EYES only |
| 62 | RaidDeployScreen | **S6** | Redundant 2nd header |
| 63 | RaidVictoryController | **NW-11** | Align/route into EndStateView — NOT in S5's brief (gap, now assigned; sequenced after S5) |
| 64 | OutpostVictoryController | **NW-11** | Same |
| 65 | TroopTrainingPanel | **S6** | Raw TMP → EnsureFont |
| 66 | ShopPanel (merchant) | **NW-15** | EYES review (already tested, emblem verified) |
| 67 | Inventory (bag) | **NW-13** + NW-15 | Capture + EYES |
| 68 | WaveCelebrationManager | **S5** | IMGUI retire into EndStateView |

### F. Dev-gated (rows 69–72) — pass = release-strip proof

| Row | Surface | Owner | Remaining |
|---|---|---|---|
| 69 | AdminOverlay 🔒 | **NW-14 (ORCH)** | Release-strip proof (note: HelpMenu conversion touched its PanelSettings handoff — assert still works dev-side) |
| 70 | OwnerDevToolsOverlay 🔒 | **PO** then NW-14 | Ships-Pi-gated DECISION is the owner's; then strip-or-gate proof |
| 71 | DebuggingController 🔒 | **NW-14 (ORCH)** | Release-strip proof |
| 72 | DevConsole 🔒 | **NW-14 (ORCH)** | Release-strip proof (file not found under that name in glob — locate first) |

### G. Dead/retired (pass = deleted) — all **S6**, with holds

| Row | Surface | Note |
|---|---|---|
| 19 | PetSelectController + PetSelectScreen.uxml + SelectScreen.uss + IntroPetCatalog refs | SEQUENCE: after S3+S4 (R1) |
| 31 | GameOverUI | SEQUENCE: after S5; edit `HeroHealth.cs` ref (R4) |
| 38 | BuildStructureInfoPanel | SEQUENCE: after S2 (R2) |
| 39 | BuildPreviewModal | **⛔ HOLD — see row 39 above; do not delete** |
| 73 | HeroTalentPanel (+Bootstrap) | Exists at `_Modules/HUD/` — sweep incl. bootstrap |
| 74 | BuildingUpgradePanel twin | Glob finds only the Mvvm — may already be gone; S6 verifies + marks TERM |
| 75 | ATB BattleHUD ✂flag | Flag-retire per canon (`dungeon-outpost-arena-one-space-primitive` — reversible), not hard-delete |
| 76 | PortraitLockOverlay | Exists at `_Modules/Core/UI/` — sweep |
| — | Orphaned assets post-#47/#47b | SettingsScreen.uxml/.uss, PauseOverlay.uxml + any UIDocument-wiring GameObjects → **NW-16**, only after verification passes |

---

## 2. GAP AUDIT — things NO in-flight silo covered (now assigned)

### 2.1 NO-CAPTURE-ROUTE ledger (image-pair rule: no bot path = no runtime image = CANNOT reach the owner)

The popup oracle only walks PanelRouter panels. Every surface below currently has **no bot
screenshot path** — each needs a scheduled capture-route work item (all → **NW-13** unless noted),
and each row stays open until its **image pair** (runtime shot + Blink/design reference) exists:

| Surface(s) | Capture route needed |
|---|---|
| HelpMenu (#44) | Open by its own entry point (H-key / launcher) |
| Jukebox (#46) | Dock/launcher Toggle() seam |
| Leaderboard (#50) | Social entry point (verify vs #57 cluster deletion) |
| Settings (#47) + Pause (#47b) | Pause driver → Settings from pause; two pairs |
| EndState variants (#27/29/30/32/68) | Force-show driver: victory / defeat / death / results — four pairs |
| Build-mode surfaces (#23, 35–37, 40) | Build-mode walk: enter → BuildMenu → palette → selection → TowerManager → exit; pair per surface + build-posture HUD pair |
| Front-end screens (#14–18, 20–21) | Boot-chain capture: bumper, title, story intro, intro player, hero-select, coach marks, load overlay |
| EquipmentPanel (#12) | NOT_REGISTERED in hub walk — own route; existing shot is -nographics noise, purge it |
| Battle-posture HUD (#25, 26) | Battle drive to hostile(prebattle) + hostile(activebattle) |
| calm(explore) (#24) | Explore-region walk capture |
| Inventory (#67), Arena Panel (#33), BugReport (#45), EchoWorkforce (#58), RaidSelection (#61), RaidDeploy (#62), Raid/Outpost victory (#63–64), TroopTraining (#65), Pi Sign-In (#60) | Entry-point drivers per surface |
| Toasts (#48–49) | Toast-fire oracle + timed capture |
| ClanChat (#51), DailyQuest (#54), HUD widgets (#22, 55–56) | HUD-state captures (widget visible, non-modal) |
| Moat water (NW-18) | Already framed by the windowed bot's bridge-crossing capture — route EXISTS; reference = design doc's recommended treatment until owner marks up |

Reference-image side of each pair: the Blink template sprite (mirrored via RpgUiImporter) or the
named design source (WO-559 carousel, WO-582 paper-doll, Obsidian panels, MOAT_WATER_DESIGN doc).
NW-13 delivers routes; NW-15 assembles + reviews the pairs.

### 2.2 Coverage gaps (now assigned)

1. **Capture routes** — the full §2.1 ledger. **ORCH/devtools job → NW-13. The single biggest blocker to TESTED/EYES and to ANY owner sign-off under the image-pair rule — schedule into the very next fleet build.**
2. **ClanChat full restyle** (#51) → NW-7.
3. **DailyQuest restyle** (#54) → NW-8.
4. **Dev-overlay release-strip proofs** (69–72) → NW-14; OwnerDevToolsOverlay Pi decision → PO.
5. **Dock-chips-during-modal** open question (#28) → ORCH one trace read (HudKitController :596 occupancy) BEFORE any silo "fixes" it blind.
6. **RaidVictory/OutpostVictory → EndStateView alignment** (#63–64) — S5's brief names GameOver/WaveCelebration/BattleArenaHud only → NW-11.
7. **Posture capture drives**: hostile(activebattle) (#26), EndState force-show (#27/29), build-mode walk (#23) → NW-13.
8. **Blink icon mirroring for unmapped medallion concepts**: nine emblems already seated (talent/potion/gem/hammer/armor/sword/quest/bag/coin per work log) — remaining to verify: **music** (Jukebox used FrameCore), **crest fallback**, anything S1/S2/S3 mint (pack-store coin vs gem, build hammer, title crest) → NW-17 (verify RpgUiImporter mirror has them; gitignore-safe).
9. **Duplicate-UIDocument remnants after S3**: UIDocument lives in 7 Onboarding files — S3 covers Title+StoryIntro only; OnboardingFlow, SplashLoading, OnboardingPanelGuard, PetSelectController remain → NW-12 + S6(#19). The dupe-error shrink is only PARTIAL until these land.
10. **Onboarding leftovers**: intro sequence player (#17), coach-marks flow (#20) → NW-12.
11. **HUD widget cluster**: move-cluster arrows (#22), CompassHud (#56), QuestTracker fragment (#55), SocialAccessCluster replacement-verify (#57) → NW-9.
12. **PlayerProgressPanel orphan-verify** (#53) + BuildingUpgradePanel-twin/DevConsole existence checks → NW-16.
13. **EYES review backlog**: ~15 rows have shots captured but unreviewed (all 🟡 EYES). Screenshot review vs Blink source is a PM+PO wave → NW-15. Machine capture ≠ done; the ten-year-old test is the bar.
14. **Row 52 numbering hole** in the matrix — confirm nothing was dropped between ClanChat and PlayerProgressPanel; fix numbering.
15. **BuildPreviewModal mis-classification** (#39) — matrix says ✂ "never instantiated"; grep shows 3 live referencing files + Offset Forge canon reuse. Escalate to PO; deleting it breaks WO-490.

### Next-wave assignment briefs (file-disjoint, one paragraph each)

- **NW-7 — ClanChat restyle (HUD silo):** Convert `_Modules/HUD/ClanChatPanel.cs` (+Bootstrap) to a kit FrameCore modal per the HelpMenu recipe; keep the 07-03 arbiter registration + Close; preserve every send/receive handler verbatim. Files: ClanChatPanel.cs, ClanChatPanelBootstrap.cs only.
- **NW-8 — DailyQuest restyle (Quests-HUD silo):** Restyle `_Modules/HUD/DailyQuestHud.cs` (+Bootstrap) off UITK onto kit widgets; keep the 07-03 modal stand-down; do NOT touch the four DailyQuest*Bridge files (logic layer). Files: DailyQuestHud.cs, DailyQuestHudBootstrap.cs.
- **NW-9 — HUD widget skin batch:** Move-cluster arrow art (#22), CompassHud (#56), QuestTrackerHud fragment (#55) onto kit plates; verify SocialAccessCluster's replacement entry points (#57) and spec if missing. HUD-kit widget files only; no modal files.
- **NW-10 — Router-panel finish batch:** WO-A Building Upgrade finish (#3), Party Shop medallion (#6 — respect the gold-X-is-canon ruling), partial-styled sweep on #7,9,10,11,13, Arena Panel (#33), LevelUpSkillPopup (#41), BugReportView (#45), Equipment paper-doll restyle (#12, WO-582 reference). All PanelRouter/overlay panel files, disjoint from every other lane.
- **NW-11 — Raid/Outpost victory alignment:** Route `RaidVictoryController` + `OutpostVictoryController` result moments into EndStateView per S5's pattern. **Sequenced strictly after S5 lands** (same EndStateView API surface).
- **NW-12 — Onboarding remainder:** Intro sequence player (#17), coach-marks (#20), UIDocument removal from OnboardingFlow/SplashLoading/OnboardingPanelGuard. **Sequenced after S3+S4+S6(#19)** — same folder.
- **NW-13 — Fleet capture/driver phases (ORCH/devtools):** New AutoPilot phases: open-by-entry-point for every non-router surface; battle-posture drive to hostile(activebattle); EndState force-show; build-mode walk; front-end screen captures (bumper/title/intro/hero-select); toast-fire oracles; EquipmentPanel + Inventory routes. Windowed, focused (runInBackground fix is in). Every capture feeds NW-15.
- **NW-14 — Release-strip proofs (ORCH):** Prove rows 69–72 absent/inert in a release build (locate DevConsole first); assert AdminOverlay's PanelSettings handoff still works dev-side post-HelpMenu-conversion.
- **NW-15 — EYES review wave (PM+PO):** Review every captured shot against its Blink source; log verdicts per row in the matrix; failures become small-diff tickets.
- **NW-16 — Deletion sweep phase 2 (after verification passes):** Orphaned UXML/USS assets from #47/#47b/#19, PlayerProgressPanel orphan-verify, twin/DevConsole existence reconciliation, matrix row-52 numbering fix.
- **NW-17 — Medallion icon-mirror check:** Verify RpgUiImporter mirrors music/crest + any newly-minted concepts; fill gaps.
- **NW-18 — Moat water implementation (world lane; owner-routed 07-03; TWICE re-ruled same day):** Design doc `docs/MOAT_WATER_DESIGN_2026-07-03.md`. **Ruling 1 — width CLOSED:** water band widens to **~18m (radius 44→62)**. **Ruling 2 — bridge-clone unification** (*"if south works, all work — unless you tell clones to change"*): ALL FOUR crossings become CLONES of the south bridge. Remove the `label == "South"` special-case in CastleMoatBuilder (~line 413: South places the stone bridge prefab; N/W/E take a separate procedural funnel-ramp path). ONE bridge implementation, yaw-rotated per side (the sides tuple already carries 0/90/180/270); generalize the analytic deck collider + lift-seat math from the hardcoded south frame (-Z axis) to the per-side rotated frame. Consequences: the funnel-ramp path RETIRES for crossings (**ref-grep before removal — verify nothing else uses it**); the 18m band needs NO per-side exceptions — every crossing inherits the south ~22m span automatically (supersedes ruling 1's "N/W/E ramp-extension geometry" item). **Canon design principle:** water runs under the FULL playable span of every crossing — the water IS the walk-off containment and seam enforcement (diegetic); invisible rails are fallback only. **Slice 1 — ASSIGN IMMEDIATELY** to the next free SME thread, `CastleMoatBuilder.cs` (single file, world lane, no taste decisions): (a) mitred annulus mesh replacing the 4 overlapping planes, two-tone sub-rings, wet-shore strip, delete the dead dip-fill path; (b) band widen to 18m (44→62); (c) bridge-clone unification per ruling 2 — de-special-case South, rotate per side, generalize deck collider + lift-seat math, retire the funnel-ramp path after the ref-grep; (d) re-derive the shore/fill constants (`MoatWidth`/`FillInnerRadius`). Acceptance = the **crossing-span oracle applied identically ×4** (span > band + dry bedding both ends) **plus the fleet's SPAWN_TO_GATE / seam-nav assertions extended to all four gates**. **Still owner-blocked (slices 2–3 only):** color/mood, motion intensity, fish density. File-disjoint: no current SME touches CastleMoatBuilder.cs. Image pair: the windowed bot's bridge-crossing capture frames the moat (runtime side); the doc's recommended-treatment description is the reference until the owner marks up.

---

## 3. RISK REGISTER — cross-silo collisions + sequencing

| # | Risk | Detail | Mitigation |
|---|---|---|---|
| R1 | **Onboarding folder collision (S3 × S4 × S6)** | PetSelect (#19, S6 delete) is referenced by `TitleController.cs` (S3), `HeroSelectController.cs` (S4), `OnboardingPanelGuard.cs`. Three silos in `_Modules/Onboarding/`. | S3+S4 land first; S6 does NOT touch Onboarding until both merge; NW-12 last. Orchestrator commits by explicit path per lane. |
| R2 | **BuildMode folder collision (S2 × S6)** | S6 deletes `BuildMode/BuildStructureInfoPanel.cs`; S2 rewrites `BuildMode/BuildPaletteUI.cs` + `BuildSelectionUI.cs` (same folder — check for references to the deleted class). | S2 lands first; S6's build-mode deletions gate on a ref-grep of S2's final files. |
| R3 | **BuildPreviewModal false-✂ (S6)** | Referenced by PartyShopPanelMvvm, NPCUpgradeStation, TowerPreviewCamera; Offset Forge canon (WO-490) reuses it. Deletion breaks live panels + a settled product direction. | ⛔ HOLD row 39. PO reclassifies. S6 told explicitly: skip #39. |
| R4 | **Death-flow collision (S5 × S6)** | S6 deletes `GameOverUI.cs` whose caller is `HeroHealth.cs`; S5 reroutes death → EndStateView and plausibly edits the same call sites (GameOverScreen/HeroHealth). | S5 lands first and ideally removes the GameOverUI call path itself; S6 then deletes the orphan. |
| R5 | **Verification pile-up** | 8 prior conversions (work log) + 6 silo returns ride ONE combined gate. A gate failure must be attributed to the right lane, not smeared. | Orchestrator gates the combined tree once but keeps per-lane diff lists; failures bounce to the owning SME by path. |
| R6 | **asmdef conflicts** | Pending edits: DeNelle.Audio += UnityEngine.UI; DeNelle.Settings += UI+TMP. S1 (Wallet), S2 (Village), S3/S4 (Onboarding) may each add UI/TMP refs to their asmdefs. | Same-file risk only if two silos share an asmdef — Wallet/Village/Onboarding/HUD are distinct. Orchestrator reviews all asmdef diffs together at gate time. |
| R7 | **PackStore = real-money surface (S1)** | Purchase flow regression is a P0-class defect. | QA checklist §4 verbatim-contract items; no merge without the money-contract line-check + vendor oracle green. |
| R8 | **Over-zealous X removal** | 07-03 ruling: the gold X IS Blink's own close, CONFORMANT. SMEs reading the matrix's older "❌ X" cells may strip legitimate closes (Party Shop #6, Leaderboard pre-conversion note). | QA checks CLOSE fixes against the ruling, not the stale cell. |
| R9 | **Dock-chips defect fixed blind** | Open #28 defect could get "fixed" by an SME inside another brief without the trace read (banned inference-fix). | ORCH does the one trace read first; only then ticket it. |
| R10 | **Capture-route dependency** | S1–S5 outputs cannot reach TESTED/EYES without NW-13 phases; "done" claims will stall at 2/4 cells. | NW-13 is scheduled INTO the same verification cycle (§5), not after it. |
| R11 | **Water-design + Knight-animation lanes** | Both non-UI; no file overlap with S1–S6 expected — but the Knight lane touches hero prefab/rig which S4's HeroSelect carousel renders. | If S4 previews the hero via prefab reference, coordinate any rig rename with the Knight lane before merge. |

**Sequencing summary:** S1, S2, S3, S4, S5 in parallel now → combined gate/build/fleet →
S6 deletions (#19 after S3/S4; #31 after S5; #38 after S2; #39 HOLD) → NW-11, NW-12, NW-16 →
NW-13 capture phases ride EVERY cycle → NW-15 EYES wave after each capture batch.

---

## 4. QA CHECKLIST — applied by PM to every SME return

Per file touched:
- [ ] Brace balance check output shown (CLAUDE.md §1); NUL-byte clean (WO-434 gate will also catch)
- [ ] No `.unity` scene file hand-edited; no bake fired by the SME
- [ ] ASCII-glyph rule: no non-ASCII glyphs in code-built labels except via the approved glyph path (EnsureFont TMP; the matrix's ✓/✗ affordability glyphs follow the Workshop recipe)
- [ ] No `UIDocument`/UXML at runtime in converted files (grep the diff for `UIDocument`, `.uxml`); UITK removal complete, not commented-out
- [ ] Built from `BuildObsidianPanel`/`BuildObsidianModal`/kit widgets — no bespoke chrome, no restyling inside drop-zones (UI canon `ui-blink-template-master-frame-formula`)
- [ ] Shared chrome Close only — no per-panel X *unless it's the Blink `close_normal.png` notch* (07-03 ruling); Close raises the same event/callback the old close did
- [ ] Arbiter/PanelManager contract: registration present, `AnyOpen` stand-down honored, battle-lock rejects preserved (Jukebox pattern)
- [ ] Every pre-existing action handler / API preserved verbatim (Instance/Toggle/Open/Close/IsOpen, PanelRouter PanelId, events, reflection bridges character-for-character)
- [ ] asmdef refs added where kit/UI/TMP now used (the Audio-gate lesson) — and ONLY the owning silo's asmdef touched
- [ ] Null-conditional `?.` on cross-module service calls; `using DeNelle.Core.Combat;` where IDamageableStructure appears; no new System.Reflection in bridges
- [ ] No silent catch — Guard/FlowTrace on risky construction (§12); traces left ON for unstabilized surfaces
- [ ] Bootstrap host-free (no PanelSettings/UIDocument host needed) per the HelpMenu recipe
- [ ] S1 ONLY — money contract verbatim: WalletService async UniTask (never async void), currency-rail chips, PackPurchased event, treasury line, CurrencyDisclaimer, cozy covenant text EXACT, CloseStore reflection route + locomotion-re-enable fallback
- [ ] S6 ONLY — every deletion preceded by a shown ref-grep with zero live references; #39 skipped
- [ ] **IMAGE PAIR produced or scheduled:** the return names its runtime capture (existing route or the NW-13 phase that will produce it) AND its reference image (Blink sprite / design source). No pair plan = the return is not accepted as closable — text claims never reach the owner
- [ ] Matrix row cells updated in the same diff (same-breath rule); this board updated by PM on acceptance

---

## 5. VERIFICATION PLAN — the one combined cycle (ORCH)

One gate → one build → one fleet → one windowed capture run must assert, covering all six returns + the 8 queued conversions:

1. **CompileGate** (`COMPILE_GATE_OK` + NUL scan) on the combined tree. On failure: attribute by lane diff, bounce to owner SME, re-gate. No partial commits before green.
2. **Build** (windowed-capable player; `runInBackground=true` fix now active — confirm no focus-loss freeze recurrence: break-log alive past t=110, no OnApplicationPause stall).
3. **Fleet, headless oracles:**
   - Popup close oracle green on ALL converted panels (now 11+: Workshop, CosmeticShop, PetSkillTree, HelpMenu, Jukebox, Leaderboard, Settings, Pause + S1 PackStore, S2 build panels as registered) — shared chrome Close found by name.
   - Vendor oracle (ShopPanel) still green — regression guard.
   - **S1 contract oracle:** PackStore opens, catalog renders (pack count > 0), a purchase attempt round-trips the WalletService seam without exception, CloseStore re-enables HeroLocomotion (soft-lock guard assert).
   - **S2:** build-mode enter/exit walk — BuildMenu opens, palette renders, selection panel appears, TowerManagerPanel opens, HUD kit stands down in build posture, exit restores calm(town) posture line.
   - **S3/S4:** boot chain bumper→title→(story intro)→hero-select→confirm→village load overlay→hub reaches calm(town); duplicate-UIDocument error count REDUCED vs baseline (log-grep count, expect remnants per gap #9 — record the number).
   - **S5:** EndState force-show for victory/defeat/death/results variants; GameOverScreen and WaveCelebration code paths route into EndStateView (FlowTrace step assert); no IMGUI OnGUI draw from WaveCelebration.
   - **S6:** deleted classes absent from build (grep player assembly/log); no MissingReference/CS0246 (gate catches); F8 break-log clean of new exceptions.
   - Posture lines: all 7 posture transitions still fire (kit regression).
   - **NW-18 (if slice 1 rides this cycle) — crossing-span oracle IDENTICAL on all four crossings** (clones of the south bridge, yaw-rotated): each span > the 18m water band AND landings bed on dry ground both ends (all inherit the south ~22m span). **Extend SPAWN_TO_GATE / seam-nav assertions to all four gates** — enemies and hero path over every bridge, not just south. Assert the funnel-ramp path is gone from crossings (and its ref-grep found no other consumer). Water-as-containment is the canon seam enforcement — the oracle, not invisible rails, is the proof.
4. **Windowed capture run (NW-13 phases) — the IMAGE-PAIR production step.** Pairs this cycle must produce (runtime shot ← route / reference source):
   - **S1:** PackStore modal ← store entry driver / FrameMerchant Blink template.
   - **S2:** BuildMenu, BuildPaletteUI, BuildSelectionUI, TowerManagerPanel + build-posture HUD ← build-mode walk / kit frame templates (5 pairs).
   - **S3:** Title, StoryIntro ← boot-chain capture / WO-C design refs (2 pairs).
   - **S4:** HeroSelect carousel, VillageLoadOverlay ← boot-chain capture / WO-559 Blink creation-carousel ref (2 pairs).
   - **S5:** EndState victory/defeat/death/results ← force-show driver / EndStateView WO-B ref (4 pairs).
   - **S6:** small-diff pairs — TroopTraining, RaidDeploy, Pi Sign-In, talent locked-text ← entry-point drivers / their Blink frames (deletion rows need no pair — pass by absence proof in the build grep).
   - **Queued conversions:** Workshop, CosmeticShop, PetSkillTree, HelpMenu, Jukebox, Leaderboard, Settings, Pause ← their new entry-point drivers / FrameCrafting-FrameMerchant-FramePet-FrameCore-FrameSettings-FrameOptions templates (8 pairs).
   - **Postures:** calm(explore), hostile(prebattle), hostile(activebattle) ← posture drives / HUD kit ref (3 pairs).
   - **NW-18 slice 1 (if landed):** moat annulus ← bridge-crossing capture / design-doc recommended treatment.
   Output to ui-shots with fresh timestamps (stale -nographics noise purged). Surfaces whose route isn't built this cycle are named in the run report as PAIR-PENDING — they cannot close.
5. **Arm the F8 watcher** for the owner's felt pass; **EYES wave (NW-15)**: assemble each pair side-by-side, review, log verdicts; PO closes rows **only on pairs, never on text**.
6. **Same-breath canon:** matrix cells + work log + this board updated; commits per lane by explicit path; push only after owner retest per release-process memory.

**Cycle exit bar:** gate green + all oracles green + capture set complete + zero new F8/exception entries. Anything less: the lane that failed goes back; the lanes that passed commit ("push the ones that passed").

---
## FELT-TEST TICKETS (owner, 16:00+, playing the 15:48 benchmark exe)
- **T-1 (UPGRADED to feature by owner): dragon = ambient fly-bys.** Owner verdict on the
  benchmark exe: "it looks amazing flying around — if we scaled it down, would be interesting
  to see random fly-byes." Design: scale the dragon down + a RandomFlyByDirector (seeded
  timer, varied altitude/path arcs over castle + OuterWorld, occasional distant roar via the
  existing DragonRoar SFX) — same living-world family as FishSchool; owner-tunable knobs
  (frequency, scale, altitude band) as data per her recipe style. Original scale note: route:
  world/enemy lane. First move for whoever takes it: pull the owner's F8 capture if she made
  one (LocalLow flag_NN.png + break-log), else grep the dragon's spawn/scale path
  (DragonRoar/dragon prefab references, likely a VisualFactory/injector scale constant) and
  produce the before/after image pair. "Still" implies a prior resize attempt — check git log
  for dragon scale commits before re-fixing (§12: find the captured truth first).
