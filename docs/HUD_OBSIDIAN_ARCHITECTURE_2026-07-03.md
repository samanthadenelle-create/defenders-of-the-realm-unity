> **THE ONE-SENTENCE ARCHITECTURE (owner, 2026-07-03): "These kits get assembled from prefabs and components into a dumb skin."**
> Mirrored pack prefabs + thin binder components = the View (dumb skin). The VM owns all state and logic.
> The factory is the assembler. Swap the skin; the wires never move.

# HUD → Blink Obsidian Conversion Architecture (2026-07-03)\n\n**Mandate (owner, BINDING):** every HUD element outside our own game icons converts to the real Blink Obsidian art, built from ONE common factory (`ElarionUiKit`), zero per-screen widget construction, iterated to her felt pass.\n\n**Owner design laws folded in (2026-07-03 rulings):**\n- **Style bar:** the pack language is dark forged steel on smoky obsidian — depth from embossing/rivets/texture; **gold is reserved for accents and content** (currency, rarity, highlights), never chrome outline. The procedural flat-black+gold-border fallback is why screens read "not styled" — every surface must wear the REAL frame; the procedural path remains only as the null-art fallback.\n- **Data-agnostic containers ("IT'S A PANEL"):** frames are containers, never screen-welded. The factory signature is `(frameName, viewModel)`-shaped; screen→frame assignments are DEFAULT bindings in a data table, not code branches. Content builders are chosen by the **VM's shape** (list/grid/detail/codex-entry), not by the frame. The conversion map below is therefore TWO tables.\n- **Stats_Panel = the hero-stats frame:** medallion socket top-left = hero portrait; header band = name/level/class; body well = stat rows on `Stat_Element` plates; bottom filigree = footer; the pack designs a **Close anchor into the top-right square notch** — `ZonesFor` gains a per-frame `close` rect, measured from real pixels.\n\n---\n\n## 0. Root causes found (the felt failures, named with file:line)\n\n| Felt failure | Root cause | Fix owner |\n|---|---|---|\n| **HP bar FULL at 9/145** | `BattleHud9Zone.FillBarLeft` (`Assets/_Modules/Village/Arena/BattleHud9Zone.cs:1701-1708`) sets `Image.Type.Filled` on **sprite-less** Images created by its `AddImage` (`:1571-1578`). uGUI ignores `fillAmount` when `Image.sprite == null` (it renders the plain full-rect quad) — so the fill paints full while the text (`:468`) is correct. Every bar in this file (`_hpFill :427`, `_bar2Fill :435`, `_targetHpFill :499`, cycle rows `:691`) has the defect latent. | Factory fill-binding contract (§1.1) + P3 conversion |\n| **PARRY!/RIPOSTE! giant stacking text** | `PlayerAttackController.OnParrySuccess` (`Assets/_Modules/Village/Enemies/PlayerAttackController.cs:383`) and the riposte swing (`:633`) call `DamageNumberSpawner.SpawnLabel` (`Assets/_Modules/Village/Enemies/DamageNumberSpawner.cs:140`) — pooled but **uncapped, un-deduped world-space TextMesh at 1.4–1.6× scale**; each parried hit inside the 0.25s window spawns another overlapping instance. | Factory `CombatText` widget (§1.8) + P3 call-site swap |\n| **Dead target's bar under "No Target"** | `BattleHud9Zone.PushTarget:549` — the `_target == null` branch early-returns after setting only the name; only the `cur == null` branch (`:553-566`) clears the HP fill/value/level/threat. | Factory `BuildTargetFrame.Clear()` contract (§1.10) |\n| **Party MP frozen at full / hero MP dropped** | `VillageHudController.cs:1878` (`mfimg.fillAmount = 1f`, never driven) + `SetMana` no-op (`:2906`) | P2 rebind to `HeroVitalsModel`/`PartyModel` |\n| **Wave chrome bleeding into battle** | Wave cluster/Start-Wave gating is scattered across `ApplyContext` (`:2389`) / `ApplyCombatGate` (`:876`) with a village-vs-world heuristic, not the space-type model | §3.3 contextual-rules table on `HudContextModel.Context` |\n| **Resource red/green flashing** | `_townResFlashUp` flash lerp `VillageHudController.cs:979` (+ gain-flash throttle `:293`) | P2: numbers count-tween only, no color flash (owner rule) |\n| **"Random X and Y beside settings"** | Glyph fallbacks for missing art: Raid `"x"` (`VillageHudController.cs:1343-1344`) and Harvest `"Y"` (`:1350-1351`) | P2: real iconed buttons or removal per earns-its-place |\n| **Square D-pad, not 4 rounded buttons** | `BattleHud9Zone` BL square tiles (`:138-142, :408+`) + `VirtualDPadLean.cs` (delete candidate) | Factory `BuildControllerCluster` (§1.11) |\n| **19 fleet-proven broken panel Closes** | All panels share `ElarionUiKit.ObsidianCloseButton` (`ElarionUiKit.cs:426`) — one site to convert to the 3-state `Close_Button` art; each consumer package must ALSO verify its `onClose` wiring actually dismisses (the shared chrome can't fix a dead callback) | P1 art + per-package acceptance |\n| **Talk button not appearing** | `SetTalkAvailable` (`VillageHudController.cs:1671`) / `BuildTownActionPanel` (`:1511`) — the availability push path must be root-caused during P2 (bridge push vs context gate) | P2 acceptance criterion |\n| **No countdown / dead wave timer** | `SetCountdown :2519` + wave-timer fallback poll `:168` — timer shows only when real; else the whole cluster hides | P2 + rules table |\n\n---\n\n## 1. THE COMMON FACTORY API (extend `ElarionUiKit` — single writer: the Kit team)\n\nAll methods live in `Assets/_Modules/Core/UI/ElarionUiKit.cs` (+ new sibling files under `Core/UI/`, same owner). All are **sprite-first with the existing procedural fallback** (null art can never blank a surface). All state changes ease (0.12s, owner smoothness directive). Sprite names below are the canonical `RpgUiCatalog` names §2 mints.\n\n### 1.1 `BuildObsidianBar` — THE bar, and THE fill-binding contract\n```csharp\npublic enum ObsidianBarKind { Health, Mana, Energy, Stamina, Cast, Xp, Heart, Loading, Stat }\npublic static BarHandle BuildObsidianBar(Transform parent, ObsidianBarKind kind,\n    Vector2 anchorMin, Vector2 anchorMax, bool withValue = false, bool framed = true);\n```\nSprites: `hud/bar_health` (Health_Bar), `hud/bar_mana` (Mana_Bar), `hud/bar_energy` (Energy_Bar), `hud/bar_stamina` (STamina_Bar), `hud/bar_cast_1..3` (Cast_Bar_1-3) + `hud/bar_cast_fill` (**Castt_Bar_Fill — typo'd source filename**), `hud/bar_stat_bg` (Stat_Bar_Background) + `hud/bar_stat_fill` (Stat_Bar_White — white, tinted per kind), `hud/bar_xp` (hud-xpbar). Frame art draws as a non-raycast overlay ABOVE the fill (the proven `DressBar` layering, `ElarionUiKit.cs:1172`).\n\n**THE ONE FILL-BINDING CONTRACT (game law, enforced by review §5):**\n1. The fill `Image` **always has a non-null sprite** (`bar_stat_fill` white fallback → kit rounded sprite → never null-with-Filled). *This is what makes the 9/145 bug (`BattleHud9Zone.cs:1701`) structurally impossible.*\n2. `type = Filled, fillMethod = Horizontal, fillOrigin = Left`.\n3. The ONLY width mutation ever is `fill.fillAmount = current / Mathf.Max(1, max)` — never `sizeDelta`, never anchor math (kills the `FloatingHealthBar.cs:466` / `NodeFillIndicator.cs:112` pattern at conversion).\n4. Updates arrive via a **VM Changed event** (`HeroVitalsModel` etc. from `CoreServices.HudModel`), never per-frame state pulls; the handle exposes `SetValue(float cur, float max)` which also writes the value label (`"9/145"`) so bar and text can never disagree.\n\n### 1.2 `BuildObsidianButton` — the 5×4 family\n```csharp\npublic enum ObsidianButtonStyle { Style1 = 1, Style2, Style3, Style4, Style5 }\npublic enum ObsidianButtonColor { Gray, Green, Red, Yellow }\npublic static Button BuildObsidianButton(Transform parent, string label,\n    ObsidianButtonStyle style, ObsidianButtonColor color,\n    Vector2 anchorMin, Vector2 anchorMax, Action onClick = null);\n```\nSprites: `button/button{1-5}_{gray|green|red|yellow}` (all 20, 9-sliced). Hover/press = the kit's eased `ColorTint` (`StyleButtonColors`, fade 0.12s) on the real art. Back-compat shim: `ButtonKind.Gold→(1,Yellow)`, `Confirm→(2,Green)`, `Danger→(1,Red)`, `Quiet→(1,Gray)` so the existing `Button()` internally routes here sprite-first — every panel in the game upgrades in one place.\n\n### 1.3 The shared 3-state Close (ONE site)\nRework `ObsidianCloseButton` (`ElarionUiKit.cs:426`) internals: `Image.sprite = button/close_normal`; `Button.transition = SpriteSwap` with `highlightedSprite = close_on`, `pressedSprite/disabledSprite = close_off`; drop the "Close" text chip. Position comes from the frame's measured `close` zone when a frame is used (Stats_Panel's top-right notch — owner ruling), else the current corner anchor. All ~19 broken Closes route through this one construction; the *wiring* of each `onClose` is verified per consumer package.\n\n### 1.4 `CurrencyChip` — gold primacy (owner directive)\n```csharp\npublic enum CurrencyKind { Gold, Crystal, Wood, Iron, Food, Wisdom }\npublic sealed class CurrencyChipHandle { public Image icon; public TMP_Text amount; public void SetAmount(long v, bool animate = true); }\npublic static CurrencyChipHandle CurrencyChip(Transform parent, CurrencyKind kind,\n    Vector2 anchorMin, Vector2 anchorMax, bool primary = false);\n```\nPlate: `element/element_stat` (Stat_Element) or `element/element_bar` (Panel_Element), 9-sliced. Icon: **our own currency icons** (mandate — icons stay ours). `primary:true` (Gold) renders one size class larger with the gold-tinted amount — gold's visual primacy is a property of the component, not per-screen styling. `SetAmount` count-tweens the number; **no red/green flash ever** (owner rule). Every surface — HUD strip, shops, upgrade panels, end-state spoils — consumes this; per-screen currency rendering is a review-blocking violation.\n\n### 1.5 `BuildToast` — Notification art\nRestyle `ToastCard` (`ElarionUiKit.cs:504`) sprite-first onto `element/notif_1|notif_2|notif_4` (Notification_1/2/4) with the tone accent retained; procedural card stays as fallback. Callers (GearGrantToast, BuildFeedbackToast) unchanged — they already use the shared card.\n\n### 1.6 `BuildActionSlot` — Action_Bar_Slot + cooldown sweep\n```csharp\npublic sealed class ActionSlotHandle { public GameObject root; public Image frame, icon, cdRing; public TMP_Text cdText, count;\n    public void SetIcon(Sprite s); public void SetCooldown(float remaining, float total); public void SetCount(int n); public Button button; }\npublic static ActionSlotHandle BuildActionSlot(Transform parent, Vector2 anchorMin, Vector2 anchorMax, Action onTap = null);\n```\nFrame: `slot/slot_action` (Action_Bar_Slot, 9-sliced). Cooldown = `Filled/Radial360` Image **with a non-null sprite** (kit `CircleSprite`), shade `CdShade`. Serves the battle ability row, the assignable-extras bar, AND the owner's **hot-swap bars** ask (assignable action/consumable slots usable in town/world per space-type rules).\n\n### 1.7 `BuildCastBar` — telegraph UI\n```csharp\npublic static CastBarHandle BuildCastBar(Transform parent, int style /*1..3*/, Vector2 anchorMin, Vector2 anchorMax);\n// CastBarHandle { fill, label; SetCast(string name, float t01); Show/Hide with eased fade }\n```\n`hud/bar_cast_{1|2|3}` frame + `hud/bar_cast_fill` fill under the §1.1 contract. Bound to a new `CastModel` in `Core.HudModel` (caster, ability, progress) fed by a producer (P4).\n\n### 1.8 `CombatText` — the PARRY fix (pooled, capped, non-stacking)\nNew file `Assets/_Modules/Core/UI/CombatTextLayer.cs` (Kit-team-owned):\n```csharp\npublic enum CombatTextKind { Parry, Riposte, Block, Status }\npublic static class CombatText { public static void Show(CombatTextKind kind, string text, Vector3 worldPos); }\n```\nContract: ONE screen-space layer; **pool of 6, hard cap**; **per-kind dedupe window 0.5s** — a repeat inside the window refreshes the live instance and bumps a counter ("PARRY! ×3") instead of spawning; **max font size capped** (stamp font, ≤44 reference px); auto-expire ~0.9s with eased rise+fade; Obsidian styling (Acme stamp font, dark outline, kind-tinted). P3 swaps the two emitters (`PlayerAttackController.cs:383`, `:633`). Damage **numbers** stay on `DamageNumberSpawner` (out of scope for this widget).\n\n### 1.9 `BuildTab` / `BuildToggle` / `BuildSlider` / `BuildDropdown`\n- `BuildTab(parent, label, min, max, onSelect)` — `element/element_tab` (Tab), selected state = `arrow_box_on`-style highlight.\n- `BuildToggle(parent, bool initial, onChanged)` — `button/toggle_on|toggle_off` (switch) and a `checkbox:` variant on `element/togglebox_on|off`.\n- `BuildSlider(parent, min, max, value, onChanged)` — uGUI `Slider` skinned `button/slider_bg|slider_fill|slider_handle` (fill under the §1.1 contract).\n- `BuildDropdown(parent, options, onSelect)` — `button/dropdown_1..3`.\n\n### 1.10 `BuildTargetFrame` / `BuildNameplate`\n- `BuildTargetFrame(parent, min, max)` on `hud/target_core` (Target_Core) + nameplate bars → handle with `Bind(TargetModel)` and a **`Clear()` that empties EVERY field** (name→"No Target", fill→0, level/value/threat→"", tint→neutral). Fixes `BattleHud9Zone.cs:549` by contract.\n- `BuildNameplate(parent, NameplateKind kind /*Player, Party, Enemy, Neutral, Rare, Boss*/)` — `hud/nameplate_*` set (Nameplate_Bar/Health/Health_Enemy/Health_Neutral/Portrait, Rare/Boss borders, Party_Nameplate) for the world-space `FloatingHealthBar` restyle; its `sizeDelta` fill (`FloatingHealthBar.cs:466`) converts to the §1.1 contract.\n\n### 1.11 `BuildControllerCluster` — the four rounded buttons (owner mockup)\n```csharp\npublic static ControllerHandle BuildControllerCluster(Transform parent, Vector2 anchor, Action<Vector2> onMove /* or 4 discrete events */);\n```\nFOUR ROUND buttons (round-format Obsidian button art — pick from `GameMenu_Button_1..3` / the roundest `Button4/5` face at eyeball time; fallback = kit `CircleSprite` disc + steel tint, never square), eased press squash, thumb-reach sized (≥56px targets). Replaces the BL square D-pad (`BattleHud9Zone.cs:138-142`) and retires `VirtualDPadLean.cs`. Placement per space type from the rules table.\n\n### 1.12 `BuildChatDock`\n`hud/chat_core` + `hud/chat_tab` + `button/chat_element_1..4` — collapsible dock (`hud/collapse|expand`) hosting chat/ranks/music entry points (absorbs `SocialAccessCluster`). **Hidden entirely in build mode** (owner rule) via the rules table.\n\n### 1.13 Fonts (decision + pipeline)\n| Role | Font (Fonts_Obsidian) | Used by |\n|---|---|---|\n| `FontRoleTitle` | **Merriweather Bold** (serif — forged-fantasy headers) | panel titles, headers, end-state |\n| `FontRoleBody` (default) | **Alata Regular** (clean geometric sans — mobile legibility) | labels, buttons, body, numbers |\n| `FontRoleStamp` | **Acme** (display) | combat text stamps, toast headlines |\n(Titillium reserved; not generated in v1 — limit TMP atlas cost.) Pipeline: new editor step `BlinkFontImporter` (importer package) generates TMP SDF assets (`TMP_FontAsset.CreateFontAsset`, static atlas) into committed `Assets/Resources/RpgUi/font/{font_title,font_body,font_stamp}.asset`; `ElarionUiKit.EnsureFont` gains a role overload `EnsureFont(TMP_Text t, FontRole role)` resolving sprite-first → `TMP_Settings.defaultFontAsset` → LiberationSans (current fallback chain preserved, `ElarionUiKit.cs:996`).\n\n### 1.14 Frame plumbing changes (Kit team)\n- `ZonesFor` (`ElarionUiKit.cs:246`): re-measure every frame's zones **from the real art pixels** (owner ruling), add `close` (Vector4) per frame — Stats_Panel's top-right notch first. Add cases: `FrameCharacter` (Stats_Panel full zone set: medallion/header/body/footer/close), `FrameSettings`, `FrameOptions`, `FrameLoot`, `FramePet`, `frame_dialogue_2`.\n- Frames stay **data-agnostic**: no content logic in `BuildObsidianPanel`; the default screen→frame map is a data table (§3.1), consulted by callers, never `switch`ed on inside the factory beyond zone geometry.\n\n---\n\n## 2. THE IMPORTER TABLE (`BlinkUiImporter.BuildTable` additions — `Assets/Editor/BlinkUiImporter.cs:62`)\n\nNew roles: `hud`, `element` (plus existing `panel/button/slot/frame/decoration`). Format: Src (under `Assets/Blink/Art/UI/Obsidian_UI/`) → role/canonical → slice border (0 = Simple).\n\n**HUD_Obsidian → `hud/`** — `Health_Bar→bar_health 0` · `Mana_Bar→bar_mana 0` · `Energy_Bar→bar_energy 0` · `STamina_Bar→bar_stamina 0` *(typo'd source "STamina")* · `Stat_Bar_Background→bar_stat_bg 12` · `Stat_Bar_White→bar_stat_fill 12` · `Cast_Bar_1/2/3→bar_cast_1/2/3 0` · **`Castt_Bar_Fill→bar_cast_fill 0` (TYPO #1 — source filename is "Castt"; canonical name is clean)** · `hud-xpbar→bar_xp 0` · `Target_Core→target_core 0` · `Nameplate_Bar→nameplate_bar 0` · `Nameplate_Health→nameplate_health 0` · `Nameplate_Health_Enemy→nameplate_health_enemy 0` · `Nameplate_Health_Neutral→nameplate_health_neutral 0` · `Nameplate_Mana→nameplate_mana 0` · `Nameplate_Portrait→nameplate_portrait 0` · `Nameplate_Boss_Border→nameplate_boss 0` · `Nameplate_Rare_Border→nameplate_rare 0` · `Nameplate_Enemy_Background→nameplate_enemy_bg 0` · `Party_Nameplate→nameplate_party 0` · `Potrait_Border→portrait_border 0` *(typo'd source "Potrait")* · `HIUD_Core→hud_core 0` *(typo'd source "HIUD")* · `HUD_Diablo-Core→hud_core_diablo 0` · `Stat_Orb_Diablo→stat_orb 0` · `Chat_Core→chat_core 48` · `Chat_Tab→chat_tab 12` · `Quest_Tracker→quest_tracker 24` · `Quest_Tracker_BAR→quest_tracker_bar 0` · `Collapse→hud_collapse 0` · `Expand→hud_expand 0` · `Block→hud_block 0` · `Interaction→hud_interaction 0` · `Arc_1/Arc_2→hud_arc_1/2 0`\n\n**Buttons_Obsidian → `button/`** — `Button{1..5}_{Gray,Green,Red,Yellow}→button{1..5}_{gray,green,red,yellow} 24` (all 20) · `Close_Button_Normal/On/Off→close_normal/close_on/close_off 0` · `Toggle_On/Off→toggle_on/off 0` · `Slider_Background→slider_bg 12` · `Slider_Fill→slider_fill 12` · `Slider_Handle→slider_handle 0` · `Dropdown_1/2/3→dropdown_1/2/3 16` · `Notification_1/2→notif_btn_1/2 32` · `Popup→popup 32` · `Chat_Element_1..4→chat_element_1..4 12` · `Map_Zoom/Map_Unzoom→map_zoom/map_unzoom 0` · `Arrow→arrow 0`\n\n**Elements_Obsidian → `element/`** — `Stat_Element→element_stat 24` · `Panel_Element→element_bar 32` · `Tab→element_tab 16` · `Notification_1/2/4→notif_1/2/4 32` · `Bar_1→element_bar_1 0` · `Bar_5→element_bar_5 0` + `Bar_5_Fill→element_bar_5_fill 0` · `Loading_Background→loading_bg 24` · `Loading_Bar→loading_fill 12` · `Scroll_Background→scroll_bg 12` · `Scroll_Up→scroll_up 0` · `ToggleBox_On/Off→togglebox_on/off 0` · `Handle→handle 0` · `Cross→cross 0` · `Arrow_Box/Arrow_Box_On→arrow_box/arrow_box_on 0` · `GameMenu_Button_1/2/3→menu_btn_1/2/3 0` · `Enchanting_Element→enchant_element 24` · `Enchanting_Slot→enchant_slot 24` · **`Socketing_Border_!→border_socket_1 24` (TYPO #2 — source filename ends in "!"; canonical name is clean)** · `Socketing_Border_2/3/4→border_socket_2/3/4 24`\n\n**Slots_Obsidian → `slot/`** (additions) — `Action_Bar_Slot→slot_action 24` · `Armor_Slot→slot_armor 24` · `Character_Slot→slot_character 24` · `Rarity_1..5→rarity_1..5 24` · `Socketing_Slot→slot_socket 24` *(names already reserved as `RpgUiCatalog` constants `:87-90` — this finally populates them)*\n\n**Panels_Obsidian → `frame/`** (gap-fill) — `Dialogue_2_Panel→frame_dialogue_2 0` (NPC card variant; owner: Dialogue_Panel/Dialogue_2 are the NPC-card frames) — the other 15 `frame_*` are already committed.\n\nRules: bars/cores/nameplates/cast = **Simple** (ornate silhouettes distort under 9-slice); buttons/slots/plates/tabs/chat-core = **9-sliced** at the borders above (eyeball-tune on import). Source paths are verbatim-with-typos; canonical names are always clean — the catalog never sees a typo. Additional typo'd sources noted for the record: `Loogin_Input_Field.png` (Elements, unimported v1). Every canonical name gets a `RpgUiCatalog` string constant (Kit team writes the constants; importer team writes only the table).\n\n---\n\n## 3. THE CONVERSION MAP\n\n### 3.1 Table A — container defaults per surface (DATA, not code)\nThe default frame per surface; any frame can hold any VM (owner law). HUD strips are chrome-less zones, not framed panels — frames apply to the panel-like HUD blocks only.\n\n| Surface | Default container | Note |\n|---|---|---|\n| Hero stats screen | `FrameCharacter` (Stats_Panel) | owner-assigned: medallion=portrait, header=name/lv/class, body=`element_stat` rows, close=top-right notch |\n| NPC dialogue card | `FrameDialogue` / `frame_dialogue_2` | name + affiliation + portrait (silhouette placeholder law) |\n| Shops | `FrameMerchant` | existing |\n| Quest / codex / assault manifest | `FrameQuest` | the readable-text/list container — quest text, enemy codex, incoming-assault manifests all qualify |\n| Settings / Help | `FrameSettings` | |\n| Music/options | `FrameOptions` | |\n| Loot | `FrameLoot` | |\n| Pet | `FramePet` | |\n| Generic / end-state | `FrameCore` | |\n| Target frame (HUD) | `hud/target_core` | HUD block, not a modal frame |\n| Chat dock (HUD) | `hud/chat_core` | |\n\n### 3.2 Table B — content builder per VM shape (decoupled from frames)\n| VM shape | Builder | Consumes |\n|---|---|---|\n| `BarVM { cur, max, kind }` | `BuildObsidianBar` | any vitals/progress/cast source |\n| `ListVM<ItemVM>` | slot-grid/list builder (kit `Slot` on real slot art + rarity_1..5) | inventory, loot, codex entries, shop rows, assault rosters |\n| `DetailVM` | detail pane (Text_Background + `element_stat` rows) | recipe detail, codex detail, quest text |\n| `WalletVM` | `CurrencyChip` row (gold primary) | every wallet strip/footer |\n| `CommandVM` | `BuildObsidianButton` / Toggle / Slider / Dropdown / Tab | any action |\n| `ToastVM` | `BuildToast` | any notification |\n| `SlotVM` (cooldown/count) | `BuildActionSlot` | ability bar, hot-swap bars, consumables |\n| `TargetVM` | `BuildTargetFrame` | HUD target block |\n| `TextStampVM` | `CombatText` | parry/riposte/status |\n\n### 3.3 Element-level conversion — `VillageHudController` (town/world HUD)\nVMs = the existing `Core.HudModel` models fed by `HudModelProducers.cs` (`Assets/_Modules/Village/HUD/`) — the sanctioned MVVM layer; every reflection/`.Instance` pull below is replaced by a model `Changed` subscription. Space types: **Town / World / Battle / BuildMode** (Battle exists in `HudContextModel`; BuildMode is added by P4 — today build-mode hiding is the `BuildModeHudBridge` hack).\n\n| Element today (file:line) | Factory replacement | VM | Visible in |\n|---|---|---|---|\n| Party frames + hero HP/MP rows (`VillageHudController.cs:1783-1968`, fills `:1854-1878`, `player_hp_fill` widget sprites `:1793`) | `BuildNameplate(Party)` + `BuildObsidianBar(Health/Mana)` — kills the dual-fill-source gotcha (BLINK_UI.md) by making §1.1 the only fill path; fixes frozen MP `:1878` + dropped `SetMana :2906` + hero-name fallback `:1908-1917` | `HeroVitalsModel` + `PartyModel` | Town+World+Battle (combat-gate: collapsed to hero row when safe, per `ApplyCombatGate :876` rules) |\n| Heart/castle banner (`:1740-1777`, `SetHeartHp :2539`) | `BuildObsidianBar(Heart)` framed | `WorldModel` | Town only |\n| Resource strip (`:1695`) + town resource badges (`:2155`) + flash (`:979`, `:293`) | ONE `CurrencyChip` row, gold primary; **no flash, count-tween only**; tap-expand: collapsed chip row → expands on tap (owner) | `EconomyModel` | Town+BuildMode; **collapsed/hidden in World** (owner: hidden rules in world); Battle: hidden |\n| Wave cluster: gear-clock+plate (`:2069-2131`), Start Wave (`:2004`), progress (`:2782`), lookout (`:1053`, `:2809`), `SetWaveImminent :2662`, no-op banner `:2670` | Wave block on `element_bar` plate + `BuildObsidianBar(Stat)` progress; Start Wave = `BuildObsidianButton(2,Green)`; countdown shows ONLY when real (else the cluster hides); wave-clear routes through `BuildToast` (kill the no-op) | `WaveModel` | **Town, between waves only** — never World, never Battle, never BuildMode (owner wave-chrome law) |\n| Top chrome: pause (`:1320`), raid "x" (`:1343`), harvest "Y" (`:1350`), settings (`:1353`), icon button recipe (`:1359-1386`) | `BuildIconButton` re-plated on kit (steel seat, our icons); raid/harvest get real icons or come OFF per earns-its-place; settings stays far corner | commands only | Pause: all; raid/harvest/settings: Town+World; BuildMode: settings only |\n| Wisdom badge (`:1394`, reflection `:1486-1497`) | kit badge chip on `element_stat`, pulse eased | `HeroVitalsModel` (wisdom added) or dedicated model | Town+World, hidden Battle/BuildMode |\n| Town action panel Talk/Quest (`:1511-1593`), context action (`:1594`), `SetTalkAvailable :1671` | `BuildObsidianButton` pair; root-cause the talk-availability push as part of conversion | interaction model/bridge | Town+World (contextual availability) |\n| Build button (`:1982`) | `BuildObsidianButton(1,Yellow)` | command | Town only |\n| Repair prompt (`:2028`, `ShowRepairPrompt :2686`) | `BuildToast(Danger)` variant | pushed args | Town |\n| Town metrics strip (`:2250-2285`, `SetTownMetrics :2823`) | Heart readout stays (earns its place); **tower 0/0 and population come OFF unless made real+verified** (owner bottom-bar ruling) | `WorldModel` | Town only |\n| Mini-map (`:2210`, `SetMinimapPoi :2865`) | keep as widget, re-plate `Minimap_Art`/`Map_Zoom/Unzoom`; earns-its-place review | existing | Town+World |\n| Passive-XP badge (`:2135`) | `element_stat` chip | existing setter | Town |\n| Runic border frame (`:1226`) | DELETE (chrome the pack supersedes) or `hud_core` dressing — owner eyeball | — | — |\n| `HudTheme` (whole file) | RETIRED — every token resolves to `ElarionUiKit`/`UiStyle.Theme`; file deleted at end of P2 | — | — |\n| `AddText` (`:3027`) | kit `Label`/`EnsureFont(role)` | — | — |\n| Pi sign-in button riding the HUD (`PiSignInController.cs:192`) | REMOVED from HUD — title/menu context only (owner earns-its-place) | — | Title only |\n| CompassHud (`HUD/CompassHud.cs`) | real, visible, useful compass on kit tokens or REMOVED (owner: "make it real or remove it") — PO call surfaced in P2 report | bootstrap-fed | World |\n| SocialAccessCluster chat/ranks/music (`HUD/SocialAccessCluster.cs`) | `BuildChatDock` (§1.12) | commands | Town+World; **hidden in BuildMode** (owner) |\n| QuestTrackerHud (`HUD/QuestTrackerHud.cs:79`) | `hud/quest_tracker` + `quest_tracker_bar` widget | thin `QuestVM` | Town+World |\n| XPBarController (UIToolkit, `HUD/XPBarController.cs:19`) + FloatingXpText (IMGUI) | `BuildObsidianBar(Xp)` on `bar_xp`; UITK/IMGUI paths deleted | `HeroVitalsModel` | Town+World |\n\n### 3.4 Element-level conversion — `BattleHud9Zone` (battle HUD) + feedback\n| Element today | Factory replacement | VM | Rule |\n|---|---|---|---|\n| TL hero plate: portrait/name/HP/resource/buffs (`:408-448`), pull-based `PushPlayerStats :450` | `BuildNameplate(Player)` + `BuildObsidianBar(Health/Energy)` — **fixes the 9/145 bug by contract**; portrait on `nameplate_portrait`/`portrait_border` | `HeroVitalsModel` (event-driven, ends the per-frame pull) | Battle only (`Context == Battle`) |\n| TC target block (`:479-519`) + `PushTarget :546` | `BuildTargetFrame` on `target_core` — `Clear()` fixes the dead-bar-under-No-Target (`:549`) | `TargetModel` | Battle |\n| ML target-cycle rows (`:119-127`, fills `:691`) | rows on `nameplate_enemy_bg` + contract bars | `TargetCycleModel` | Battle |\n| Star conditions (`:525-544`) | `element_bar` plate + `FontRoleBody` | `MomentumModel` | Battle |\n| BL square D-pad (`:138-142`) | `BuildControllerCluster` — four rounded buttons (owner mockup) | input seam | Battle+World (space-type placement) |\n| BC ability row + extras (`:144-151`, `:240-260`) | `BuildActionSlot` row — also delivers the hot-swap bars | `AbilitiesModel` + AssignableSkillBar | Battle; hot-swap variant Town/World per rules |\n| BR slash + arc satellites (`:153-176`) | `BuildActionSlot` (big variant) on `slot_action`; arc kept, art swapped | `AbilitiesModel` | Battle |\n| TR settings+flee (`:115-117`) | `BuildObsidianButton(1,Red)` Flee + icon button | commands | Battle |\n| Panel dressing `Frame()` (`:1630`) / `AddPanel/AddImage/AddText` privates | DELETED — all construction via factory | — | — |\n| PARRY/RIPOSTE labels (`PlayerAttackController.cs:383`, `:633` → `DamageNumberSpawner.SpawnLabel :140`) | `CombatText.Show(Parry/Riposte, …)` (§1.8) | event args | Battle+World |\n| FloatingHealthBar world bars (`Village/Combat/FloatingHealthBar.cs:466` sizeDelta fill, own palette `:50-55`) | `BuildNameplate(Enemy/Neutral/Rare/Boss)` + §1.1 contract | existing `Func<float>` delegates (clean) | world-space |\n| Cast telegraphs (new) | `BuildCastBar` | new `CastModel` | Battle |\n\n### 3.5 OUT OF SCOPE (explicit)\n- **Our own game icons** (mandate): `Resources/HudIcons/**` class/ability/widget icons, `HeroPortraits/**`, currency/resource glyph icons. `Icons_Obsidian/` (70 files) is **NOT imported** in this program.\n- Damage **numbers** (`DamageNumberSpawner.Spawn`) — feel-tuned, stays.\n- Village panels/modals already on `BuildObsidianPanel` (audit §2a) — they inherit the button/Close/font/frame upgrades **through the factory for free**; their own redos are the audit's WO-A…WO-F, not this program.\n- World gizmos (PlacementGrid, GhostPreview), cursors (Cursors_Obsidian — desktop polish, later), Prefabs_Obsidian (we never adopt Blink prefabs/UXML — uGUI canon).\n\n---\n\n## 4. THE TEAM SILO MAP (file-disjoint; strict ownership)\n\n**Build order: P0 → P1 (P4 in parallel) → P2 ∥ P3 → felt-pass iteration loop.** Consumers (P2/P3) may not start until P0+P1 merge.\n\n### P0 — Importer package (tiny, lands first)\n**Owns:** `Assets/Editor/BlinkUiImporter.cs`, new `Assets/Editor/BlinkFontImporter.cs`, the generated committed PNGs under `Assets/Resources/RpgUi/{hud,element,button,slot,frame}/` + `Resources/RpgUi/font/`.\n**Does NOT touch:** any runtime C#.\n**Acceptance:** table matches §2 exactly (typo sources → clean canonical names); every sprite imports Sprite/Single, correct 9-slice; run `Defenders/Art/Import Blink UI Pack` batchmode green; 3 TMP font assets committed; fresh-clone safe (Blink absent ⇒ importer warns, game still runs on fallbacks).\n\n### P1 — Kit/Factory package (the ONLY `ElarionUiKit` writer)\n**Owns:** `Assets/_Modules/Core/UI/ElarionUiKit.cs`, `RpgUiCatalog.cs` (new constants only), `UiStyle.cs`, new `Core/UI/CombatTextLayer.cs` (+ any new Core/UI widget files). Nobody else edits these files during the program — all requests route to this team.\n**Delivers:** §1 API complete; `ZonesFor` re-measured from real pixels (+ per-frame `close` zone); 3-state Close; button family shim; style-bar compliance (steel/embossing depth, gold = accents only — retire the procedural gold-trim border as the *primary* look, keep as null-art fallback).\n**Acceptance:** compile+data gates green; a kit demo scene/dev overlay showing every widget at multiple sizes; fill contract unit-verifiable (`BarHandle.SetValue(9,145)` ⇒ `fillAmount≈0.062` AND fill sprite non-null); existing panels (skill tree, shops, crafting) render unbroken (sprite-first fallback intact); no consumer files touched.\n\n### P4 — Model/producer package (parallel with P1; file-disjoint from it)\n**Owns:** `Assets/_Modules/Core/HudModel/**` (add `CastModel`, `SpaceType/BuildMode` context value, wisdom field), `Assets/_Modules/Village/HUD/HudModelProducers.cs`, `HudModelHost.cs`, and the bridge retirements' Village side (`HeroAbilitiesHudBridge`, `PartyHudBridge` stay until P2 flips, then delete).\n**Acceptance:** `HudContextModel.Context` correct across Town/World/Battle/BuildMode transitions (autopilot-verified); producers change-gated; no HUD reference (assembly law).\n\n### P2 — Town HUD package\n**Owns:** `Assets/_Modules/HUD/VillageHudController.cs`, `HudTheme.cs` (retire+delete), `CompassHud.cs`, `SocialAccessCluster.cs`, `QuestTrackerHud.cs`, `XPBarController.cs`, `FloatingXpText.cs`, `VirtualDPadLean.cs` (delete). Consumes P1 API only.\n**Acceptance (felt fixes bolded):** zero raw `new GameObject(... typeof(Image))`/`AddComponent<Image>` widget construction left in the diff (factory-only); `HudTheme` deleted; **resource numbers never flash, tap-expand works, hidden-in-world rule enforced**; **wave chrome only in town between waves, countdown real-or-absent**; **party MP + hero MP live** (SetMana path or model rebind proven with a screenshot at partial mana); **talk button appears when an NPC is in range** (root cause named in report); **chat/ranks/music hidden in build mode**; **raid/harvest "x"/"Y" glyphs gone**; **Pi sign-in off the HUD**; bottom bar carries only real, verified numbers; gold chip primacy everywhere on this HUD; every Close on HUD-opened panels verified to actually close (list the 19, tick each).\n\n### P3 — Battle HUD + feedback package\n**Owns:** `Assets/_Modules/Village/Arena/BattleHud9Zone.cs`, `BattleArenaHud.cs`, `Village/Combat/FloatingHealthBar.cs`, the label call-sites in `Village/Enemies/PlayerAttackController.cs` + `DamageNumberSpawner.cs` (SpawnLabel path only), `HUD/BattleHudVisibilityManager.cs`.\n**Acceptance:** **HP bar at 9/145 renders ~6% — screenshot proof at low HP** (the `FillBarLeft` sprite-less-Filled path deleted; grep proves no `Image.Type.Filled` without a sprite assignment in the diff); **PARRY/RIPOSTE: spam-parry autopilot run shows ≤1 live stamp per kind, counter escalation, capped size, auto-expire**; **target frame: kill target → frame fully clears; `_target == null` shows a fully-empty frame** (`:549` branch fixed); four rounded controller buttons in place, square D-pad gone; ability row/arc on `slot_action` with radial sweeps; battle HUD visible ONLY when `Context == Battle` (no lingering into town — the DontDestroyOnLoad gate proven).\n\n**Merge protocol:** P0/P1/P4 land on the integration branch first; P2/P3 branch from that; no cross-package file edits — a needed kit change is a P1 ticket, never an inline edit.\n\n---\n\n## 5. THE REVIEW PROTOCOL (architect checks per package report, before orchestrator gates)\n\n1. **Factory-only construction:** grep the diff for `new GameObject`, `AddComponent<Image>`, `AddComponent<Button>`, `AddComponent<Text>`, `Sprite.Create`, private `AddPanel/AddImage/AddText/StylePanel` helpers in HUD consumer files — any hit outside `ElarionUiKit`/`CombatTextLayer` is a block. Also grep for `HudTheme.` (must trend to zero, then file deleted).\n2. **Fill contract:** grep for `Image.Type.Filled` and `fillAmount` in consumer diffs — every Filled image must come from a factory handle; grep for `sizeDelta` writes near "fill"/"bar"; block any sprite-less Filled.\n3. **MVVM compliance:** grep consumer diffs for `.Instance`, `FindObjectOfType/FindAnyObjectByType/FindObjectsByType`, `GetMethod(` (reflection pulls), `EconomyService`, `GameStateService` — Views bind `CoreServices.HudModel` models or push-seams only; VMs/models contain no `GameObject/Image/Sprite/RectTransform`.\n4. **Sprite-first-fallback:** every new `RpgUiCatalog.Get` call site null-checked with a procedural fallback (spot-check + a Blink-absent boot test: delete `Resources/RpgUi/hud` locally, game must not blank).\n5. **Contextual rules:** each element's visibility traced to the §3.3/§3.4 rules table via `HudContextModel` — no scene-name string checks, no new bespoke gates; wave chrome, chat dock, resource strip, controller cluster verified in all four space types (autopilot pass per type).\n6. **No per-screen styling:** no new `Color(` literals for chrome in consumer diffs (content tints like rarity via kit maps only); no per-screen fonts (all text through `Label`/`EnsureFont(role)`); no per-screen Close; gold used only for accents/content per the style bar.\n7. **Felt-fix evidence:** each package report must include the named screenshots (low-HP bar, parry spam, cleared target frame, world-context HUD, build-mode HUD) — the owner is never the bug detector.\n\n---\n\n## RETURN SUMMARY\n\n**Factory API list (all `ElarionUiKit`, single-writer):** `BuildObsidianBar` (+ the one fill-binding contract: non-null fill sprite, `Filled/Horizontal/Left`, only `fillAmount = cur/max`, VM-event-driven — broken path fixed: `BattleHud9Zone.cs:1701-1708` sprite-less Filled via `:1571-1578`), `BuildObsidianButton` (5 styles × 4 colors), 3-state `ObsidianCloseButton` rework (`ElarionUiKit.cs:426`), `CurrencyChip` (gold primacy, no flash), `BuildToast`, `BuildActionSlot` (+cooldown sweep, hot-swap bars), `BuildCastBar`, `CombatText` (pooled/capped/deduped — the PARRY fix for `PlayerAttackController.cs:383/:633` → `DamageNumberSpawner.cs:140`), `BuildTab/Toggle/Slider/Dropdown`, `BuildTargetFrame` (with total-`Clear()` — fixes `BattleHud9Zone.cs:549`), `BuildNameplate`, `BuildControllerCluster` (4 rounded buttons), `BuildChatDock`, role-based fonts (Merriweather-Bold titles / Alata body-default / Acme stamps, TMP assets generated by the importer), `ZonesFor` re-measured from real pixels + per-frame `close` zone, frames as data-agnostic containers with a screen→frame **data table** and VM-shape-selected content builders.\n\n**Build order:** P0 Importer → P1 Kit/Factory (∥ P4 Models/Producers) → P2 Town HUD ∥ P3 Battle HUD+Feedback → owner felt-pass iteration loop (kit-side tuning only; consumers re-verify).\n\n**Packages + acceptance:** §4 above — P0 (table+fonts, fresh-clone safe), P1 (API + demo + fill-contract proof + no consumer breakage), P4 (context correctness incl. BuildMode), P2 (factory-only, HudTheme deleted, resource/wave/talk/chat/Pi/x-Y felt fixes with screenshots, 19 Closes ticked), P3 (9/145 screenshot proof, parry-spam proof, target-clear proof, rounded controller, battle-only visibility).\n\n**3 highest-risk integration points I will watch:**\n1. **Kit-wide back-compat blast radius** — every panel in the game calls `BuildObsidianPanel`/`Button`/`ObsidianCloseButton`; P1's 3-state Close, button re-skin, and re-measured `ZonesFor` ripple into 60+ surfaces at once. Mitigation: sprite-first fallback preserved byte-for-byte, kit demo + regression screenshots of skill-tree/shop/crafting before P2/P3 branch.\n2. **The dual-fill-source seam** — `player_hp_fill`/`player_mp_fill` widget sprites vs `RpgUiCatalog` bar fills vs the new `hud/` bars (the documented BLINK_UI.md gotcha). If P2 converts the party frames while `PartyHudBridge`/`HeroAbilitiesHudBridge` still push and P4 models also fire, double-driving or dead bars result. One binding source per element, bridge deletion synchronized with the model flip.\n3. **Space-type context timing** — `BattleHud9Zone` is DontDestroyOnLoad and `HudModelHost` registers `CoreServices.HudModel` *after* scene load (null races already noted at `BattleHud9Zone.cs:180-184`); all the new visibility law hangs on `HudContextModel.Context` being right during scene transitions and build-mode enter/exit. P4's autopilot transition matrix (Town↔World↔Battle↔BuildMode) gates P2/P3 merges.\n\n

---

## ORCHESTRATOR AMENDMENTS (2026-07-03, owner-directed — BINDING over conflicting lines above)

### A1 — Prefab parameter extraction (owner law: "the parameters are the structures that map")
Assets/Blink/Art/UI/Obsidian_UI/Prefabs_Obsidian/ ships 58 COMPLETE assembled uGUI prefabs
(HUDCore, PartyNameplate [verified: full HP/MP panel with correct Filled-type fills], TargetNameplate,
CastBar1-3, DiabloHealth/Mana, all buttons/toggles, and full screens). These are the SOURCE OF TRUTH
for widget structure. P0 gains PrefabParamExtractor (editor tool): parse each prefab YAML ->
Assets/Resources/RpgUi/params/widget-params.json (committed): hierarchy, anchors, sizes, fill
configs, sprite refs by name. The factory consumes these parameters — no eyeballed dimensions
anywhere the pack already measured. (The "never adopt Blink prefabs" line = no direct GUID
instantiation; extraction IS the pattern; these are plain uGUI, not UXML.) Visual targets: WE render
each prefab via our own editor screenshot tooling (not external) — those renders are the
screenshot-compare standard per widget.

### A2 — TOTAL PRESENTATION DEMOLITION (owner rulings: "gut everything" -> "disassemble all the
entire presentation" -> "scrap it so nothing — I mean NOTHING — exists except the scaffolding")
Scope exceeds the HUD: the ENTIRE presentation layer is scrapped and rebuilt from the factory +
widget-params + the rules tables. Nothing visual survives. SCAFFOLDING THAT SURVIVES: Core.HudModel
VMs + producers (P4), CoreServices seams, PanelRouter routing (as plumbing), data catalogs, FlowTrace,
and our game icons. Every panel/screen/HUD in _Modules/HUD, Village HUD/Arena presentation, and every
BuildObsidianPanel consumer gets rebuilt through the new factory — the P2/P3 element tables are
REQUIREMENTS (what must exist + rules), not preserve-code instructions. Sequencing still P0->P1(P4)
->P2/P3, then the remaining screens in audit priority order under the same review protocol.

### A3 — Palette ruling pending
Owner shop mockup (black + gold filigree + maroon leather + parchment) vs pack forged-steel grey:
awaiting her one-line ruling ("Blink structure, black-gold-warm palette" proposed). Until ruled:
pack art untinted + a global chrome-tint hook in the factory so the ruling applies in one place.


### A4 — GENERIC HUD KIT: actionable areas, contexts as data (owner ruling 2026-07-03)
"Battle" is the wrong word — the HUD kit is GENERIC. Design: ONE HudKit skin divided into named
ACTIONABLE AREAS (working set, refined from HUDCore + thumb ergonomics: vitals [top-left],
status [top-center], system [top-right], targetInfo, actionRail [bottom-right thumb arc],
actionBar [bottom-center], moveCluster [bottom-left thumb], feedback [screen-space stamps/toasts],
dock [chat/social edge]). Each area = a stable mount point with a wiring contract. CONTEXTS
(Town/Overworld/Battle/BuildMode from HudContextModel) are DATA ROWS in hud-areas.json mapping
area -> widget list + visibility; no per-mode code branches. The former "P3 battle HUD" package
becomes the GENERIC HudKit package: assemble the skin's areas from mirrored prefabs, wire once,
populate per context from data. The 3.3/3.4 element tables remain the requirements, re-homed
into area/context rows.

**A4.1 — The first division (owner ruling): HOSTILE | FRIENDLY — two separate trees.**
The area taxonomy roots at "them or us". hud-areas.json root = two trees:
- hostile: targetInfo, castTelegraphs, targetCycle/enemy roster, threat/assault manifests,
  boss/rare framing -> territorially HIGH on screen (top / top-center crown).
- friendly: vitals, party, resources, abilities/actionRail/actionBar, moveCluster, dock,
  own status/buffs -> territorially LOW + at the thumbs.
system chrome (settings/pause) and feedback (stamps/toasts) sit outside both trees as overlays.
Every widget's placement, styling family, and context rules inherit from its tree first.

**A4.2 — Posture follows the scene (owner ruling): friendly scene = CALM posture; hostile scene = BATTLE posture.**
Context derives from the SAME first division applied to the SPACE. The classifier already exists:
SceneOwnership (Player-owned vs IsEnemyOwned — surviving scaffolding, already resolved per scene load).
- Friendly scene (hub/town, player-owned) -> CALM POSTURE (owner term): friendly tree fully out; hostile tree
  dormant (wakes only for live threats: an active wave populates hostile areas WHILE live, then sleeps).
- Hostile scene (arena/dungeon/outpost, enemy-owned) -> BATTLE POSTURE: hostile tree crowns the screen;
  friendly tree tightens to combat essentials at the thumbs.
Sub-postures as data, not new modes: BuildMode = a calm-posture variant (near-empty HUD);
Modal = overlay that mutes both trees. HudContextEvaluator gains SceneOwnership as its primary input;
the named-mode enum becomes derived, not authored.

**A4.3 — Calm forks by ground (owner ruling): calm(town) | calm(explore).**
- calm(town): home ground — wave block (between waves only), build button, vendor/talk affordances,
  resource chips visible, heart status. The domestic HUD.
- calm(explore): friendly posture in the open — compass/travel/node affordances up; resource strip
  HIDDEN until tapped (owner rule); NO wave chrome; engage affordances staged for the calm->battle
  handoff the instant ground turns hostile.
Posture set v1: calm(town), calm(explore), battle, + build as a calm(town) variant, modal overlay.
All occupancy = rows in hud-areas.json.

**A4.4 — Hostile forks by battle phase (owner ruling): hostile(prebattle) | hostile(activebattle) | hostile(postbattle).**
- hostile(prebattle): PREPARATION — only weapon panels + associated telemetrics: potion assignment,
  set spells and skills (the loadout moment before engaging). No world chrome, no town chrome.
- hostile(activebattle): the fight — hostile tree crowned (target, telegraphs, cycle), friendly tree
  at combat essentials (vitals, abilities, potions-in-slots, move cluster).
- hostile(postbattle): resolution — Victory / Defeat (the shared end-state template owns this posture);
  spoils, rating, one action out.
Full posture set: calm(town) [+build variant] · calm(explore) · hostile(prebattle) ·
hostile(activebattle) · hostile(postbattle) · modal overlay. All occupancy in hud-areas.json.

**A4.5 — The three battle stages (owner ruling): every battle has three stages.**
hostile(prebattle) is NOT a menu — it is the ENGAGEMENT WINDOW, entered when the player is ABOUT TO
ENGAGE or an enemy IS PURSUING. Trigger sources already exist: aggro/awareness sensors
(AwarenessSensor, EnemyAlertTell), the chase/leash system, the engage affordance. On entering the
window: weapon panel + potion slots + set-spells/skills surface, telemetrics on, world chrome falls
away — the HUD tenses before the fight does (parallel to chase music). Exits: engagement -> 
hostile(activebattle); threat lost/leashed -> back to calm(explore).

**A4.6 — The battle arc completed (owner rulings):**
hostile(prebattle) [the engagement window] -> **the ARENA, which is PURE BATTLE** — 
hostile(activebattle) is the isolated arena and nothing else: no world chrome, no town affordances,
only the fight (matches combat canon: isolated real-time arena, no structures in battle) ->
**hostile(postbattle): says WIN or LOSS and DECIDES — path, reward, devastation.** Postbattle is
not just a screen; it is the DECISION NODE: outcome routes what happens next — victory: the path
onward + the reward granted; defeat: the devastation (what loss costs). The end-state template is
its face; the routing/consequence logic is its function. HUD postures mirror the arc 1:1.

**A4.7 — The arc is the MASTER STATE; presentation submodels consume it (owner ruling).**
Devastation includes DEATH / INJURED. The posture arc (calm(town|explore) -> hostile(prebattle|
activebattle|postbattle) + outcomes) is one master state tree; every presentation layer is a
submodel reading it:
- HUD submodel: area occupancy per posture (hud-areas.json)
- ANIMATION submodel: calm gait -> engagement-window alert/weapon-drawn stance -> combat locomotion/
  stances -> postbattle victory pose | death | INJURED locomotion carrying devastation home.
  Scaffolding exists: InjuredLocomotion trees (Knight controller), HeroVictoryPoseBridge, death states.
- AUDIO submodel: already obeys it (chase music on the engagement window, battle/victory themes).
One state tree, many dumb presentation skins — the same MVVM law at game scale.

**A5 — CANONICAL UI FOLDER TREE (owner-specified 2026-07-03).** Target home for the entire UI system:
Assets/UI/{Core/{Buttons,Panels,Bars,Controls,Decorations,Cursors,Icons}, Themes/{Default,FantasyDark,
Rarity,Highlight}, Screens/{MainMenu,Character,Inventory,TalentTree,QuestLog,Merchant,Map,Settings,
Dialogue}, Prefabs/{Buttons,Panels,Windows,Elements}, Data/{JSON,ScriptableObjects}, Animations,
Materials, Shaders, Fonts, Sounds}.
EXECUTION: physical relocation rides the WO-545 Addressables migration (assets must leave Resources/
then anyway — one move, not two; tonight's pipeline keeps the Resources/RpgUi paths so the overnight
build mandate is not risked). Logical mapping is effective NOW: new JSON → Data/JSON semantics,
mirrored prefabs → Prefabs/*, fonts → Fonts, UiStyle themes → Themes (Rarity = a theme, confirming
rarity slot art as theme members). New UI assets created from tomorrow land in Assets/UI/ directly.

**A6 — External (Grok) UI plan reconciliation (2026-07-03).** ADOPTED: theme JSON schema
(UI_Theme_<name>.json: colors + per-widget sprite sets) as the UiStyle/Themes data contract
(FantasyDark/Rarity/Highlight as hot-swappable files); the strict naming convention
UI_[Type]_[Style]_[Variant]_[State]_[Color] applied AT the Assets/UI migration (with A5's move).
DECLINED with reasons: pre-migration mass move/rename (build-risk tonight, rides WO-545);
color-as-interaction-state (pack colors are SEMANTIC variants — states come from state art +
tint easing); a UIManager god-object (PanelRouter + factory already split those duties; no weld).
Phases 2-4 of the external plan are already delivered by P0/P1 (factory, mirrored prefabs, controls).
