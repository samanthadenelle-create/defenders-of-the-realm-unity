# F8 Triage Ledger — 2026-09-02 (seq 4235 → 4639)

**Sweep:** every `logs/f8-inbox/capture-*.md` with seq in `[4235, 4639]`.
**Method:** classified on the per-capture **Trigger** line (the only per-capture evidence — see the
drain caveat below). Read-only pass; no code touched.

---

## 1. Totals

| Bucket | Count |
|---|---|
| Captures swept (seq 4235–4639) | **405** |
| **PROD-021 R2 catalog cascade** (already triaged + CLOSED by the lead) | **381** |
| **Distinct roots** (this ledger) | **24** |

Kinds: 402 `error`, 2 `flagged`, 1 `exception`.

**The bulk ack is defensible.** 381 of 405 carry one of the following trigger signatures, and every one
of them is a downstream node of the single `StandaloneWindows64/catalog_*.hash` 404 that
`tools\r2-ship.ps1` fixed at 2026-09-02 01:48 (`R2_PUSH_OK 2 uploaded` /
`R2_PARITY_OK targets=Android,StandaloneWindows64,WebGL objects=228`):

| Cascade signature | n |
|---|---|
| `RemoteProviderException : Unable to load asset bundle from : https://pub-ab6d…r2.dev/…` | 61 |
| `[Flow:VisualFactory] model not found via Addressables OR Resources: 'Structures/…'` | 76 |
| `[Flow:MagentaGuard] hid stray MAGENTA placeholder 'PlayerBaseLayout/…'` | 90 |
| `[Flow:Structure] '<id>': visual 'Structures/…' is not resident yet — retaining …` | 45 |
| `[Flow:StructureAssets] async load of 'Structures/…' FAILED (Failed) after 0.0s` | 16 |
| `OperationException : GroupOperation failed because one of its dependencies failed` | 18 |
| `System.Exception: Dependency Exception ---> …OperationException` | 17 |
| `OperationException : CheckCatalogsOperation failed …404 Not Found` | 10 |
| `OperationException : ChainOperation failed because dependent operation failed` | 5 |
| `ResourceManagerException : ProvideResources failed` | 1 |
| *(rounding across sub-variants of the above)* | remainder |

**The 24 distinct-root captures are contiguous: seq 4355–4377, plus the single post-fix seq 4639.**
Nothing outside that band needs a ticket.

### ⚠ Evidence caveat that bounds every claim below
The daemon **drained a three-session backlog in one burst at 2026-09-02 01:35:00–01:35:17**. Every
capture written in that burst carries the **same** `Player.log` tail in its `## Auto-harvested
context` block — the tail as of the drain, i.e. the **end of the last session**, not the moment of
that capture. The per-capture `t` field (session seconds) is therefore the only reliable per-event
timestamp. Sessions in the band, by `t`:

- **Session A** — seq 4355–4364, `t` 372.6 → 607.5 (contains the owner's flag at `t=520.8`)
- **Session B** — seq 4365–4370, `t` 174.0 → 385.2
- **Session C** — seq 4371–4377, `t` 12.0 → 252.6
- **Session D** — seq 4639, `t` 17.1, wall clock 01:50:15 — **after** the R2 fix

Where a root below cites a harvested `[Flow:*]` line rather than a trigger line, that line is from the
**tail of Session A** (roughly its final ~90 s, which does bracket the flag at `t=520.8`). It is
correlative, not proof of simultaneity. That is stated per-root.

---

## 2. Distinct root #1 — **THE OWNER'S FLAG** (seq 4362, 4363)

- **Seq:** 4362 (`kind=flagged`), 4363 (`kind=flagged`, the mobile FLAG-button twin, `t` +0.004 s)
- **Scene:** `Main_Castle_Overworld`, `t=520.8158569335938`, utc `2026-09-01T20:12:15.0954…Z`
- **Verbatim trigger:**
  `[Main_Castle_Overworld] Something is wrong here. PLease trace it`
- **Player-facing:** yes, by definition — she flagged what she was looking at.
- **Consequence of the content outage?** **No.** Nothing in the flag's neighbourhood touches
  Addressables.

### What she was looking at, from the Session-A tail

Three named defects sit in that tail, and one of them dominates:

**(a) The hero was sliding through the west gate with a dead animator.** Repeated, throttled:

```
[Flow:HeroOwner] ANIMATION-VELOCITY STALL: root travelled 14.53 m/s but Animator Speed=0.00 (velSelf=0.00).
  The animator is being fed a DEAD value — a mover other than this component wrote the transform and
  nothing re-published its speed.
```
Emitted at `Assets/_Modules/Village/Hero/HeroLocomotion.cs:1690`. The state line beside it:
```
[Flow:HeroOwner] scene='Main_Castle_Overworld' owner=HeroLocomotion ownerCC=none ownerAgent=on-mesh
  scriptedMove=off velSelf=0.00 velRoot=14.49 animFeed=velSelf animSpeed=0.00 rootYaw=270.0
  … inputSuppressed=True autoWalk=False pos=(-39.50, 0.08, 0.00)
```
and immediately upstream:
```
[Flow:Seam] WarpTo sample HIT @ (-34.00, 0.08, 0.00) dist=0.00 (req (-34.00, 0.08, 0.00)) scene='Main_Castle_Overworld'
[Flow:Seam] WarpTo post-warp: agent.isOnNavMesh=True @ (-34.00, 0.08, 0.00)
```
The hero warps to x=-34, then travels x = -35.1 → -39.5 → -43.35 at z=0 (**due west**) at
**12–14.5 m/s** with `animSpeed=0.00` and `inputSuppressed=True`. That is a hero **gliding
through the west gate in an idle pose at ~3× run speed**, with input taken away from her.

Corroborating that she was on the walk-to-the-gate tutorial step at the time:
```
[Flow:Tutorial] FocusMask resolved highlightId=world.gate_direction target=WaveSpawnPoint-W style=Glow rect=(962,254,120,120)
```

**(b) A blank dialogue header plate:**
```
[Flow:UI] TextFitGuard [DialogueViewUI/ObsidianPanel/PanelContent/Zone_Header/Label]: armed but text
  still EMPTY after 600 frames — standing down (a blank plate here is a TEXT-NEVER-SET bug, not a fit bug)
```

**(c) Cosmetics service null — but at teardown**, immediately adjacent to
`[Flow:HeroXp] carrier destroyed`, so it is shutdown ordering, not what she saw:
```
[Flow:Cosmetics]   Refresh: CosmeticOwnershipService.Instance is null — 'Hero (Blaise)' keeps its default look.
```

### Judgement

**(a) is the flag.** It is the only item in the window that is loud, visual, sustained, and at the exact
world position and heading the tutorial was pointing her toward.

**⚠ AND IT MAY ALREADY BE CLOSED.** `WorkOrders/WORK_ORDER_1295_continuous_castle_gate_traversal.md`
retired `GateWarp` + the runtime `NavMeshLink` and the owner felt-verified it **on 2026-09-02** —
verbatim *"i can now go through gates normally"*. The flag session is **2026-09-01 20:12 UTC, before
that landed**, and its `[Flow:Seam] WarpTo` lines prove the warp was still live in it. Session D
(seq 4639, post-fix) shows a clean `[Flow:HeroOwner]` line with `inputSuppressed=False`, no
`WarpTo`, no stall — **but the hero was stationary (`velRoot=0.00`), so it proves nothing about a
moving hero.** WO-1298 is therefore written as a *verification* ticket, not a re-fix.

**(b) is a separate, unowned defect** and is folded into WO-1298 as a secondary item — it is one line
of evidence and does not warrant its own ticket until someone confirms which zone header it is.

---

## 3. Distinct root #2 — `MusicDirector.CrossfadeTo` NRE on a destroyed AudioSource (seq 4364)

- **Seq:** 4364 (`kind=exception`), Session A, `t=607.53` — the last event of the session, at teardown.
- **Verbatim trigger + stack (elided to the load-bearing frames):**
```
NullReferenceException: Object reference not set to an instance of an object.
UnityEngine.AudioSource.set_generatorObject (UnityEngine.Object value)
UnityEngine.AudioSource.set_clip (UnityEngine.AudioClip value)
DeNelle.Audio.MusicDirector.CrossfadeTo (UnityEngine.AudioClip clip, System.Single targetVol,
  System.Boolean loop, System.Single fadeSeconds) (at D:/EoA/Assets/_Modules/Audio/MusicDirector.cs:341)
Cysharp.Threading.Tasks.UniTaskScheduler:PublishUnobservedTaskException(Exception)
DeNelle.Audio.<CrossfadeTo>d__33:MoveNext() (at D:/EoA/Assets/_Modules/Audio/MusicDirector.cs:377)
```
- **Seam:** `Assets/_Modules/Audio/MusicDirector.cs:341` is `fadeIn.clip = clip;`. `fadeIn` is
  `_musicA`/`_musicB`, selected at line 337, and **is never null-checked before the assignment**. The
  NRE is raised inside `set_generatorObject`, i.e. the managed `AudioSource` wrapper outlived its
  destroyed native object. Reached from an `async UniTaskVoid` (`<CrossfadeTo>d__33`), so it surfaces
  via `PublishUnobservedTaskException` rather than at a call site.
- **Player-facing:** marginally — an unobserved exception at scene teardown; music can also die on the
  next scene if `_fading` is left latched. Not a blocker.
- **Consequence of the content outage?** **No.** No Addressables in the path.

---

## 4. Distinct root #3 — founding tutorial STEP-STUCK (seq 4370, 4376)

- **Seq 4376** (Session C, `t=246.55`), verbatim:
```
[Flow:Tutorial] STEP-STUCK :: founding_walk — no 'hero.reached:guide_gate' after 120s in-step
  (bound 120s, builder time excluded; ff.tutorialv2 on; builderOpenedThisStep=True, coachBeats=3);
  [WO-1036 clock: played-and-charged 120s, wall 232s, excluded (builder/frozen) 57s, discarded suspend gap 55s];
  RESCUED via watchdog and recorded as SKIPPED - the step was NOT completed, its outro is suppressed
  (no fiction narrated), grants still applied so the player is never half-granted.
```
- **Seq 4370** (Session B, `t=385.23`), verbatim:
```
[Flow:Tutorial] STEP-STUCK :: founding_defend — no 'wave.tutorial_band_repelled' after 120s in-step
  (bound 120s, builder time excluded; ff.tutorialv2 on; builderOpenedThisStep=False, coachBeats=3);
  [WO-1036 clock: played-and-charged 120s, wall 176s, excluded (builder/frozen) 1s, discarded suspend gap 55s];
  RESCUED via watchdog and recorded as SKIPPED …
```
- **Seam (both):** `DeNelle.Village.TutorialFlow:TickWatchdog ()` at
  `Assets/_Modules/Village/Tutorial/V2/TutorialFlow.cs:2253`.
- **Player-facing:** **yes.** The FTUE step is silently skipped and its narration suppressed. The owner
  is felt-testing the founding flow right now, so this is directly in her path.

### Consequence vs independent — judged separately

- **`founding_walk` — INDEPENDENT of the content outage, with high confidence.** The awaited signal is
  `hero.reached:guide_gate`, a world-trigger/locomotion event with **no Addressables dependency**. And
  it is the *same seam* as root #1: the flag session had the FocusMask pointing at
  `WaveSpawnPoint-W` while the hero warped and slid past x=-43 — i.e. the hero arrived at the gate
  region and the reach-event did not fire. `WorkOrders/WORK_ORDER_1295` changed exactly that geometry,
  so this may also be resolved; it has not been proven.
- **`founding_defend` — CANNOT BE ATTRIBUTED FROM THESE CAPTURES.** The awaited signal is
  `wave.tutorial_band_repelled`. Enemy *art* was unavailable during the outage, but
  `EnemyContentWarmer` logs its own failure as *"a visual defect only; the game did NOT stall, which
  is the point"* (`EnemyContentWarmer.cs:346`), so enemies should still have spawned as tinted
  capsules and been repellable. **There is no per-capture spawn evidence** — the shared drain tail
  (see the caveat) shows only `enemies=0 / wave=False` at end-of-session, which is after the step was
  already skipped and proves nothing about `t≈265–385`. **Do not assert a cause; instrument first.**

Written up as one ticket (WO-1300) because both land on the same signal plumbing in the same file,
with the two steps kept as explicitly separate sub-cases.

---

## 5. Distinct root #4 — ghost-modal report on 'Combat Item Picker' is a **FALSE POSITIVE** (seq 4360, 4361)

- **Seq:** 4360 (`t=502.48`), 4361 (`t=503.66`) — Session A, ~17 s before the owner's flag.
- **Verbatim trigger:**
```
[Flow:UI] PanelManager: 'Combat Item Picker' recorded as OPEN but its IsOpen probe reports NOT open
  — blank/failed panel masquerading as open (WO-465 invisible-scrim class).
```
  emitted at `Assets/_Modules/Core/UI/PanelManager.cs:175`.
- **Root, proven by ordering in source:** `Assets/_Modules/HUD/Kit/HudKitController.cs`
  - line **1753** registers the handle with the probe `() => _itemPicker != null`
  - line **1754** calls `PanelManager.NotifyOpened(_itemPickerPanelHandle)`
  - line **1757** is the *first* assignment of `_itemPicker`

  `NotifyOpened` runs its `IsOpen` verify (PanelManager.cs:168–178) **synchronously, inside the call at
  1754** — three lines before `_itemPicker` is ever set. The probe therefore returns `false` **every
  single time the picker opens, by construction.** The picker itself is fine.
- **Player-facing:** **no** — the modal opens normally. But it fires a `FlowTrace.Fail` → `Debug.LogError`
  → an F8 error capture on **every** combat-item-picker open, so it is actively polluting the queue
  the owner's real flags have to be found in. That is the §12 "oracle that cries wolf" failure mode.
- **Consequence of the content outage?** **No.**

---

## 6. Distinct root #5 — `dep MISS` on Synty shader-graph materials is a **FALSE POSITIVE** (13 captures)

- **Seq:** 4355, 4356, 4357, 4358, 4365, 4366, 4367, 4368, 4371, 4372, 4373, 4374, 4375 (all three sessions)
- **Verbatim trigger — full material name:**
```
[Flow:StructureAssets]   dep MISS on 'Structures/Tower_Wooden_Watchtower': material 'Castle_Wall_01'
  has NO albedo and NO tint — renders as an untextured grey blob.
```
- **Emitted at:** `Assets/_Modules/Core/Addressables/DependencyClosureTrace.cs:119`, reached from
  `Assets/_Modules/Core/Addressables/StructureAssetLoader.cs:150`.
- **Root, proven from the asset:** the material **is present and correctly textured**.
  `Assets/Synty/PolygonFantasyKingdom/Materials/Walls/Castle_Wall_01.mat` uses the Synty shader graph
  `Assets/Synty/PolygonGeneric/Shaders/Generic_Basic.shadergraph`
  (`m_Shader` guid `0730dae39bc73f34796280af9875ce14`), whose albedo slot is named **`_Albedo_Map`**
  and carries `Assets/Synty/PolygonFantasyKingdom/Textures/Castle/Wall_Brick_01.png`
  (guid `24f1ea296c9e695449086de7c2eca5e4`).
  `DependencyClosureTrace.Verify` probes only `_BaseMap` then `_MainTex` (lines 101–103) and reads the
  tint from `_BaseColor`/`_Color`, which the Synty material leaves at white — so it concludes
  "NO albedo and NO tint" for a fully-textured material.
- **Player-facing:** **no.** The watchtower renders correctly; only the oracle is wrong.
- **Consequence of the content outage?** **No** — the material resolved, which is why `mat.name` is in
  the message at all. Independent of PROD-021.
- **Why it matters right now:** the owner has just ruled the Archer Tower back onto her Tripo
  `Tower_Wooden_Watchtower` ladder. A standing red "dep MISS" on exactly that prefab will read as a
  real art defect to the next seat that opens the inbox, and this file's own comment
  (`DependencyClosureTrace.cs:105–108`) warns that *"an oracle that cries wolf gets ignored on the day
  it is right."* This is the second occurrence of that class.

---

## 7. Distinct root #6 — `enemyfam-skeletonhumanoid` is not a real label (seq 4359, 4369, 4377, **4639**)

- **Seq:** 4359 (`t=445.9`), 4369 (`t=212.2`), 4377 (`t=252.6`), and **4639 (`t=17.1`, wall clock
  01:50:15 — 2 minutes AFTER the R2 push landed)**. The post-fix occurrence is what proves this is not
  a 404.
- **Verbatim trigger + stack:**
```
UnityEngine.AddressableAssets.InvalidKeyException: … No Location found for Key=enemyfam-skeletonhumanoid
UnityEngine.AddressableAssets.Addressables:DownloadDependenciesAsync (object,bool)
DeNelle.Core.EnemyContentWarmer/<>c__DisplayClass43_0:<WarmFamily>b__0 () (at .../EnemyContentWarmer.cs:386)
DeNelle.Core.Diagnostics.Guard:Try (…) (at .../Guard.cs:34)
DeNelle.Core.EnemyContentWarmer:WarmFamily (string) (at .../EnemyContentWarmer.cs:384)
DeNelle.Core.EnemyAssetLoader:PrewarmFamily (string) (at .../EnemyAssetLoader.cs:125)
DeNelle.Village.EnemyAnimatorLateBinder:Arm (UnityEngine.Animator,string,string) (at .../EnemyAnimatorLateBinder.cs:…)
```
- **Root, proven from source + settings:**
  - Declared labels are exactly five —
    `Assets/AddressableAssetsData/AddressableAssetSettings.asset:116-120`:
    `enemyfam-orc`, `enemyfam-hollow`, `enemyfam-shared`, `enemyfam-troll`, `enemyfam-bosses`.
    **`enemyfam-skeletonhumanoid` is not among them, and no other `enemyfam-*` key is missing** — this
    is the only bad one.
  - `EnemyContentWarmer.FamilyOf` (`EnemyContentWarmer.cs:215-225`) takes the text before the first
    `_` of the last path segment.
  - `EnemyAnimatorLateBinder.Arm` at **`Assets/_Modules/Village/Enemies/EnemyAnimatorLateBinder.cs:65`**
    calls `EnemyAssetLoader.PrewarmFamily(ctrlName)` — the **controller** name (`SkeletonHumanoid`) —
    while `modelName` is in scope at line 60 and is what every other call site passes:
    `EnemyFactory.cs:1004` → `PrewarmFamily(m)`, `EnemyLateSkinner.cs:92` → `PrewarmFamily(model)`.
    `FamilyOf("SkeletonHumanoid")` → `"skeletonhumanoid"` → label `enemyfam-skeletonhumanoid` → no
    location → throw.
- **Player-facing:** indirectly. The family pre-fetch is skipped, so the enemy's real body/controller
  trickles in per-address instead of arriving as one family fetch — the exact "enemy spawns and
  slides for now" case the `Arm` doc comment at lines 67–70 describes. The `Guard.Try` at
  `Guard.cs:34` contains the throw so nothing crashes, but it logs an error → an F8 capture per
  occurrence.
- **Consequence of the content outage?** **No — proven.** Seq 4639 fired **after** `R2_PUSH_OK` /
  `R2_PARITY_OK`, on a fresh session, at `t=17.1`.

---

## 8. Work orders minted from this ledger

| WO | Root | Seq | Severity |
|---|---|---|---|
| **1298** | Owner flag — hero animation-velocity stall / warp slide at the castle gate (**verify against WO-1295, do not re-fix**) | 4362, 4363 | P0 — owner flag |
| **1299** | `MusicDirector.CrossfadeTo` NRE on a destroyed `AudioSource` under an in-flight UniTask | 4364 | P3 |
| **1300** | Founding tutorial STEP-STUCK — `founding_walk` and `founding_defend` completion signals never fire | 4370, 4376 | P1 — FTUE |
| **1301** | PanelManager ghost-modal report on 'Combat Item Picker' is a false positive (verify-before-assign) | 4360, 4361 | P2 — queue pollution |
| **1302** | `DependencyClosureTrace` reports every Synty shader-graph material as `dep MISS` | 4355-4358, 4365-4368, 4371-4375 | P2 — queue pollution, blocks the Tripo tower ladder read |
| **1303** | `EnemyAnimatorLateBinder.Arm` prewarms by controller name → `enemyfam-skeletonhumanoid` never resolves | 4359, 4369, 4377, 4639 | P2 |

**Main-line next free after this batch: 1304.** The banner
(`CLI_LANES_WO_NUMBERS.md`, "RECONCILED 2026-09-02 (CLI): main line next free = **1298**") was **not**
edited by this agent — the lead bumps it in the commit that lands these files.

## 9. Nothing else needs a ticket

Every capture outside seq 4355–4377 and 4639 carries a PROD-021 cascade signature (§1 table). Three
further lines were seen in the harvested tail but are **not** ticketed, deliberately:

- `[Flow:Cosmetics] Refresh: CosmeticOwnershipService.Instance is null` — occurs adjacent to
  `[Flow:HeroXp] carrier destroyed`, i.e. scene teardown ordering. Single line, no player symptom.
- `[Flow:VFXManager] ReturnToPool(Collector_Ready): host is mid-(de)activation — reparent deferred one
  frame (WO-929)` — that is the WO-929 mitigation reporting that it worked.
- `[Flow:UI] TextFitGuard [DialogueViewUI/…/Zone_Header/Label] … EMPTY after 600 frames` — folded into
  WO-1298 §Secondary rather than given its own ticket; one line of evidence, unowned zone header.
