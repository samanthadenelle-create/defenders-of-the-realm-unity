# OVERNIGHT ORDERS - 2026-09-05 (CLI, owner asleep from ~00:00)

**Owner, verbatim (2026-09-04 23:55):** "can you run these from now as you see fit its midnight and i need
some sleep" / "ill leave phone on so you can test anytime you want".

Standing rules for the night (all already binding; restated so the morning report can be checked against them):
- Every lane edit-only + file-disjoint; ONE gate on the combined tree (`COMPILE_GATE_OK` + `REGRESSION_OK n/n`
  on fresh logs); APK + install through the scripts on every green; the headed walk proves each screen on the
  Seeker by its trace line. Commit local by explicit path. **NEVER push.**
- Owner rulings are NOT mine to make: anything that needs a creative/balance/copy decision gets its default
  from the review + the owner's rulings on file, is flagged in the RESULT, and waits for her word.
- No `git add -A`; no scene hand-edits; no FlowTrace stripping; the `[Flow:*]` line is the proof.

## MUST (in order; each gated before the next lane loads)
1. **WO-1391** building upgrade page - instrument the preview rig FIRST (model resolved? RT rendered?), read the
   device line, then fix: model-or-icon never noise; the affordability sentence names the shortfall; face fits;
   glyph gone; kit styling. Capture + device proof on the Cathedral page.
2. **WO-1393** close-frame tap grace + queue drawer over the Troops card (QUEUE closes it). Device proof: the
   Night Market must NOT open on a queued tap; UPGRADE TO L4 reachable with the drawer open.
3. **WO-1392** harvest result reconciliation - the two producers agree to the unit; overflow left PENDING,
   never burned; warn before COLLECT. Headless equality pin RED first.
4. **WO-1389** the post-first-raid FTUE beat (why + how to upgrade; guided taps) - HUD voice default, flagged.
5. **WO-1388** Builder's Hour pack ($1.99, small basket, 6 h temporary builder) + funnel telemetry
   (store_opened / pack_tapped / checkout_started / checkout_failed) + `stats.js` store_funnel block. Pack
   name/badge/basket copy flagged for her approval.
6. **The headed walk, resumed** from the HUD: Journey -> Raids -> RaidSelection -> RaidDeploy -> Realm Map;
   Build; Hero (equipment / bag / skill tree / loadout); Talk; Night Market sections; every graph node in
   `docs/qa/UI_SCREEN_GRAPH_2026-09-04.md` (d) with no headless case. PNGs to `docs/qa/UI_REVIEW_2026-09-05/`,
   INDEX.md updated with the trace line per node.
7. **Two independent reviews per subtree** (A-E in the sprint doc) from the PNGs + the graph; merged by the
   CLI with every claim re-read against the PNG/code; ONE ticket per failing screen (mint from the banner);
   owner rulings collected into ONE list for the morning.

## STRETCH (only after every MUST is green and installed)
- **WO-1384b** Night Market card: rounded corners + a chasing, softly colour-shifting glow (owner 23:59, ruling
  on the WO) - HUD lane, tunables on the rail, proven by a 3 s `adb screenrecord` + the aurora cost line.
- The graph's dead ends as tickets: BattlePass registered-never-opened; RealmStore double-registered under
  GOOGLE_PLAY; RealmMap no release door; CosmeticShop unreachable; the two "Night Market" labels; dock
  "Settings" opens HelpMenu; deck cards have no back-to-deck.
- Perf silo, three measurable lanes from `docs/reference/SEEKER_PERFORMANCE_AUDIT_2026-09-04.md`: VFX ambient
  ring shed (one session, re-read the VfxPerfGate line), FlowTrace off for one session (fps off `dumpsys
  gfxinfo`), the intermediate-texture flag on the renderer asset (rebuild, compare baseline).

## PARK (owner's word needed - do not touch)
1373 reward shape (A/B/C) · 1377 type names · 1371 zero-vs-seeded · 1370 wording · 1368 drawer placement ·
1244 command center scope · 1184 lookout warning treatment · 1292 (blocked on 1291) · 1314 (Pi parked) ·
the `collector_farm` name (farm vs quarry) · WO-1389 Q1 voice · WO-1388 pack name.

## MORNING REPORT TEMPLATE (fill as you go; `docs/HANDOVER_2026-09-05_overnight.md`)
- builds installed on the Seeker (version, time, markers), commits (sha, subject), suite count on the last
  green marker, board counts, every FIXED with its felt-test, every finding with its PNG + trace, the rulings
  list, and anything NOT done with the reason.
