# Settings — `DeNelle.Settings`

Settings + pause UI.

## Files

- `SettingsController`, `SettingsModel`, `SettingsBootstrap` — settings panel + persistence
- `PauseController` — pause menu
- `AudioMixerBridge` — volume sliders → mixer
- `MusicToggleBootstrap` — music on/off wiring

> Maintenance: update this README when files are added/removed.
