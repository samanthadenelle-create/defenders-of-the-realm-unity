# GAP AUDIT — 2026-07-18 (fleet sweep, one agent per key system)

> **Point-in-time ledger (frozen per CLAUDE.md §15).** Read-only audit fleet, one agent per
> key system, verified **from code, not comments**. Purpose: surface every gap so the PO can
> queue by leverage. Nothing here was fixed blind; safe reds were cleared separately (see the
> "Already actioned" tail). Severity = shipping/correctness impact, not effort.
>
> Anchor: `CANON_GROUND_TRUTH_2026-07-18.md`. Branch `wip/village2-and-f8-tickets`.

---

## P1 — real, shipping/correctness impact

| # | System | Gap | Where | Note |
|---|--------|-----|-------|------|
| 1 | Dungeons | **Corrupt scene was shipping into builds** — `d4_sunken_crypt.unity` is 57% NUL bytes (§0 mount-garble), git-tracked and in EditorBuildSettings. WO-434's NUL guard only scans `.cs`. A headless re-bake here composed cleanly (rc 0) but the on-disk write **stayed corrupt** (native-Windows read: 22,526 NULs) — the shared tree re-garbles it (§11 two-session hazard). | `Assets/Scenes/DungeonCompose/d4_sunken_crypt.unity` | **HARM STOPPED** — set `enabled: 0` in EditorBuildSettings (excluded from builds, commit pending). **Clean re-bake must run in the dungeon session's OWN worktree**, not this shared tree. |
| 1b | Data | **`waves.json` — the core defense loop — has NO typed regression in the headless gate.** Loaded at runtime (`WaveManager.cs:659`→`WaveData.cs:415`); only parse-checked. A renamed top-level key → empty `WaveSchedule` → 0-wave schedule → **no combat, no gate error**. The exact silent-mapping class the suite exists to catch, on the main loop. | `waves.json`, `DataRegression.RunAll` | Add a typed `Check` asserting `>0` waves + first-wave fields. |
| 2 | Economy | **Split-brain Wood/Iron.** Two independent stores; wave/harvest income lands in the in-session pool (not persisted) while Lumbermill/Forge **upgrade spend reads `GameState.Wood`** (persisted). Wood earned is invisible to the upgrade flow and diverges across reload. `GrantSpendable` (writes both) is used only by `ResourceCollector`+dev tools, not the main loops. | `EconomyService.cs`, `WaveManager.cs:2021`, `ResourceBuildingProgression.cs:498`, `LiveWalletSource.cs:78` | The canon comment misdirects the fix via a **phantom `ResourceLedger`** class that doesn't exist. |
| 3 | Economy | **Paid packs advertise Glimmer but never grant it.** Founder's Vow shows 1000 Glimmer for $49.99, delivers 0 — `ApplyPackContents` applies only Crystals/Food/Coins; Glimmer lives in a separate store `DeNelle.Wallet` can't reach. No `FlowTrace.Fail` on the lost portion. Convenience tokens same. | `PackStore.cs:415`, `PackStoreVM.cs:82-101` | Real-money undelivered entitlement. |
| 4 | Combat | **Enemy stat divergence, no SSOT.** `orc-raider` (and the rest of the un-JSON'd roster) is hand-built in **4 sites** with different HP/AI/speed: hp 95 charger (3 open-world copies) vs hp 170 skirmisher ×1.2 difficulty = **204** (garrison). Any balance edit must hit 4+ files or drift. | `RegionMobSpawner.cs:549`, `EnemyOutpost.cs:879`, `CampGuards.cs:206`, `GarrisonStatBlocks.cs:121` | The triplicated `BuildGuardDef` switch is the drift vector. Move to `enemies.json`. |
| 5 | Core/Save | **`Tribes` + `Wards` never persisted** — earned exploration/clear progression resets every reload (in-memory only, absent from SaveSchema/Snapshot/ApplyPersisted). `Arena` W/L round-trips out-of-band via PlayerPrefs and is **not cleared on New Game** (stale world state on replay). | `GameState.cs:223,238,306`, `GameStateService.ResetToNewGame:790` | Data-loss defect, not debt. |
| 6 | Core/Save | **Save-integrity tests are blind to the newest ~20 persisted fields.** Round-trip test asserts only the v1–13 ~41 fields — never BaseLayout/Army/BuildingTiers/EchoLanes/Hero*/Population*/GearInventory/etc. Migrator tests stop at v10 (**16 steps incl. the v18 crystal-fold untested**). A dropped v14+ field or broken migration **ships green.** | `Tests/SaveLoadRoundTripTest.cs:216`, `Tests/SaveMigratorTest.cs` | The most regression-prone code (60 hand-copied field lines) has the weakest guard. |
| 7 | HUD/MVVM | **WO-744 "MVVM COMPLETE" is over-claimed** (oracle empty-baseline + HardFailOnNew, dated today). `BuildMenu.cs` **spends the economy + upgrades towers from the View** (header lies "reads only VM data"); `CosmeticShopPanel` has **no VM at all**; the oracle only scans uGUI-*constructing* files, matches the VM-routing exemption against **comments**, and omits key banned symbols — so it **cannot see** either miss. The `[ui-mvvm]` gate can rot silently. | `UiMvvmConformanceRegression.cs`, `BuildMenu.cs:406/524`, `CosmeticShopPanel.cs` | Needs a WO-744 follow-up + oracle tightening. |

## P2 — real defects, player-felt / build-visible

| # | System | Gap | Where |
|---|--------|-----|-------|
| 8 | Combat | **Casters walk while channeling** — `DriveNav` has no `_casting` guard, re-sets `isStopped=false` every frame and overrides the cast root. Already instrumented in-code as "the bug" (F8-38); one-line fix never applied. | `Enemy.cs:967-997` |
| 9 | Combat | **ATB engine-def collapse** — `EngineDefId` maps everything except necromancer/hollow-warrior to `"skeleton"`, so a dragon/troll/berserker breach fights as a skeleton (stats, not just visual). | `Enemy.cs:489-501` |
| 10 | BuildMode | **Arcane Spire upgrade scales nothing but VFX** — `ApplyTierStats` handles only `DefenseTower`+`WallSegment`; `ArcaneTower` is a separate class, so L2/L3 charge escalating cost and change no Range/Damage. Owner's own "Upgrade does NOTHING" note quoted at the site. One-line fix (add an `ArcaneTower` case). | `BuildModeController.cs:2191` |
| 11 | BuildMode | **HealingFountain has two disconnected upgrade systems**, neither persists heal-rate correctly (BuildMode path bumps `ps.level` but not `_currentLevel`; the fountain's own UI bumps `_currentLevel` but never writes BaseLayout → resets on reload). | `HealingFountain.cs:70-88`, `BuildModeController.ApplyTierStats` |
| 12 | BuildMode | **Gate-clearance guards the gate mouth only, not the interior** — a solid mid-field wall of towers carves NavMesh with no rebake and can strand enemy agents from the Heart. Softlock **iff** `EnemyBrain` idles instead of attacking the nearest blocker (unverified — needs a capture). | `BuildModeController.cs:1563`, `BaseLayoutLoader.cs:448` |
| 13 | HUD/MVVM | **UXML panels render empty in builds** (canon §8) — the **money** path `JupiterSwapPanelController`, `ArenaDefensePaletteUI` (its twin was already rewritten to uGUI for this exact reason), and the dungeon `DungeonHudController`/`CraftingPanelController`. Headers claim "code-built, no UXML" — lies. | those files |
| 14 | HUD/MVVM | **`EndStateView` bypasses the single-modal arbiter** (never calls `PanelManager.NotifyOpened`) → death popups stack. Self-flagged F8-15, not fixed. | `EndStateView.cs:62-135` |
| 15 | Dungeons | **Room Forge output is geometry-only** — a baked compose scene has no `DungeonController`/hero/camera/`Lantern`/encounters; the socket-format and the legacy wall-format dungeon systems **never meet**, so forged dungeons get none of the felt mechanics. **Tincture** light-shrink and **Lightbearer Cloak** reward are fully built but have **zero callers**; `Lantern` has **no FlowTrace** (can't headless-verify oil). | `DungeonBaker.cs:78`, `Lantern.cs:205/278` |
| 16 | Onboarding | **`world_encounter` FTUE step = single point of failure** — non-skippable, its only enemy is one staged rep gated behind `ff.overworldencounter`, whose own comment says "REVERT to false." Flag flip → nothing to fight → silent 120s watchdog stall. The watchdog masks all missing signals as 2-min waits. | `TutorialFlow.cs:843`, `FeatureFlags.cs:151` |
| 17 | Onboarding | **Pet-vs-workforce Echo duality** — FTUE grants a **pet-roster** echo but the `founding_echo`/`ctx_echo_assign` lessons teach the **workforce `EchoService`** (separate counter). Verify the founding pet appears as an assignable workforce Echo, or "assign it" points at an empty roster. | `TutorialFlow.cs:690`, `EchoUnlockFeedback.cs:72` |
| 18 | Economy | Jupiter swap devnet-wallet vs **mainnet** aggregator; **SKR mint empty** yet SKR is the store's default+only rail; release-build swap **no-ops but returns `true`** (no `onError`). Works today only because the stub fabricates balances. | `JupiterSwapService.cs:51/65/309`, `PackStore.cs:53` |
| 19 | Arena | **`ArenaGround` material binds no base texture** (flat green). A textured `Grass_1.mat` already ships in-folder; `ArenaGroundFix` exists but only fixes `Ground`, misses `DistantGround`. Needs an in-editor material bind (safer than YAML). | `ArenaGround.mat`, `ForestClearingArena.prefab:556/1096` |

## P3 — hygiene, dead code, doc/comment staleness (canon §15 debt)

- **Stale catalog docs / comment-vs-code lies:** `battle-atb.md` flags F-WAVE-1/F-SWAP-2/F-MGR-1 are **all fixed in code** (mark resolved); `core.md` is ~13 versions behind (says v20, lists Settlements/PetName as not-round-tripped — both wired); `GameState.cs` comments lie about Settlements/PetName (wired) + EchoLanes (stale v31 grammar); stale file-header banners across SaveSchema/SaveMigrator/GameState/GameStateService ("v10/41-field" vs actual v33/~60).
- **ASCII/tofu in runtime strings:** em-dash `—`, `·`, `»` (U+00BB, labeled "ASCII marker" — lie), `…`, `→` in `ShopVM`/`EquipVM`/`RumorBoard*`/`HeroSkillTree*`/`WorkshopCraftVM`/`MusicSelectionPanel`. The `»` collapses music selection to a color-only cue if it tofus.
- **Dead code:** phantom `StakingBonusManager` (reflection no-op per SKR purchase) + phantom `ResourceLedger` (comment only); `HUDManager` described in READMEs but **doesn't exist** (`VirtualDPadLean` orphaned); dead ATB switch branches (`orc-warrior/tank/mage`); `CombatantDefSO` family has no `.asset`; `LocalSaveProvider.Delete` sibling ".sig" cleanup; offline `SendDelta` re-serializes full snapshot (delta payload is dead code).
- **Room Forge P3:** `DungeonComposeLayout` silently drops its `"version"` field (no migration hook); Case1 checks socket id/type/count but not position/orientation; stair mating code-supported but no sample layout/test exercises it.

---

## Already actioned this sweep (committed / in flight)
- **Reds cleared (commit `04dab9c2`, gate 8→6):** HUDUI (4 tofu glyphs → ASCII + 1 stale baseline); fountain L2/L3 null-load (blanked aspirational ladder → Well base).
- **DATAWEB:** 3 pure-staleness copies synced; **armor/weapons HELD** — StreamingAssets holds `blink_*` gear referenced live by `HeroBodySwapper` class-default armor + `SaveIntegrityRegression`. **Owner ruling needed: is blink gear dead or live?**
- **Corrupt d4 scene:** headless re-bake in flight.
- **Canon:** `SESSION_CANON_LOADER.md` body brought forward 07-12→07-18 (was self-contradicting its own top banner).

## Remaining reds after this sweep (baseline)
arena-ground (in-editor bind = P2 #19), B2 dual-wallet (P1 #2), pet active-slot (save-schema), core-save Tribes/Wards/Arena (P1 #5), DATAWEB armor/weapons (owner ruling), orc-raider (P1 #4). HUDUI + fountain now green.

## Data-catalog layer (audit complete)

The catalog layer is **well-defended**: all 48 loaders wrap their parse (no uncaught `DeserializeObject`), dual-copy drift is clean for the paired set, no id collisions, WebGL-safe empty fallbacks. Gaps are **coverage**, not active silent-break defects:

- **P1 — `waves.json` typed-gate hole** (see row 1b above).
- **P2 — both generic gates iterate the StreamingAssets root only** → any Resources-only catalog escapes version + drift + non-empty enforcement. Live example: `widget-params.json` has **no `version`** and is never flagged. Fix: iterate the **union** of both roots. | `CoreDataHubRegression.cs:62`, `DataWebRegression.cs`
- **P2 — Tier-1 core-loop catalogs are parse-only** (no typed `>0`/field assert): `heart.json`, `enemy-roles.json`, `spawn-areas.json`, `towers.json`, `walls.json`, plus `gear-recipes.json` + `crafting-recipes.json` (live loaders, zero typed regression). A field rename silently empties them with no gate line.
- **P2 — 4 async loaders catch only `JsonException`, not `Exception`** (`WaveData.cs:434/464`, `DungeonLayout.cs:307`, `CraftingData.cs:226`, `LoreFragments.cs:133`) — a non-Json throw (custom-converter `FormatException`) escapes instead of degrading; the other 44 loaders catch general `Exception`.
- **P3 — orphaned canonical data:** `ad-creatives.json` + `ad-placements.json` have **zero consumers** (their interpreters `AdCreativeGenerator`/`AdGateService` don't exist) — parked-unbuilt; versioned data that will drift unnoticed. `dungeons/healers-cottage.json` has no coverage of any kind.
- **P3 — confirms the DATAWEB hold** (row in "Already actioned"): the resolved weapons copy is the 34-weapon one, but `DataRegression` comments reference "433+ weapons" (the copy Resources overwrote) — corroborates that armor/weapons need an owner ruling before syncing.

Full catalog coverage matrix (dual-copy / in-sync / version / typed-reg for all ~60 catalogs) captured in the audit transcript.

---
*Ledger compiled by the audit fleet (one agent per key system). Route by leverage; nothing here is a blind fix.*
