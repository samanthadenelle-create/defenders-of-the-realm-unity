# Resume — morning of 2026-08-25

**Written 2026-08-24 late, by the CLI lead seat, immediately before owner shutdown.**
The shutdown IS the reboot: it clears the commit-charge leak that aborted the regression three times.

---

## ⭐ START HERE — the first three commands, in this order

The machine is fresh, so the gates can finally run. **Judge every one by its MARKER on a fresh
log, never by the exit code** — this repo's runners exit 0 on refusals and FAILs.

```powershell
# 1. Compile gate
.\run-unity-method.ps1 -Method 'DeNelle.Editor.CompileGate.Run'      # want: COMPILE_GATE_OK

# 2. The full regression — THIS HAS NEVER COMPLETED. Three aborts, three different points.
.\run-unity-method.ps1 -Method 'DeNelle.Editor.DataRegression.RunAll' # want: REGRESSION_OK <n>/<n> suites

# 3. UI capture — and OPEN THE PNGs. Compile-green never proves a panel looks right.
#    (find the capture entry point in Assets/Editor/UICaptureLaunch.cs)  # want: UI_CAPTURE_OK
```

⛔ **Before running ANY of them, confirm the P0 below is actually fixed in the tree.**

---

## ⛔ THE P0 — verify it before trusting a single gate result

`tools/assert-unity-editor-pin.ps1` had no explicit `exit 0` on its success path. In PowerShell an
unset `$LASTEXITCODE` is `$null`, and `$null -ne 0` is **TRUE** — so every caller's guard fired on
SUCCESS and `exit $null` exited **0**.

**Net effect: `run-unity-method.ps1` — the runner every gate in the repo goes through — returned
success WITHOUT LAUNCHING UNITY.** Also affected: `build-windows.ps1`, `build-webgl.ps1`,
`run-tests.ps1`, `tools/run-unity-playmode.ps1`.

A fix was in flight at shutdown (explicit `exit 0` + hardening the five callers' guards against an
unset `$LASTEXITCODE`). **Confirm it landed, and confirm the success path empirically** — run a
gate and check that Unity actually started (a real log with real timestamps, not an instant return).

⚠ **Root cause, recorded so it does not repeat:** WO-1178's acceptance criteria listed only
FAILURE-path tests (exit 9 on a bad pin, exit 8 on a missing marker) and never required proving
that a *correct* pin lets the run proceed. A gate needs acceptance on both paths.

---

## What landed 2026-08-24 (all committed, or staged in one wave at shutdown)

| Area | State |
|---|---|
| **APK on the Seeker** | 545 MB, `Performing Streamed Install / Success`, `R2_PARITY_OK 43` verified at install |
| **Mainnet purchases SETTLED** | ⭐ card SKR and wallet SKR both **391**, approved and settled on chain. ⚠ Corrected below: this was the SECOND, not the first — one settled earlier the same day, so the rail REPEATS |
| **Board** | clean: 0 malformed, 0 contradictions, 0 fallback-bucketed, 0 unlabeled. Ready 29 / Fixed 124 / Done 359 / Closed 523 |
| **`BOARD_CHECK_OK`** | now prints on EVERY run, not only under `--check` (a marker you only get with a remembered flag is not a gate) |
| **`UI-###`** | taught to the board parser; UI-001 / UI-002 render as themselves |
| **MON002** | renamed to **WO-1079**, banner bumped 1079 → 1080 in the same edit. UI-seat next mint: **1080**. CLI next mint: **1186** |
| **WO-823** | verified-facts block added at lines 434–475 (see below) |
| **Panels WO-1075 / 1077 / 1078** | dev lane implemented, lead-verified, **ACCEPT WITH NOTE** — no regressions |
| **WO-1076** | ⛔ REFUSED CORRECTLY — already shipped in `a2162f17d` |

---

## ⚠ Open — needs the owner

1. ~~Did the purchased contents land in the bank?~~ **ANSWERED** — the owner's full felt-test came
   back clean, so the grant landed. Money path proven end to end: card → wallet → chain → server.
2. **Server-side proof of the settlement.** Owner runs SQL (no approved DB executor in any agent
   lane):
   ```sql
   SELECT sku, network, amount_base_units, decimals, usd_anchor,
          discount_bps, discount_reason, issued_at, consumed_at, consumed_tx
   FROM purchase_quotes ORDER BY issued_at DESC LIMIT 5;
   ```
   ⭐ `discount_bps` **must come back NULL** on that row. If it is not null, a discount was applied
   to a purchase that should not have had one.
3. **The felt-test route** — `FELT_TEST_WALKTHROUGH.md`, ordered by where she stands in the game.
   The store section is now partly proven; the rest is not.

---

## ⚠ Known defects carried into the morning

- **`PurchaseQuoteService.cs:143`** — `UsdApproxLabel` uses the **undiscounted** `UsdAnchor`, so a
  discounted approval screen reads *"send exactly 240 SKR (~ $2.99). 20% shortfall discount applied."*
  Discounted token amount beside a full-price USD. Needs a ticket.
- **`repair_shortfall` is an eligibility input, not just an audit label.** A forged `reason` does buy
  a discount; the amount and the one-per-7-days window are server-owned, so the exploit ceiling is
  **one 20%-off quote per authenticated wallet per 7 days**. Ruled acceptable — but restate it in
  the WO in those terms, not as "the hint is never authorization."
- **`tools/verify-dungeons.ps1` does not parse** ("string is missing the terminator") — pre-existing
  at HEAD. It has never run. Same class as `checkin_gate.ps1` looking like a gate for months.
- **13 orphan bundles** in the R2 bucket, unnamed by the current catalog. Dead bytes, not a
  correctness problem. Worth a cleanup ticket.
- **WO-1075 shifts every line below 480 in `RaidDeployScreen.cs` by +12**, so WO-823 Phase E's line
  references (`:477`, `:526`) will not resolve after it lands. Coordination item, not a defect.

---

## ⭐ The through-line of the night — three defects, one shape

1. **WO-1076 was already fixed and sat READY**, and was handed to a dev seat anyway.
2. **WO-1077's premise was contradicted by its own source** — the button was already reachable.
3. **Four panel tickets were minted from `Builds/wo1060-capture.log`**, an aged capture describing a
   build that had moved on; their claimed finding-drops sum to more than the baseline allows.

All three are the project's dominant failure mode: **one fact recorded in two places, and the copies
drifted.** Same as the stale WO-number block and the retired dependency table in `CLAUDE.md`.

A WO was minted to attack the mechanism for the capture case — the fix must be something the harness
or the board can ENFORCE (capture logs record the HEAD commit they were taken at; a minted ticket
cites it, so staleness is mechanically detectable). **A rule that lives only in prose goes stale.**

⚠ **And one correction I owe the record:** I told the owner mid-session that Codex and I had each
built WO-1177 and WO-1178 twice. That was **wrong**. `D:\eoa-codex-batch1-clean` is a *linked
worktree* parked on an older commit, so its `git diff` reported my own committed work as
"uncommitted modifications." All 16 files were byte-identical, proven by hash. Nothing was
duplicated and there is nothing to merge — ⛔ and that worktree must NOT be merged as a branch,
because its baseline predates the landed commits and a merge would resurrect both tickets as
conflicts.

---

## Dev lane state (three-seat loop)

Owner couriers `BATCH_STATE.md` (lead → dev) and `batch_results_state.md` (dev → lead) **by hand**.
Both are append-only by section.

- **Batch 7 (panels)** — handed back, lead-verified, ready to commit.
- **WO-1163** (resource ladder / Stone) — worktree `D:\eoa-codex-1163`, branch `codex/wo1163`,
  **clean with no commits ahead** at shutdown, so it had not started. Its file lock is a declared
  scope, not an observed diff.
- **Next vetted batch** is ready to hand out — five candidates, file-disjoint from each other and
  from WO-1163: **PROD-014 slice (b)**, **WO-875**, **WO-814**, **PROD-012 ruling 2**, **WO-970**.
  ⚠ Confirm with the owner she has not already couriered these across — four appear in BATCH_STATE's
  "WORK AVAILABLE NOW" block, and delivery is unknowable from the repo.

**Needs a ruling before it can be handed out:** WO-1171 §4 (wallet connect/disconnect placement +
confirm wording), WO-1129 §3.3 (mint `AssetRoots.IncomingTripo` or is that folder about to be
deleted), WO-823 Phase E E4 (veteran-save residual) — and E is blocked on WO-1075 landing anyway,
same file, same method.

**Already done, pull from Ready:** WO-1128 is code-complete; only gates and the owner's felt-close
remain.

---

## WO-823 — settled, so nobody re-opens it

Slot cost **already** scales with the unit: `TroopDialogueCommands.SlotOf` reads per-troop `slots`
from `troops.json`. Footman/archer **1**, shieldguard/outrider/battlemage **2**, echo-legionnaire
**3**, catapult **4**. So 3 slots is at most ONE elite, and the catapult does not fit at all.
⭐ At Barracks tier 1 only footman and archer are trainable, both 1 slot — so a fresh save's first
raid is three tier-1 basics **by construction**. ⛔ **THE NUMBER 3 STAYS.**

The real remaining work is **presentation**: render the deploy budget as a slot meter that fills by
each unit's own cost, so a 2-slot troop visibly eats two of three. "3 of 10" reads as "three troops."

⛔ **Phase E is NOT BUILT** — `FirstRaidMinDeployableSlots` / `FirstRaidSoftGate` return zero hits.
The live gate is still WO-820's full-army rule.

---

## Path to production

```
reboot (done by shutdown) → COMPILE_GATE_OK → REGRESSION_OK → UI_CAPTURE_OK
   → gate outstanding handbacks → owner felt-test → push
```

`R2_PARITY_OK` is already green (43 objects, re-verified at install).

⚠ **`.githooks/pre-push` will refuse** any push where `ServerData/` is newer than
`Builds/r2-parity.log`, or where that log lacks `R2_PARITY_OK`, **or** where the editor pin is
wrong — the pin check now runs unconditionally, so it can block even a docs-only push. There is
deliberately **no override flag**. The only remedy is `tools\r2-ship.ps1`.

The app is already live on the dApp Store, so the next submission is an **UPDATE**.

---

## Landed after the main note was written (late 2026-08-24)

**⭐ The playtest came back clean.** Owner ran a full session on the shipped APK and reported
"not a single issue other than guessing what the crystal mine does." Two settled mainnet
purchases confirmed in `purchase_quotes`: `impulse-iron-medium` at 17:41 and
`impulse-wood-medium` at 02:44, both 391000000 base units, decimals 6, `discount_bps` NULL,
real `consumed_tx` signatures. **The rail has now repeated** — that was the open question.

**Two tickets were in flight at shutdown** (agents finishing unattended; check `WorkOrders/` and
the banner for their numbers):

1. **The crystal mine.** ⚠ The premise was corrected mid-flight and the correction is the whole
   point: she **never opened the card** — *"i didnt open the card, only placed cause it was an
   item."* So the defect is not necessarily bad card copy. It is that **a player placed a building
   into their town without ever learning what it does**, because the palette presented it as a
   placeable item and nothing in the flow required or invited knowing more first. If placement is
   reachable with fewer gestures than reading, that asymmetry is the defect.
2. **Collector ordering.** Owner asked for collectors LAST in the palette array, because they are
   the only items that "dont get free" and take 7 minutes to build. ⚠ Her phrasing is ambiguous
   between *the only items that cost resources* and *excluded from the free build slots* — the
   agent was told to prove which from source and flag it for her. Also verify the 7 minutes, and
   settle how "last in array" interacts with **WO-1167**'s category grouping, since grouped
   headers may mean last-in-array never reaches the player as last-on-screen.

**⛔ WO-1163 is BLOCKED and needs an owner ruling — deliberately parked overnight.** The dev lane
refused it correctly: the ruled ladder requires Wood/Stone/Iron plus Gold, but the live tier
contract carries only Wood/Food-slot/Crystals with no Iron or Gold fields and no conversion
amounts. Guessing would reprice a **live economy that started taking real money today**. An agent
was verifying the gap and preparing recommendations; read its findings before putting the question
to her. ⚠ This is the second correct dev-lane refusal of the session — the two-seat verification is
doing its job.

---

## ⭐ THE MORNING MANDATE (owner directive, 2026-08-25 pre-shutdown)

> *"After I reboot, can you run headed and non headed test cases on the fixed ones?"*
> *"and resolve any findings"*

**Run BOTH kinds against the Fixed set, then FIX what they surface — do not hand back a report.**

| Kind | Proves | Cannot prove |
|---|---|---|
| **Headless** (`DataRegression.RunAll`) | data, catalogs, economy, save/migration — anything marker-judged | anything about pixels or input |
| **Headed** (UI capture, open the PNGs) | resolved rectangles, touch-floor measurements, **which control wins a tap** | anything about server state |

**Specifically owed:**
1. The **raycast-identity capture** WO-1075 / WO-1077 / WO-1078 each ask for. They were accepted on
   source reasoning about uGUI sibling order — sound, but not data.
2. A **fresh `UI_TOUCH_FAIL` count**, which settles the arithmetic three tickets disagreed about and
   shows whether WO-1075's ~9.5px spill created a new overlap against the shared Close.
3. `[economy-credit-reporting]` — registered, committed, and **has never executed once**.

⚠ **Report in TWO buckets, never one number:** *proven by a test that exists* vs *flipped on reading*.
Most of the 124 Fixed rows were flipped on source evidence, and a run can only exercise what someone
wrote a suite for. The second bucket is where the next round of test-writing goes. A green marker
must never be allowed to imply coverage it does not have.

**Resolve findings in the same pass.** Anything the runs surface gets fixed, gated and committed —
except what genuinely needs an owner ruling, which parks with the specific question stated in one
sentence. ⛔ Never guess a ruling to keep momentum; that is what nearly repriced a paid economy.

**Already-known findings to resolve in this pass** (all detailed above): the undiscounted
`UsdApproxLabel` on the approval screen, `tools/verify-dungeons.ps1` not parsing, the 13 orphan R2
bundles, and restating the `repair_shortfall` exploit ceiling honestly in its WO.

⭐ **The conservative WO-1163 slice can ship in this same pass** — unlock `collector_forge` in
`build-categories.json` and repoint its `satisfiedByStructureIds` off `armorer`. It dissolves the
iron deadlock with zero cost changes, zero money-path exposure and zero schema movement, and needs
none of the four parked rulings.
