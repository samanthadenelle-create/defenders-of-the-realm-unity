# WO-1023 RESULT — Talent icon map integrity

**Status:** IMPLEMENTED — PENDING GATE (edit-only lane; lead session gates + commits)
**Implemented:** 2026-08-15

## What was done

### 1. The two duplicate icons re-tagged (blinkSource only; iconPaths unchanged, so hero-talents.json untouched)

| node | was | now | greyscale distinctness vs the kept twin |
|---|---|---|---|
| `ranger.t2n2` Venomcraft | `Rogue7.png` (dup of knight.t2n6 Venombrand) | **`Classes/Assassin/Rogue/Rogue6.png`** | Rogue7 = a fist gripping a demon-skull dagger — one dark vertical mass on the right. Rogue6 = a horizontal blade laid across a cauldron with dripping strands — bright horizontal bar over a round dark bowl. Silhouettes share nothing; reads apart with all hue removed. Semantically it is the most literal "poison CRAFT" in the Rogue set. |
| `shared.n9` Arcane Bolt | `Arcanist1.png` (dup of mage.t1n1 Arcane Focus) | **`Classes/Elementalist/Arcanist/Arcanist17.png`** | Arcanist1 = ONE braided comet streaking upper-right. Arcanist17 = THREE parallel bolt comets streaking lower-left. Count (1 vs 3) and direction both invert — distinct by shape, not hue. Reads exactly as "ranged magic dart". |

Both picks come from the unused pool (ranger used Rogue1/3/4/7; the map used Arcanist 1–12,15,18,20 — 13/14/16/17/19 free; 16 is a human figure, 14/19 are a squiggle and a rune ring — 17 is the only bolt-reading candidate). `why` rationales written for both entries per acceptance.

### 2. Files changed

- `tools/apply_talent_icon_map.py` — the two SKILL_MAP rows (the script regenerates the map JSON, so it had to move in the same edit or a rerun would revert the re-tag)
- `Assets/Resources/Data/Canonical/talent-icon-map.json` — regenerated by running the script; diff is exactly the 2 entries
- `Assets/StreamingAssets/Data/Canonical/talent-icon-map.json` — synced, hash-verified **byte-identical**
- `Assets/Resources/Talents/ranger/ranger_07.png` + `Assets/Resources/Talents/shared/shared_09.png` — replacement art copied from the Blink pack (present on this machine) by the script (83/83 copied); PNGs opened and visually confirmed to be the new art. Existing `.meta` files retained (same GUIDs — no scene/prefab rewire). Both are LFS-tracked.
- `docs/BLINK_ICON_MAP.md` — regenerated by the script (2 rows)
- `Assets/Editor/Regression/TalentIconMapRegression.cs` — **NEW** oracle (needs its `.meta` generated at the gate)
- `Assets/Editor/Regression/DataRegression.cs` — registration line for the new suite, above the END fence / regression-marker suite

### 3. The oracle — `[talent-icons]` (`TalentIconMapRegression.Run`)

Marker `TALENT_ICON_MAP_OK` / `TALENT_ICON_MAP_FAIL`. Asserts all five WO §2 points:
1. exact 1:1 node↔entry coverage (all trees + Shared pool via the REAL `HeroTalentCatalog`; no unmapped, no orphan, no duplicate entry)
2. no duplicate `blinkSource` (pins the §1 defect shut)
3. every `iconPath` loads via `Resources.Load<Sprite>` — the SAME call `HeroSkillTreePanelMvvm.LoadIcon` makes at runtime, so it stays honest in a player build
4. map `iconPath` == `hero-talents.json` node `iconPath`
5. Resources vs StreamingAssets map copies byte-identical

### 4. Brace / NUL gate (CLAUDE.md §1)

- `TalentIconMapRegression.cs`: 44/44 braces, no NUL — OK
- `DataRegression.cs`: 837/837 braces, no NUL — OK

## What remains (lead session)

- `COMPILE_GATE_OK` (also generates the new oracle's `.meta`)
- `REGRESSION_OK <n>/<n> suites` with `[talent-icons]` counted (the suite-count denominator self-derives from source, so the new registration is picked up automatically)
- **Prove-the-red** acceptance item: temporarily break one assertion (e.g. flip a `blinkSource` back to `Rogue7.png`), show `TALENT_ICON_MAP_FAIL`, restore — needs a batchmode run, forbidden in this edit-only lane
- Commit by explicit path (JSON ×2, 2 LFS PNGs, script, doc, 2 `.cs` + new `.meta`, WO + RESULT)
- Owner felt-verify: open the talent tree, confirm the four affected nodes (Venombrand/Venomcraft, Arcane Focus/Arcane Bolt) render distinct art
