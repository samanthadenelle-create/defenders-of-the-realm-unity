# WORK ORDER 103 — Village Scene Rebake

**Status:** READY TO IMPLEMENT
**Date:** 2026-05-29
**Priority:** Urgent — scene is stale; all recent fixes are code-only and will not appear until the builder reruns
**Scope:** Small — single batchmode command, no code changes

---

## Justification

The following changes have been merged to `VillageSceneBuilder.cs` since the last village build and require a rebake to take effect in `Village.unity`:

| Change | Work Order / Issue |
|---|---|
| Polyperfect building mesh swap (Phase A) | WO-101 |
| Wall perimeter + gates added | WO-101 |
| Building positions corrected (Farm moved off north gate) | DEF-101 |
| Enemy spawn points placed 12 m outside each gate | DEF-101 |
| Hero GO gets `tag = "Player"` | WO-102 / WO-101 |
| Dungeon gate/castle door URP material assigned | WO-97 / DEF-99 |
| Gate-clearance validation assertion added | DEF-101 |

**Do NOT hand-save `Village.unity`** — use the builder only (corruption-on-resave history per PIPELINE_STATE.md §4).

---

## Command

With Unity editor **closed**, run from the project root:

```powershell
powershell -ExecutionPolicy Bypass -File .\run-unity-method.ps1 `
    -Method DeNelle.Editor.VillageSceneBuilder.BuildVillage `
    -LogName village-rebake-wo103.log
```

---

## Verification

After the batchmode run completes, confirm in `Builds/village-rebake-wo103.log`:

- [ ] `Exiting batchmode successfully` present — no compile errors
- [ ] No `error CS` lines
- [ ] No `[ValidateBuildingGateClearance]` error lines (would mean a building still overlaps a gate)
- [ ] `Village.unity` modified timestamp updated

Then open the scene in editor and confirm:
- [ ] Farm building is not blocking the north gate
- [ ] 4 `SpawnPoint_*` GameObjects exist in the Hierarchy outside each gate
- [ ] No purple materials on gate/wall meshes
- [ ] Wall perimeter visible around the arena

---

## Files Changed (by prior agents — rebake propagates them)

| File | Status |
|---|---|
| `Assets/Editor/VillageSceneBuilder.cs` | Already edited — rebake applies it |
| `Assets/Scenes/Village.unity` | Will be regenerated by this WO |
